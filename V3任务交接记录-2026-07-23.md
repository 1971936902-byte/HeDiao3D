# HeDiao3D V3 任务交接记录

日期：2026-07-23
分支：`HeDiao3D_V3`
仓库：`github.com/1971936902-byte/HeDiao3D`
当前最新提交：`a94fb24 Refresh trial plan after evidence imports`

## 1. 当前目标

V3 目标仍然是按五层架构完成安全试雕闭环，并逐步接近真实 CAM 生产证据链：

```text
前端 HeDiao3D
  素材、模型预览、参数、中文工艺流程、报告、下载

后端 Orchestrator
  任务队列、文件缓存、模型修复、调用外部 CAM 引擎

CAM 引擎层
  FreeCAD CAM / BlenderCAM / OpenCAMLib
  更专业的刀路生成

仿真层
  CAMotics + 自研旋转包裹预览
  材料去除仿真、空跑验证

后处理层
  自研三轴控制器 + Y轴旋转夹具专用 NC
```

当前不能标记为完成。项目处于“安全试雕/工程验证阶段”，不是“下载 NC 后直接生产成品”的阶段。

## 2. 当前服务器信息

用户反馈服务器已重启，并提供了新的 SSH 端口：

```text
移动 SSH: root@223.109.239.30 -p 30220
电信 SSH: root@180.127.11.167 -p 30220
项目路径: /opt/hediao3d
服务: hediao3d-api / nginx / hediao3d-cloudflared
```

交接注意：不要把密码/API Key 写入仓库。后续接手人需要重新连接服务器后确认 host key、服务状态和 cloudflared 公网地址。

建议连接后执行：

```bash
cd /opt/hediao3d
git status --short --branch
git log -1 --oneline
systemctl is-active hediao3d-api nginx hediao3d-cloudflared
curl -fsS http://127.0.0.1:8787/api/health
```

## 3. 最近已完成的关键进展

### 3.1 生产残料/过切门禁收紧

生产包解锁现在要求真实材料去除仿真和残料/过切证据同时闭合：

```text
simulationEvidence.realMaterialRemovalVerified=true
simulationEvidence.productionUnlockEligible=true
simulationEvidence.level=material-removal-verified
simulationEvidence.residualClosureReview.productionResidualEvidenceReady=true
```

没有 `residualValidation` 时，即使 CAMotics/等效材料去除结果已回填，也只能作为安全试雕/工程验证证据，不能解锁 production-package。

关键测试：

```bash
npm run test:v3:production-package-unlock
npm run test:v3:camotics-material-validate
npm run test:v3:camotics-result-api
```

### 3.2 锁定生产包的操作员指引

`GET /api/orchestrator/jobs/:id/production-package` 返回 `423` 时，现在会带：

```text
operatorGuidance.materialRemovalGate
```

其中包含：

```text
id=material-removal-proof
status=review/block/pass
summary
residualEvidenceRequired
nextActions
```

前端会优先展示后端返回的 `operatorGuidance.materialRemovalGate`，避免前后端各自解释生产锁。

关键提交：

```text
e9a5a3b Expose locked production residual gate guidance
5015cf6 Surface residual production gate guidance
```

### 3.3 安全试雕包离线文件同步残料门禁

安全试雕 ZIP 内的离线说明现在也会携带材料去除/残料门禁，避免操作者下载后脱离网页判断不清：

```text
safe-trial-execution-plan.json
mvp-operator-status.json
mvp-operator-status.md
next-action-checklist.md
```

关键提交：

```text
8d26746 Carry residual gate into trial artifacts
```

### 3.4 证据回填后刷新安全试雕计划

CAMotics/等效材料去除结果、机床验收、试雕反馈等证据回填后，`refreshEvidenceDeliveryArtifacts()` 会重算：

```text
safe-trial-execution-plan.json
next-action-checklist.md
mvp-operator-status.json/md
package-integrity.json
production-closure-audit.json/md
```

这样安全试雕包中的离线计划和最新证据状态保持一致，且 `package-integrity.json` 会重新记录刷新后的 `safe-trial-execution-plan.json` 哈希。

关键提交：

```text
a94fb24 Refresh trial plan after evidence imports
```

## 4. 最近验证记录

本地最近通过：

```bash
node --check server.mjs
node --check scripts/v3-camotics-result-api-test.mjs
npm run build -- --emptyOutDir false
npm run test:v3:focused-ui
```

服务器最近通过：

```bash
npm run test:v3
npm run test:v3:camotics-result-api
npm run test:v3:production-package-unlock
curl -fsS http://127.0.0.1:8787/api/health
```

注意：服务器重启后需重新确认服务状态和公网 cloudflared 地址。

## 5. 当前整体状态

已可用：

- 导入/载入佛头 3D 模型。
- 原始 `.stl/.obj/.glb/.gltf` 导入成功/失败双路径。
- V3 Orchestrator job、文件缓存、交付清单。
- 三轴控制器 + Y 轴旋转夹具 `wrapY` 后处理。
- 安全试雕包、离料空跑、旋转标定空跑。
- CAMotics/等效材料去除结果回填。
- Native CAM / OpenCAMLib 真实输出证据回填入口。
- 试雕反馈和机床验收回填入口。
- production-package fail-closed 安全门禁。

仍未完成：

- OpenCAMLib 从 `experimental-real-api` 推进到稳定 `production-candidate`。
- strict contact validation 从 critical 推进到 ready。
- 残料/过切指标从工程估算升级为 measured 或 swept-volume/material-removal validated。
- 用户真实机床的离料空跑、旋转标定、软料试雕、机床验收回填。
- 真实机床 Profile 固化，包括 Y 轴每圈等效行程、方向、回差、限位和控制器 G-code 方言。

## 6. 建议下一步

优先顺序：

```text
1. 重连新服务器，确认服务恢复和公网地址。
2. 在服务器执行 npm run test:v3:camotics-result-api 和 npm run test:v3:production-package-unlock。
3. 使用固定佛头样件继续推进 Linux OpenCAMLib 真实候选输出。
4. 补 residualValidation.maxGougeMm / maxUndercutMm 的 measured 或 swept-volume validated 证据链。
5. 回填真实离料空跑、软料试雕和机床验收记录。
```

继续保持原则：

```text
没有真实 CAM candidate -> 不解锁生产 NC
没有真实材料去除 + 残料/过切闭合 -> 不解锁生产 NC
没有空跑/试雕/机床验收同包哈希绑定 -> 不解锁生产 NC
```

