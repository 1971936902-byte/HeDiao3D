# 核雕 3D 生成与刀路生成系统改进项清单

更新时间：2026-07-16

## 1. 当前系统定位

当前项目已经具备 MVP 闭环：

- 多图/单图载入与本地浮雕网格生成。
- Meshy 多图转 3D Mesh、Mesh 修复、重网格。
- GLB/STL 预览与下载。
- 基础四轴刀路、精加工刀路、NC/TAP/TXT/CSV 导出。
- 刀路叠加显示、模拟雕刻、包络贴合指标。
- 真实夹持区、端部过渡、安全提示等实机场景参数。

但从市场上成熟 CAM 软件与 AI 3D 生成产品看，目前仍处于“可演示、可验证流程”的阶段，距离“稳定交付给雕刻机量产”还需要补齐几类能力：工艺参数体系、刀具/机床库、真实材料去除仿真、后处理可靠性、模型质量检查、任务管理和生产级 UI。

## 2. 市场参考要点

### 2.1 CAM / CNC 软件常见能力

- Vectric Aspire / VCarve 类软件通常明确区分 3D 粗加工、3D 精加工、边界限制、边界偏移、多刀具清残、预览最终雕刻效果、加工时间估算、旋转包覆预览等能力。
- Fusion / NX CAM 类软件更强调工具库、机床库、夹具/刀柄碰撞检测、G-code 级仿真、后处理器、特征识别、工艺模板、质量闭环。
- BlenderCAM / FabEX 这类开源路线提供导入 3D 对象、选择多种刀路策略、可视化刀路、渲染仿真、4/5 轴和多后处理支持等方向。

### 2.2 AI 3D 生成产品常见能力

- Meshy API 已支持多图转 3D、单图转 3D、Text to 3D、Remesh、Repair Printability、AI Texturing、Webhook 等能力。
- 多图转 3D 通常要求 1-4 张同一物体不同角度照片，照片光照、背景、角度一致性会显著影响生成质量。
- AI 生成 Mesh 适合快速得到完整模型，但直接进入 CAM 前仍需要做：比例校准、姿态校准、封闭性检查、非流形检查、表面平滑、降噪、端部夹持区重建、可加工性分析。

## 3. P0 级：实机可雕刻前必须补齐

### P0-1 刀具库与工艺参数库

现状：当前只有刀具直径、步距、进给、主轴等少量参数。

建议：

- 增加刀具库：球刀、平刀、锥刀、V 刀、微雕刀。
- 每把刀记录：直径、刃长、刀尖半径、刀角、有效伸出、推荐转速、推荐进给、最大切深、材料适配。
- 增加核雕材料库：橄榄核、桃核、象牙果、木质材料、树脂测试料。
- 每种材料记录：推荐主轴、粗加工进给、精加工进给、单层切深、最小残料厚度。

落地方式：

- 新增 `src/toolLibrary.ts` 和 `src/materialLibrary.ts`。
- UI 中增加“刀具/材料预设”下拉。
- 生成刀路时根据材料和刀具给出默认参数及风险提示。

验收标准：

- 用户选择“0.6mm 球刀 + 橄榄核”后，系统自动填入合理转速、进给、步距。
- 参数超出推荐范围时出现明确警告。

### P0-2 真实粗加工 / 精加工分层策略

现状：已有普通刀路和精加工刀路，但粗加工还不够像真实 CAM。

建议：

- 粗加工支持 Z 向多层切削，避免一次下刀过深。
- 粗加工保留余量，例如 0.1-0.25mm，精加工再去除。
- 精加工支持等距环绕、沿 X 扫描、沿 A 轴扫描、交叉精修。
- 增加清残策略：大刀粗雕后，小刀只加工大刀到不了的区域。

落地方式：

- 在 `server.mjs` 的 Mesh CAM 中增加 `roughingPasses`、`stockAllowance`、`finishingStrategy`。
- 输出多个独立程序：`rough.nc`、`finish.nc`、`rest.nc`。

验收标准：

- 生成的粗加工刀路最大切深不超过设定值。
- 精加工刀路贴合最终模型，不再承担大量去料。

### P0-3 G-code 后处理器与机床配置

现状：当前有通用四轴、维宏风格、新代风格，但还需要机床级配置。

建议：

- 增加机床配置库：控制系统、A 轴方向、A 轴单位、X/Y/Z 行程、回零方式、安全高度、最大进给。
- 增加后处理模板编辑器：开头、换刀、暂停、结尾、主轴启停、冷却/吸尘。
- 增加导出前 G-code 校验：坐标越界、进给超限、A 轴异常跳变、安全高度不足。

落地方式：

- 新增 `machineProfiles.ts`。
- 后处理函数从固定字符串改为模板化。
- UI 增加“机床配置”和“导出前校验报告”。

验收标准：

- 同一个刀路可导出不同机床格式。
- 坐标超过机床行程时禁止直接下载，并提示问题行号。

### P0-4 材料去除仿真升级

现状：已有模拟雕刻，但更偏刀路包络显示，不能完全代表最终材料去除效果。

建议：

- 使用体素/高度场方式模拟材料被刀具扫过后的剩余形状。
- 球刀按半径真实扣除材料，平刀按圆柱底面扣除材料。
- 显示“过切区域”“欠切区域”“残料区域”。
- 支持快览模式和高精度模式，避免仿真太慢。

落地方式：

- 前端使用 Web Worker 执行仿真，避免卡 UI。
- 先实现 2.5D/旋转高度场仿真，再升级三角网格布尔仿真。
- 增加误差热力图：目标模型 vs 仿真结果。

验收标准：

- 仿真结果能输出最大欠切、最大过切、平均误差。
- 用户能看到最终雕刻形态，而不只是线条。

### P0-5 端部夹持区与不可达区域建模

现状：已有左/右夹持区和端部过渡，但缺少专门的“不可达区域工艺模型”。

建议：

- 明确区分：可雕刻区、夹持区、过渡区、禁止下刀区。
- 端部生成保护帽/过渡面，不让刀路在端部突然缺失。
- 对佛头顶部、核两端等缺损区域提供“保守补面”策略。
- 允许用户手动拖动端部边界。

落地方式：

- 在模型视图中显示半透明夹持带。
- CAM 采样时对禁止区直接剔除，对过渡区做深度渐变。

验收标准：

- 刀路不会进入夹持区。
- 过渡区不会出现突兀断层或顶部缺口。

## 4. P1 级：提升生成质量与成功率

### P1-1 图片采集质量检测

问题：AI 多图转 3D 对照片质量非常敏感。

建议：

- 自动检测图片数量、清晰度、曝光、背景复杂度、主体居中程度。
- 提示用户补拍正面、背面、左侧、右侧。
- 检测多图是否是同一对象，角度是否覆盖完整。

落地方式：

- 前端先做基础图像质量评分。
- 后续可接入视觉模型做主体一致性判断。

验收标准：

- 上传图片后给出“可用于 Meshy / 建议补拍 / 不建议使用”的结论。

### P1-2 3D Mesh 质量体检

建议增加 Mesh 进入 CAM 前的体检报告：

- 是否封闭。
- 是否存在非流形边。
- 是否有大孔洞。
- 面数、法线方向、薄壁区域。
- 模型长宽高是否合理。
- 是否偏离旋转轴。
- 是否存在 CAM 采样无法命中的区域。

落地方式：

- 后端解析 STL/GLB 后输出 `meshQualityReport`。
- UI 中用红/黄/绿显示风险。

验收标准：

- 刀路生成前能明确提示“这个模型顶部缺损来自 Mesh 本身还是 CAM 采样失败”。

### P1-3 姿态校准与旋转轴校准

现状：AI Mesh 生成后可能姿态不固定，影响包覆刀路。

建议：

- 增加一键自动对齐：最长轴对齐 X，中心线对齐 A 轴。
- 支持手动旋转、平移、缩放。
- 支持设置雕刻原点和旋转轴。
- 增加“按核胚尺寸重缩放”。

验收标准：

- 同一 Mesh 在生成刀路前能稳定对齐核胚坐标。

### P1-4 Mesh 修复策略分层

现状：已有 Meshy 修复和重网格，但用户不知道该选哪个。

建议：

- 将修复流程做成向导：
  1. 质量体检。
  2. 小孔洞修复。
  3. 非流形修复。
  4. 重网格。
  5. 重新采样。
- 每一步显示修复前后对比。

验收标准：

- 用户能看到“修复前未命中 4595 点，修复后未命中 320 点”这类结果。

### P1-5 包络贴合分析升级

现状：已有贴合率、未贴合点、连续贴合等指标。

建议：

- 增加误差分布直方图。
- 增加按区域统计：左端、右端、顶部、底部、主体。
- 增加未命中点聚类，自动判断是端部缺损、顶部缺损还是局部噪声。
- 支持点击问题区域后定位到 3D 视图。

验收标准：

- 用户能从报告中知道“哪里不贴合、为什么不贴合、该怎么修”。

## 5. P2 级：生产效率与体验优化

### P2-1 任务中心与历史版本

建议：

- 增加任务列表：图片、生成模型、修复模型、刀路、仿真结果。
- 每次 Meshy 调用、修复、刀路生成都保存版本。
- 支持回退到某个版本。

验收标准：

- 用户不需要重新上传素材，就能比较不同参数生成的结果。

### P2-2 参数预设与工艺模板

建议：

- “快速试雕”“标准雕刻”“高精细雕刻”“低风险首刀”预设。
- 支持保存用户自己的工艺模板。
- 支持一键应用模板。

验收标准：

- 新用户可以不理解所有参数，也能安全生成第一版刀路。

### P2-3 UI 工作流重构

当前侧栏功能已经较多，后续建议改为阶段式工作流：

1. 素材采集。
2. 3D 生成。
3. Mesh 体检/修复。
4. 工艺设置。
5. 刀路生成。
6. 仿真校验。
7. 导出机床文件。

建议：

- 顶部增加步骤条。
- 每个阶段显示完成状态。
- 右侧工作区支持视图标签：3D 模型、刀路、仿真、误差热力图、G-code。

验收标准：

- 用户能清楚知道当前处于哪一步、下一步应该做什么。

### P2-4 下载包与交付报告

建议导出一个完整加工包：

- `rough.nc`
- `finish.nc`
- `preview.glb`
- `source.stl`
- `simulation-result.png`
- `quality-report.json`
- `operator-note.pdf`

验收标准：

- 用户下载一次即可拿到实机试雕所需全部文件和参数说明。

### P2-5 加工时间与成本估算

建议：

- 区分 G0 快速移动和 G1 切削进给。
- 支持机床最大快速速度。
- 支持换刀时间、暂停时间。
- 增加工时、材料、刀具损耗成本估算。

验收标准：

- 估算时间接近实际加工时间，而不是只按点距粗略估计。

## 6. P3 级：商业化与扩展能力

### P3-1 云端任务队列

建议：

- Meshy 生成、Mesh 修复、CAM 计算、仿真计算都进入任务队列。
- 支持任务暂停、失败重试、进度条、日志查看。
- 支持 Webhook 回调，减少前端轮询。

### P3-2 多 AI 服务适配

建议保留 Meshy，同时抽象 AI 3D Provider：

- Meshy。
- Tripo。
- Rodin。
- 本地 InstantMesh / Hunyuan3D / CRM 类模型。

验收标准：

- 同一组图片可选择不同服务生成，并比较模型质量。

### P3-3 工厂级权限与项目管理

建议：

- 用户/项目/素材/刀路权限管理。
- 操作日志。
- 参数审计。
- 实机反馈记录。

### P3-4 实机反馈闭环

建议：

- 雕刻完成后上传实物照片。
- 与目标模型进行外观比对。
- 记录“过深、过浅、毛刺、断刀、端部残料”等问题。
- 反向调整工艺模板。

## 7. 推荐实施路线

### 第一阶段：实机试雕安全版

目标：让下载文件具备低风险上机条件。

优先做：

1. 刀具库与材料库。
2. 机床配置与后处理校验。
3. 粗加工分层与精加工余量。
4. 端部夹持/不可达区可视化。
5. G-code 导出前安全报告。

### 第二阶段：结果可信版

目标：用户能在上机前看懂最终效果和风险。

优先做：

1. 材料去除仿真。
2. 过切/欠切热力图。
3. Mesh 质量体检。
4. 包络分析升级。
5. 导出加工包。

### 第三阶段：稳定生产版

目标：形成可重复、可追溯、可优化的生产流程。

优先做：

1. 任务中心。
2. 历史版本。
3. 工艺模板。
4. 多 AI Provider。
5. 实机反馈闭环。

## 8. 建议优先开发 Backlog

| 优先级 | 改进项 | 预计难度 | 价值 | 备注 |
|---|---|---:|---:|---|
| P0 | 刀具库/材料库 | 中 | 高 | 直接影响实机安全 |
| P0 | 机床配置/后处理校验 | 中 | 高 | 避免错误 G-code |
| P0 | 粗加工分层/余量 | 高 | 高 | 当前最接近工业 CAM 差距 |
| P0 | 端部不可达区可视化 | 中 | 高 | 适配核雕真实夹持 |
| P0 | 导出前安全报告 | 中 | 高 | 上机前最后一道闸 |
| P1 | Mesh 质量体检 | 中 | 高 | 定位缺损原因 |
| P1 | 姿态/旋转轴校准 | 中 | 高 | 提高刀路稳定性 |
| P1 | 材料去除仿真 | 高 | 高 | 判断最终雕刻效果 |
| P1 | 误差热力图 | 中 | 中 | 辅助调参 |
| P2 | 阶段式工作流 UI | 中 | 中 | 降低使用门槛 |
| P2 | 加工包导出 | 低 | 中 | 便于交付和测试 |
| P2 | 历史版本/任务中心 | 中 | 中 | 提升迭代效率 |

## 9. 外部资料链接

- Meshy Multi-Image to 3D API：https://docs.meshy.ai/en/api/multi-image-to-3d
- Meshy Repair Printability API：https://docs.meshy.ai/en/api/repair-printability
- Meshy Remesh API：https://docs.meshy.ai/en/api/remesh
- Vectric Aspire 3D Rough Toolpath：https://docs.vectric.com/docs/V12.0/Aspire/ENU/Help/form/Rough%20Machining%20Toolpath/index.html
- Vectric 3D Finish Toolpath：https://docs.vectric.com/docs/V12.0/VCarvePro/ENU/Help/form/Finish%20Machining%20Toolpath/index.html
- Vectric Preview Toolpaths：https://docs.vectric.com/docs/V12.0/Aspire/ENU/Help/form/Preview%20Toolpaths/index.html
- Vectric Rotary Machining and Wrapping：https://docs.vectric.com/docs/V12.0/Aspire/ENU/Help/form/rotary-machining-and-wrapping/
- BlenderCAM：https://blendercam.com/
- Siemens NX CAM toolpath / simulation / postprocessing overview：https://blogs.sw.siemens.com/nx-manufacturing/how-cam-software-optimizes-toolpaths-postprocessing-and-simulation/
