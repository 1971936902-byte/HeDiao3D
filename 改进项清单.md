# 核雕 3D 生成与雕刻机数据生成系统改进项清单

更新时间：2026-07-16

## 1. 文档目标

本清单参考 AI 3D 生成平台、成熟 CAM 软件、四轴旋转雕刻流程和当前项目实现情况，整理后续可落地的优化方向。

项目最终目标不是只生成“能看的 3D 图”，而是形成一条可验证、可复用、可逐步上机的核雕数字化流程：

1. 多角度照片或单图素材进入系统。
2. 生成真实 3D Mesh 或核胚浮雕网格。
3. 对 Mesh 做质量体检、姿态校准、尺寸标定、端部/夹持区处理。
4. 生成粗加工、精加工、清残刀路。
5. 通过材料去除仿真、包络贴合分析、安全校验判断是否可试雕。
6. 导出雕刻机可读取的 G-code/ENG/NC/TAP/TXT 文件和完整加工包。
7. 记录实机反馈，持续修正工艺模板。

## 2. 调研依据与清单使用方式

### 2.1 调研依据

本清单按以下三类产品能力进行对标：

| 类别 | 参考对象 | 重点参考能力 |
|---|---|---|
| AI 3D 生成 | Meshy、Tripo、Rodin、本地 Hunyuan3D/InstantMesh 工作流 | 多图/单图生成 3D Mesh、任务队列、模型修复、Remesh、格式导出 |
| 浮雕与旋转雕刻 CAM | Vectric Aspire/VCarve、ArtCAM 类软件 | 粗加工、精加工、边界控制、余量、刀具库、材料去除预览 |
| 通用制造 CAM | Fusion Manufacture、NX CAM、Mastercam | 后处理器、机床配置、碰撞/限位校验、仿真、加工报告 |

调研结论：

- AI 3D 平台解决“从图片得到可看的 3D 模型”，但通常不负责生成可直接上机的 CNC 刀路。
- 成熟 CAM 软件的关键能力不是单纯输出 G-code，而是围绕刀具、夹具、毛坯、机床极限和后处理做安全约束。
- 核雕场景的难点集中在小尺寸、高细节、四轴包络、两端夹持不可达、AI Mesh 局部缺损和实机试雕反馈闭环。

### 2.2 清单使用方式

建议按“先安全、再质量、再效率、最后生产闭环”的顺序推进：

1. P0 项必须优先完成，目标是让下载文件具备空跑和低风险试雕条件。
2. P1 项用于提高 3D 生成成功率和刀路贴合度，减少顶部、两侧、背面缺损。
3. P2 项优化页面、任务、模板、加工包和估算，降低使用成本。
4. P3 项面向真实业务生产，沉淀客户、项目、机床和试雕经验。

每个改进项都建议包含：功能入口、参数、可视化、报告指标、失败提示、验收标准、测试素材和 GitHub issue。

## 3. 市场对标结论

### 3.1 AI 3D 生成类产品

代表能力：Meshy、Tripo、Rodin、Hunyuan3D、本地 InstantMesh/Hunyuan3D 工作流。

可借鉴能力：

| 能力 | 市场常见做法 | 对本项目的启发 |
|---|---|---|
| 多图转 3D | 1-4 张或更多角度图生成 GLB/OBJ/FBX/STL | 继续保留 Meshy，多 Provider 预留是正确方向 |
| 任务状态 | 创建任务、轮询/SSE/Webhook、失败重试 | 长任务必须进入任务中心，不能让用户误以为卡死 |
| 模型修复 | Remesh、拓扑简化、面数控制、格式转换 | AI Mesh 不能直接视为可加工 Mesh，必须二次体检 |
| 版本比较 | 多次生成结果并排预览 | 同一素材应支持多版本质量比较 |
| 平台监控 | 请求日志、积分消耗、成功率、耗时 | API 成本和失败原因应可追踪 |

关键结论：

- Meshy 等 AI 3D API 适合解决“从图片生成完整视觉模型”，但不直接提供 CNC 刀路。
- AI 模型默认更偏展示、游戏、3D 打印资产，不理解核胚夹持区、旋转轴、刀具半径、机床极限。
- 本项目的核心壁垒应放在“AI Mesh 到可雕刻工艺模型再到安全刀路”的中间层。

### 3.2 成熟 CAM 软件

代表能力：Vectric Aspire/VCarve、Fusion Manufacture、NX CAM、Mastercam、ArtCAM 类浮雕/旋转雕刻软件。

可借鉴能力：

| 能力 | 市场常见做法 | 对本项目的启发 |
|---|---|---|
| 粗加工/精加工分离 | 粗加工去大料并保留余量，精加工单独成面 | 当前已有粗/精，应继续补清残与余量分析 |
| 加工边界 | 以模型边界、向量边界、夹具边界限制下刀 | 核雕必须显式处理左右夹持和不可达端部 |
| 刀具库 | 球刀、平刀、锥刀、V 刀参数化 | 刀路和仿真必须按真实刀具几何计算 |
| 机床后处理 | 不同控制系统独立 Post Processor | 必须做目标雕刻机 Profile 和实机验收 |
| 刀路仿真 | 材料去除、残料、过切/欠切、碰撞 | 模拟雕刻应从视觉预览升级为可量化校验 |
| 时间估算 | G0/G1、换刀、暂停、机床速度分开估算 | 当前估算要继续接近实机节拍 |

关键结论：

- 成熟 CAM 的核心不是“生成线”，而是“用边界、余量、刀具、机床约束生成可安全执行的线”。
- 核雕尺寸小，刀具直径、刀尖半径、步距、A 轴插补误差会被放大，必须比普通木雕更保守。
- 两端不可达是正常生产现实，不应被当成 3D 缺损强行修满，而应建模为夹持保护区和过渡区。

## 4. 当前项目能力基线

### 4.1 已具备能力

- 阶段式工作流：素材、建模、工艺、CAM、任务。
- 示例素材上传与多角度图片载入。
- 本地核胚浮雕网格生成。
- Meshy 多图转 3D Mesh，支持 GLB/STL 载入、预览、下载。
- Meshy Repair/Remesh 流程。
- AI Provider 抽象，Meshy 已接入，Tripo/Rodin/本地方案可扩展。
- 3D 视图、刀路视图、模拟雕刻视图、精加工视图。
- 粗加工、精加工、合并程序导出。
- 通用四轴、维宏、新代等机床风格导出。
- 刀具、材料、机床配置和安全参数。
- 工艺模板、参数快照、回滚。
- 旋转轴校准、Mesh 姿态校准、毛坯左右/中部直径标定。
- 夹持区、过渡区、禁止下刀区显示。
- 包络贴合率、未贴合点、区域诊断、误差分析。
- G-code 行级校验、安全报告、加工包 ZIP 导出。
- 加工时间和成本估算。
- 图片采集向导和任务中心。

### 4.2 主要短板

| 短板 | 影响 | 优先级 |
|---|---|---|
| AI Mesh 几何质量不可控，顶部/两侧/背面可能缺损 | 刀路采样缺口、局部欠切、误判为 CAM 问题 | P0 |
| CAM 内核仍偏轻量 | 清残、碰撞、刀柄干涉、复杂边界能力不足 | P0 |
| 材料去除仿真还不够接近真实刀具 | 用户难判断下载文件后的最终雕刻效果 | P0 |
| 后处理没有按真实机器逐台验收 | 上机存在方向、单位、限位、抬刀风险 | P0 |
| 页面功能较多，仍需继续拆分 | 用户找不到关键按钮，学习成本高 | P1 |
| 实机反馈没有闭环 | 不能把试雕经验沉淀成稳定模板 | P2 |

## 5. P0：上机安全与可雕刻性

### P0-1 目标机床 Profile 与后处理验收

目标：每台真实雕刻机都要有独立配置，不能只靠通用 G-code。

改进项：

- 增加目标机床档案：控制系统、A 轴方向、A 轴单位、A 轴零点、最大行程、最大进给、安全高度、主轴启停、回零方式。
- 后处理模板拆成程序头、加工段、换刀段、暂停段、程序尾。
- 导出前强制校验：XYZ/A 越界、进给超限、A 轴突跳、Z 安全高度不足、主轴未启动、夹持区下刀。
- 增加“空跑文件”导出，Z 轴整体抬高，只验证机器动作。
- 增加“实机验收记录”：空跑通过、软材料试雕通过、正式材料试雕通过。

验收标准：

- 同一刀路可导出不同机床格式。
- 存在禁止项时禁用正式 G-code 下载。
- 机床空跑不反向、不撞限位、不进入夹持区。

### P0-2 真实毛坯与夹持区建模

目标：把真实核胚形状和两端不可达区域纳入 CAM 约束。

改进项：

- 支持左/中/右多截面直径输入，并扩展到更多截面。
- 支持导入扫描 Mesh 或手动绘制毛坯外轮廓。
- 将夹持区、过渡区、可雕刻区、禁止区分层显示。
- 对两端自动生成保护帽和过渡坡面，禁止刀路追求端点完整。
- 支持保存“核胚规格模板”，例如小核、中核、长核、偏心核。

验收标准：

- 刀路不会进入夹持区。
- 过渡区深度连续，不出现断崖式缺口。
- 模拟结果能清楚显示“两端保留是工艺限制”，不是模型错误。

### P0-3 材料去除仿真升级

目标：让模拟雕刻更接近真实加工后的形状。

改进项：

- 用旋转高度场或体素方式模拟毛坯剩余材料。
- 球刀按球面半径扣除材料，平刀按平底圆柱扣除材料，锥刀按刀尖角扣除材料。
- 区分目标模型、刀具中心轨迹、实际刀具扫掠包络。
- 输出过切、欠切、残料、未覆盖区域热力图。
- 支持快览模式和高精度模式。
- 高精度计算放到 Web Worker 或后端任务，避免页面卡顿。

验收标准：

- 输出最大欠切、最大过切、平均误差、95 分位误差。
- 快览模式 5-15 秒内返回。
- 高精度模式允许较久，但必须显示进度、可取消、可重试。

### P0-4 粗加工、精加工、清残完整策略

目标：从“生成刀路线”升级为“接近真实 CAM 的加工策略”。

改进项：

- 粗加工支持多层切深、最大单刀切深、保留余量。
- 精加工只切最终表面，不承担大量去料。
- 增加清残刀路：用更小球刀处理大刀无法达到的细节区域。
- 支持沿 X、沿 A、交叉、螺旋、等角步距、等弦高等策略。
- 空走路径优化，减少非切削时间。
- 刀路按粗加工、精加工、清残分别着色和统计。

验收标准：

- 粗加工不会一次切入过深。
- 精加工余量稳定在设定范围。
- 清残区域可视化明确。
- 时间估算能分开统计粗加工、精加工、清残、空走。

### P0-5 安全报告强制闸口

目标：下载文件前必须知道能不能上机。

改进项：

- 安全报告独立为最后确认环节。
- 报告包含机床、刀具、材料、坐标范围、夹持区、最大切深、最小 Z、安全高度、估算时间、风险等级。
- 风险分为：禁止上机、建议修复、可空跑、可试雕。
- 加工包内固定包含 `safety-report.json`、`operator-note.md`、`quality-report.json`。

验收标准：

- 禁止项存在时不能下载正式文件。
- 用户可以下载诊断报告和空跑文件。
- 报告能解释为什么不能上机。

## 6. P1：提升 3D 生成与 CAM 成功率

### P1-1 图片采集向导增强

改进项：

- 按正面、左侧、右侧、背面、顶部补拍引导。
- 检测清晰度、曝光、主体居中、遮挡、背景复杂度、角度覆盖。
- 自动裁切主体、统一背景、生成 Meshy 输入图。
- 给出“适合生成 / 建议补拍 / 不建议使用”的结论。
- 保存每次生成对应的原图、裁切图、质量评分。

验收标准：

- 用户能知道应该补拍哪一张。
- Mesh 两侧和顶部缺损率下降。

### P1-2 多 AI Provider 比较

改进项：

- Meshy 作为主 Provider。
- 预留 Tripo、Rodin、本地 Hunyuan3D/InstantMesh。
- 同一组素材允许生成多个候选 Mesh。
- 对候选 Mesh 自动评分：封闭性、面数、尺寸、端部完整度、主体相似度、可加工性。

验收标准：

- 用户能在多个结果中选一个进入 CAM。
- Provider 失败不影响本地浮雕流程。

### P1-3 Mesh 质量体检增强

改进项：

- 检查封闭性、非流形边、退化面、法线方向、孔洞、尖峰、薄壁、悬空碎片。
- 按区域输出：左端、右端、顶部、底部、主体。
- 判断缺口来源：Mesh 本身缺损、姿态错误、毛坯越界、夹持区裁切、CAM 采样失败。
- 修复前后指标对比。
- 支持手动标记“保留缺口 / 自动补面 / 重新生成”。

验收标准：

- 用户能分清“顶部缺口是模型问题还是刀路问题”。
- 修复后可量化地看到指标提升。

### P1-4 姿态、比例、旋转轴校准

改进项：

- 一键将最长轴对齐核胚长度方向。
- 自动居中旋转轴，显示偏心量。
- 支持手动旋转、平移、缩放。
- 支持输入实物核长、最大直径、两端直径后等比缩放。
- 保存校准矩阵，导出报告时写入。

验收标准：

- 模型坐标、毛坯坐标、机床坐标一致。
- 包络误差不再因轴向错误大面积偏移。

### P1-5 包络分析与热力图

改进项：

- 未贴合点贴近模型表面显示，而不是漂浮过远。
- 增加误差热力图、误差直方图、区域误差排行。
- 点击问题区域时自动定位 3D 视图。
- 自动给出建议：重建 Mesh、补面、扩大过渡区、调轴、提高采样密度、换小刀清残。

验收标准：

- 报告能回答“哪里不贴合、差多少、为什么、怎么修”。
- 误差显示不会遮挡模型主体。

## 7. P2：效率、体验与交付

### P2-1 页面布局继续拆分

建议工作流：

1. 素材采集。
2. 3D 生成。
3. Mesh 体检/修复。
4. 毛坯与工艺设置。
5. CAM 生成。
6. 仿真校验。
7. 加工包导出。
8. 实机反馈。

改进项：

- 顶部步骤条显示完成状态和风险状态。
- 左侧只显示当前阶段必要参数。
- 右侧主视图用标签切换：3D 模型、刀路、模拟雕刻、误差热力图、G-code、报告。
- 高级参数默认折叠。
- 关键按钮固定在阶段底部，避免用户找不到“生成刀路/模拟雕刻”。

验收标准：

- 新用户能按步骤完成一次试雕文件导出。
- 页面不再因为按钮堆叠导致误操作。

### P2-2 工艺模板与参数版本

改进项：

- 内置低风险首刀、快速试雕、标准核雕、高精细雕刻。
- 支持用户保存自定义模板。
- 每次生成刀路自动保存参数快照。
- 可回退到任一历史版本。
- 支持模板对比：刀具、步距、切深、时间、风险。

验收标准：

- 用户能复用成功参数。
- 试错过程可回退。

### P2-3 加工时间与成本估算

改进项：

- 区分 G0 快移、G1 切削、A 轴旋转、换刀、暂停、主轴预热。
- 粗加工、精加工、清残分别估算。
- 增加机床小时费、材料成本、刀具损耗、AI 生成积分成本。
- 给出低/中/高三档估算，而不是单一绝对值。
- 实机完成后允许录入真实耗时并校正模型。

验收标准：

- 报价和排产可以参考。
- 报告说明估算依据和误差范围。

### P2-4 加工包导出升级

改进项：

- 一键导出完整加工包：
  - `rough.nc`
  - `finish.nc`
  - `rest.nc`
  - `combined.nc`
  - `air-run.nc`
  - `source.stl`
  - `preview.glb`
  - `simulation-result.png`
  - `quality-report.json`
  - `safety-report.json`
  - `operator-note.md`
  - `parameters.json`
- 加工包文件名包含项目名、日期、机床、刀具、版本号。

验收标准：

- 用户下载一次即可交给操作员空跑或试雕。
- 文件命名清晰，不会覆盖旧版本。

### P2-5 任务队列与日志

改进项：

- Meshy 生成、Mesh 修复、重网格、CAM、仿真全部进入任务队列。
- 支持进度条、取消、失败重试、日志查看。
- 支持 SSE/Webhook，减少长轮询。
- 显示 API 积分消耗、耗时、失败原因。

验收标准：

- 长任务有明确状态。
- 失败后能知道下一步该补图、重试还是调参数。

## 8. P3：生产闭环与商业化

### P3-1 实机反馈闭环

改进项：

- 雕刻后上传实物照片。
- 记录过切、欠切、毛刺、断刀、端部残料、夹持痕迹。
- 将反馈绑定到素材、Mesh、参数、机床、刀具批次。
- 根据反馈修正工艺模板和估算模型。

验收标准：

- 同类核胚越做越稳定。
- 成功经验能沉淀为模板，而不是只存在操作员记忆里。

### P3-2 项目、客户与权限管理

改进项：

- 项目、客户、素材、模型、刀路分组管理。
- 操作日志、参数审计、文件版本。
- 多用户权限：设计员、工艺员、操作员、管理员。

验收标准：

- 生产文件可追溯。
- 操作员只能下载已通过安全校验的文件。

### P3-3 本地/云端混合部署

改进项：

- 前端可本地运行。
- AI 生成、重计算、仿真可走服务器。
- API Key、模型文件、客户素材分级存储。
- 支持局域网部署和云端部署两种模式。

验收标准：

- 店内可局域网使用。
- 敏感素材和 API Key 不暴露到前端。

## 9. 推荐实施顺序

| 顺序 | 改进项 | 优先级 | 难度 | 价值 | 当前状态 |
|---:|---|---|---:|---:|---|
| 1 | 目标机床 Profile 与后处理验收 | P0 | 中 | 高 | 已补机床验收记录、空跑/软材料/正式材料状态、导出包追溯，仍需真实设备验收 |
| 2 | 真实毛坯与夹持区建模 | P0 | 中 | 高 | 已从左/中/右扩展为 5 截面毛坯标定、模板和报告追溯，后续可接扫描 Mesh/手绘轮廓 |
| 3 | 材料去除仿真升级 | P0 | 高 | 高 | 已补球刀/平刀/锥刀扫掠快览、扫掠覆盖和残料风险，高精度 Worker/后端仿真待增强 |
| 4 | 粗/精/清残完整策略 | P0 | 高 | 高 | 已补粗/精/清残分段查看、清残触发条件、清残占比和报告/导出包追溯，后续可接更小刀自动清残 |
| 5 | 安全报告强制闸口 | P0 | 中 | 高 | 已补禁止上机/建议修复/可空跑/可试雕等级，正式文件绑定三项确认，报告和加工包记录闸口状态 |
| 6 | 图片采集向导增强 | P1 | 中 | 高 | 已补角度缺失、质量归因和逐角度补拍动作建议，后续可接拍摄端实时取景提示 |
| 7 | 多 AI Provider 比较 | P1 | 中 | 中 | 已补 Meshy/Tripo/Rodin/本地 Hunyuan3D 对比卡片、状态/隐私/成本/适用场景评分，真实接入待扩展 |
| 8 | Mesh 质量体检增强 | P1 | 中 | 高 | 已补左端/主体/右端/顶部/底部区域归因、风险计数和导出包追溯，后续可接可视化高亮 |
| 9 | 姿态、比例、旋转轴校准 | P1 | 中 | 高 | 已补长轴/比例/采样方向校准向导、一键采用长轴和同步 Mesh 尺寸，后续可接交互式轴线标注 |
| 10 | 包络分析与热力图 | P1 | 中 | 中 | 已补热力图风险格占比、最差 X/A 区域、连续风险带和导出参数追溯，后续可接点击定位到 3D 视图 |
| 11 | 页面布局继续拆分 | P2 | 中 | 中 | 阶段式已完成，需继续减拥挤 |
| 12 | 工艺模板与参数版本 | P2 | 低 | 中 | 内置模板/快照已有，自定义待完善 |
| 13 | 时间与成本估算 | P2 | 低 | 中 | 已补实机反馈耗时校正、误差率和校正成本区间 |
| 14 | 加工包导出升级 | P2 | 低 | 中 | 已补参数、截图、源模型和交付清单，后续可继续补自动截图多视角 |
| 15 | 任务队列与日志 | P2 | 中 | 中 | 已补任务进度、取消、重试和日志展开，后续可接 SSE/Webhook |
| 16 | 实机反馈闭环 | P3 | 高 | 高 | 已补实机反馈记录、照片、缺陷标签、真实耗时和成功参数复用 |
| 17 | 项目、客户与权限管理 | P3 | 高 | 中 | 已补项目档案、客户编号、角色权限、项目归档和操作员下载闸口 |
| 18 | 本地/云端混合部署 | P3 | 中 | 中 | 已补部署阶段、密钥/素材/重计算配置、健康检查和部署方案文档 |

## 10. 下一阶段建议开发包

### 10.1 实机试雕安全版

优先目标：下载出来的文件可以先空跑，再低风险试雕。

包含：

1. 目标机床 Profile 完整化。
2. G-code 下载前强校验和安全闸口。
3. 空跑文件导出。
4. 毛坯多截面标定。
5. 夹持区、过渡区、不可达区强约束。
6. 球刀半径材料去除仿真。
7. 粗加工、精加工、清残三段式刀路。
8. 加工包自动包含报告和操作说明。

### 10.2 生成质量提升版

优先目标：减少 Mesh 缺损和 CAM 缺口。

包含：

1. 图片采集向导增强。
2. Mesh 区域级质量体检。
3. 姿态/比例/旋转轴校准向导。
4. 多 AI Provider 结果比较。
5. 包络误差热力图。

### 10.3 生产闭环版

优先目标：把试雕经验沉淀成稳定工艺。

包含：

1. 实机反馈记录。
2. 成功/失败案例库。
3. 工艺模板自动推荐。
4. 成本与耗时实机校正。
5. 项目与权限管理。

## 11. 外部参考资料

- Meshy Multi-Image to 3D API：https://docs.meshy.ai/en/api/multi-image-to-3d
- Meshy Remesh API：https://docs.meshy.ai/en/api/remesh
- Meshy API 能力与计费：https://www.meshy.ai/api
- Vectric Aspire 3D Rough Toolpath：https://docs.vectric.com/docs/V12.0/Aspire/ENU/Help/form/Rough%20Machining%20Toolpath/index.html
- Vectric VCarve 3D Finish Toolpath：https://docs.vectric.com/docs/V12.0/VCarvePro/ENU/Help/form/Finish%20Machining%20Toolpath/index.html
- Vectric Aspire User Guide：https://docs.vectric.com/docs/V12.0/Aspire/ENU/Help/page/user-guide/
- Siemens NX CAM Postprocessing and Simulation：https://www.siemens.com/en-us/products/nx-manufacturing/cam-software/postprocessing-simulation/
- Autodesk Fusion Manufacture 帮助中心：https://help.autodesk.com/view/fusion360/ENU/?guid=MFG-MANUFACTURE
