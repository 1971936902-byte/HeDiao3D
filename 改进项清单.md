# 核雕 3D 生成与雕刻机数据生成系统改进项清单

更新时间：2026-07-16

## 1. 目标定位

本项目的目标不是只做“好看的 3D 预览”，而是形成一条可验证、可复用、可逐步上机的核雕数字化流程：

1. 多角度照片或单图素材进入系统。
2. 生成真实 3D Mesh 或核胚浮雕网格。
3. 对 Mesh 做质量体检、姿态校准、端部/夹持区处理。
4. 生成粗加工、精加工、清残等 CNC 刀路。
5. 通过材料去除仿真、包络贴合分析、安全校验判断是否可试雕。
6. 导出机床可读取的 NC/TAP/TXT/CSV 文件和加工说明。

当前系统已经打通了 MVP 闭环，并在 V2 中补入了多项生产化能力；下一步应重点从“能生成”升级到“可信、可控、可上机试雕”。

## 2. 当前能力基线

### 2.1 已具备能力

- 单图/多图上传，支持示例素材载入。
- 本地核胚浮雕网格生成，支持 360 度包覆预览。
- Meshy 多图转 3D Mesh，支持 GLB/STL 载入、预览、下载。
- Meshy Repair/Remesh 流程，支持修复与重网格结果载入。
- 3D 视图、刀路叠加视图、模拟雕刻视图、精加工视图。
- 基础四轴刀路、粗加工/精加工程序、合并程序导出。
- 通用四轴、维宏、新代等机床风格导出。
- 刀具、材料、机床配置和安全参数体系。
- Mesh 质量体检、图像采集质量评分、加工质量体检。
- 旋转轴校准、Mesh 长轴识别、轴向反转。
- 夹持区、过渡区、禁止下刀区可视化。
- 包络贴合率、未贴合点、误差提示等分析能力。
- 操作任务中心、工艺模板、加工包说明导出。

### 2.2 当前主要短板

- AI Mesh 的几何质量仍依赖输入照片，端部、顶部、背面区域容易出现缺损或形变。
- CAM 内核仍偏轻量，缺少成熟 CAM 软件中的残料清根、碰撞检测、刀柄干涉、G-code 级仿真。
- 材料去除仿真还应进一步贴近真实刀具半径、刀尖形状、步距纹理和机床动态限制。
- 机床后处理尚未达到“每台机器独立验收”的程度，需要实机样件反馈闭环。
- UI 功能越来越多，需要继续做阶段式工作流、视图分层和参数向导，避免侧栏拥挤。

## 3. 市场对标结论

### 3.1 3D 生成产品对标

Meshy、Tripo、Rodin、Hunyuan3D 等 AI 3D 工具更擅长“快速生成完整视觉模型”，但它们默认输出通常面向展示、游戏或 3D 打印，不等同于可直接 CNC 加工的工艺模型。

可借鉴能力：

- 多图转 3D、单图转 3D、文生 3D。
- Remesh、Repair Printability、纹理生成、Webhook 任务回调。
- 多版本结果比较。
- 生成任务队列和失败重试。

需要本项目自行补齐：

- 核胚尺寸标定。
- 旋转轴、夹持区、不可达区定义。
- Mesh 可加工性分析。
- CAM 刀路、后处理、安全校验。

### 3.2 成熟 CAM 软件对标

Vectric Aspire/VCarve 类软件强调 3D 粗加工、3D 精加工、边界限制、刀路预览、旋转包覆加工；Fusion/NX CAM 类软件强调工具库、机床库、后处理、机床仿真、碰撞检测、工艺模板和生产级验证。

可借鉴能力：

- 粗加工与精加工明确分离。
- 粗加工保留余量，精加工只负责最终表面。
- 多刀具清残。
- 加工边界、边界偏移、夹具避让。
- 刀路预览和材料去除仿真。
- G-code 后处理器和机床级校验。
- 估算加工时间、优化空走路径。

核雕场景的特殊要求：

- 核胚很小，刀具直径、步距、刀尖半径对最终细节影响极大。
- 两端通常需要夹持，不可能完整雕刻到端点。
- 旋转轴偏心会显著影响包络贴合。
- 真实材料形状不是标准圆柱，需要标定毛坯外形。
- 上机前必须保守处理缺损、尖峰、薄壁和过深区域。

## 4. P0：实机试雕安全必做项

### P0-1 机床级后处理验收

现状：已有多种后处理风格，但还缺少按真实机床逐台验收。

优化项：

- 为目标雕刻机建立独立 Machine Profile。
- 记录控制系统、A 轴方向、A 轴单位、A 轴零点、最大行程、最大进给、安全高度、回零方式。
- 增加后处理模板：程序头、换刀、暂停、主轴启停、程序尾。
- 导出前逐行校验 G-code：坐标越界、进给超限、A 轴突跳、Z 安全高度不足。

验收标准：

- 使用同一刀路可导出不同机床格式。
- 发现越界或高风险指令时禁止下载正式文件，只允许下载诊断报告。
- 真实机床空跑时不撞限位、不反向、不异常抬刀。

### P0-2 真实毛坯与夹持区建模

现状：已有核长、直径、左右夹持区和过渡区，但仍按理想圆柱/近似核胚处理。

优化项：

- 增加“毛坯扫描/标定”入口，允许输入多个直径截面或导入扫描 Mesh。
- 可视化显示可雕刻区、夹持区、过渡区、禁止区。
- 对两端不可达区域生成保守保护帽，不追求端点完整雕刻。
- 支持手动拖动左右边界，保存为工艺参数。

验收标准：

- 刀路不会进入夹持区。
- 过渡区深度连续，不出现顶部断崖式缺口。
- 模拟结果能明确显示两端保留区域，而不是误认为模型缺损。

### P0-3 材料去除仿真升级

现状：已有模拟雕刻和包络显示，但还需更接近实际刀具切削。

优化项：

- 使用旋转高度场或体素方式模拟材料剩余形状。
- 球刀按球面半径扣除材料，平刀按平底圆柱扣除材料，锥刀按刀尖角扣除材料。
- 区分目标模型、刀具中心轨迹、实际可切削包络。
- 输出过切、欠切、残料、未覆盖区域热力图。
- 支持快览模式和高精度模式，重计算放入 Web Worker 或后端任务。

验收标准：

- 输出最大欠切、最大过切、平均误差、95 分位误差。
- 用户能从模拟雕刻结果判断“下载后大概雕成什么样”。
- 仿真耗时可控，快览模式 5-15 秒内返回。

### P0-4 粗加工、精加工、清残完整策略

现状：已有粗加工/精加工程序，但清残和余量策略还可加强。

优化项：

- 粗加工支持最大单层切深和多层去料。
- 粗加工保留 0.05-0.25 mm 余量，精加工再贴合目标面。
- 增加清残刀路：用小刀只处理大刀无法达到的细节区域。
- 支持沿 X、沿 A、交叉、等高/等距等精加工策略。
- 空走路径优化，减少非切削时间。

验收标准：

- 粗加工不会一次切入过深。
- 精加工刀路不承担大量去料。
- 清残区域能通过不同颜色显示。
- 加工时间估算中能区分粗加工、精加工、清残、空走。

### P0-5 上机前安全报告

现状：已有加工质量体检和加工包说明。

优化项：

- 安全报告独立成强制环节。
- 报告包含：机床参数、刀具参数、材料参数、坐标范围、夹持区、越界检查、最大切深、最小 Z、安全高度、估算时间。
- 将风险分为：禁止上机、建议修复、可试雕。

验收标准：

- 存在禁止项时，“下载正式 G-code”按钮禁用。
- 加工包内自动包含 `safety-report.json` 和 `operator-note.md`。

## 5. P1：提升 3D 与 CAM 成功率

### P1-1 图片采集向导

优化项：

- 按正面、左侧、右侧、背面引导拍摄。
- 检测清晰度、曝光、主体居中、背景复杂度、角度覆盖。
- 提示“建议补拍哪一张”。
- 自动裁切主体并统一背景。

验收标准：

- 上传后给出“适合 Meshy / 可用但建议补拍 / 不建议使用”的结论。
- 能降低 Mesh 顶部、两侧、背面缺损概率。

### P1-2 AI 3D Provider 抽象

优化项：

- 将 Meshy 封装为 `Ai3dProvider`，保留接口扩展能力。
- 后续支持 Tripo、Rodin、本地 Hunyuan3D/InstantMesh。
- 同一素材可生成多个版本并并排比较质量。

验收标准：

- 前端不绑定单一服务字段。
- 新增 Provider 不影响 CAM 逻辑。

### P1-3 Mesh 质量体检增强

优化项：

- 检查封闭性、非流形边、退化面、法线方向、薄壁、孔洞、尖峰。
- 按区域分析：左端、右端、顶部、底部、主体。
- 自动判断缺损来源：Mesh 本身缺损、姿态错误、CAM 采样失败、夹持区剔除。
- 体检结果与修复按钮联动。

验收标准：

- 用户能明确知道“佛头顶部缺口是 Mesh 缺损还是刀路采样缺口”。
- 修复前后指标可对比。

### P1-4 姿态与比例校准

优化项：

- 一键将最长轴对齐核胚长度方向。
- 自动居中旋转轴，显示偏心量。
- 支持手动旋转、平移、缩放。
- 支持输入实物核长、直径后等比缩放。

验收标准：

- 生成刀路前，模型坐标、核胚坐标、机床坐标一致。
- 包络贴合分析不再因为轴向错误产生大面积偏差。

### P1-5 包络分析与误差热力图

优化项：

- 未贴合点不只显示散点，还要贴近模型表面显示误差热力。
- 增加误差直方图、区域误差排行、连续缺损长度。
- 点击问题区域时定位 3D 视图。
- 给出自动建议：补面、扩大过渡区、调轴、重建 Mesh、提高采样密度。

验收标准：

- 报告能回答“哪里不贴合、差多少、为什么、怎么修”。

## 6. P2：效率、体验和交付优化

### P2-1 阶段式 UI 工作流

建议工作流：

1. 素材采集。
2. 3D 生成。
3. Mesh 体检/修复。
4. 工艺设置。
5. CAM 生成。
6. 仿真校验。
7. 加工包导出。

优化项：

- 顶部增加步骤条和完成状态。
- 左侧只显示当前阶段必要参数。
- 右侧视图区用标签切换：3D 模型、刀路、模拟雕刻、误差热力图、G-code。
- 高级参数默认折叠。

验收标准：

- 新用户能按步骤完成一次试雕文件导出。
- 页面不再因为按钮堆叠导致“找不到生成刀路/模拟雕刻”。

### P2-2 工艺模板与参数版本

优化项：

- 保留低风险首刀、快速试雕、标准核雕、高精细雕刻。
- 支持用户另存为自定义模板。
- 每次生成刀路保存参数快照。
- 可回退到任一历史版本。

验收标准：

- 用户能对比不同参数下的刀路、仿真和加工时间。

### P2-3 加工时间与成本估算

优化项：

- 区分 G0 快移、G1 切削、换刀、暂停、主轴预热。
- 按粗加工、精加工、清残分别估算时间。
- 增加机床小时费、材料成本、刀具损耗成本。
- 给出低/中/高三档估算，而不是单一绝对值。

验收标准：

- 估算结果在实机试雕后可校正。
- 报告中明确说明估算依据和误差范围。

### P2-4 加工包导出升级

优化项：

- 一键导出完整加工包：
  - `rough.nc`
  - `finish.nc`
  - `rest.nc`
  - `combined.nc`
  - `source.stl`
  - `preview.glb`
  - `simulation-result.png`
  - `quality-report.json`
  - `safety-report.json`
  - `operator-note.md`
- 支持 ZIP 打包。

验收标准：

- 用户下载一次即可交给操作员试雕。

### P2-5 任务队列与日志

优化项：

- Meshy 生成、Mesh 修复、重网格、CAM、仿真全部进入任务队列。
- 支持进度条、取消、失败重试、日志查看。
- 支持 Webhook 回调，减少前端轮询。

验收标准：

- 长任务不会让用户误以为系统卡死。
- 每次失败都能看到失败原因和下一步建议。

## 7. P3：商业化与生产闭环

### P3-1 实机反馈闭环

优化项：

- 雕刻后上传实物照片。
- 记录过切、欠切、毛刺、断刀、端部残料、夹持痕迹。
- 将反馈绑定到素材、Mesh、参数、机床、刀具批次。
- 反向修正工艺模板。

验收标准：

- 同类核胚越做越稳定，参数不是一次性经验。

### P3-2 项目与权限管理

优化项：

- 项目、客户、素材、模型、刀路分组管理。
- 操作日志、参数审计、文件版本。
- 多用户权限：设计员、工艺员、操作员、管理员。

### P3-3 本地/云端混合部署

优化项：

- 前端本地运行，AI 生成和重计算可走云端。
- CAM 与仿真可按性能选择本地或服务器。
- API Key、模型文件、客户素材分级存储。

## 8. 推荐实施顺序

| 顺序 | 改进项 | 优先级 | 难度 | 价值 | 说明 |
|---:|---|---|---:|---:|---|
| 1 | 机床级后处理验收 | P0 | 中 | 高 | 决定能否安全上机 |
| 2 | 真实毛坯与夹持区建模 | P0 | 中 | 高 | 解决两端不可达和缺口误判 |
| 3 | 材料去除仿真升级 | P0 | 高 | 高 | 判断最终雕刻效果 |
| 4 | 粗/精/清残完整策略 | P0 | 高 | 高 | 接近真实 CAM 工艺 |
| 5 | 上机前安全报告 | P0 | 中 | 高 | 下载前最后一道闸 |
| 6 | 图片采集向导 | P1 | 中 | 高 | 提高 AI Mesh 成功率 |
| 7 | Mesh 质量体检增强 | P1 | 中 | 高 | 快速定位缺损原因 |
| 8 | 姿态与比例校准 | P1 | 中 | 高 | 提高刀路稳定性 |
| 9 | 包络分析与误差热力图 | P1 | 中 | 中 | 让风险更直观 |
| 10 | 阶段式 UI 工作流 | P2 | 中 | 中 | 降低学习成本 |
| 11 | 工时与成本估算 | P2 | 低 | 中 | 辅助报价和排产 |
| 12 | ZIP 加工包 | P2 | 低 | 中 | 方便交付试雕 |
| 13 | 任务队列与日志 | P2 | 中 | 中 | 长任务体验更稳定 |
| 14 | 实机反馈闭环 | P3 | 高 | 高 | 长期形成工艺壁垒 |

## 9. 下一阶段建议开发包

建议下一阶段以“实机试雕安全版”为目标，优先完成以下闭环：

1. 机床 Profile 细化和 G-code 导出前强校验。
2. 真实毛坯/夹持区/不可达区建模。
3. 球刀半径材料去除仿真与误差热力图。
4. 粗加工、精加工、清残三段式刀路。
5. 加工包 ZIP 导出和安全报告。

完成后再进入“质量提升版”：

1. 图片采集向导。
2. Mesh 区域级质量体检。
3. 姿态、比例、旋转轴校准向导。
4. 多 AI Provider 对比。
5. 实机反馈记录。

## 10. 外部参考资料

- Meshy Multi-Image to 3D API：https://docs.meshy.ai/en/api/multi-image-to-3d
- Meshy Image to 3D API：https://docs.meshy.ai/en/api/image-to-3d
- Meshy API Pricing：https://docs.meshy.ai/en/api/pricing
- Meshy API Changelog：https://docs.meshy.ai/en/api/changelog
- Vectric Aspire 3D Rough Toolpath：https://docs.vectric.com/docs/V12.0/Aspire/ENU/Help/form/Rough%20Machining%20Toolpath/index.html
- Vectric VCarve 3D Finish Toolpath：https://docs.vectric.com/docs/V12.0/VCarvePro/ENU/Help/form/Finish%20Machining%20Toolpath/index.html
- Vectric Aspire User Guide：https://docs.vectric.com/docs/V12.0/Aspire/ENU/Help/page/user-guide/
- Siemens NX CAM Postprocessing and Simulation：https://www.siemens.com/en-us/products/nx-manufacturing/cam-software/postprocessing-simulation/
- Siemens NX CAM Toolpath / Simulation / Postprocessing Overview：https://blogs.sw.siemens.com/nx-manufacturing/how-cam-software-optimizes-toolpaths-postprocessing-and-simulation/
