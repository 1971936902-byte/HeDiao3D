# HeDiao3D V3 后续完善项清单

日期：2026-07-19
用途：把暂不阻断基本可用版的功能集中归档，后续按优先级继续开发。

## P0：真实试雕前仍需补齐

1. **真实 Linux OpenCAMLib 输出**
   - 用真实佛头模型在 Linux 服务器跑 OpenCAMLib。
   - 产出真实 `neutral-toolpath.json`、`opencamlib-cutter-contact-report.json`。
   - 替换 preview/fixture/heightfield scaffold。

2. **真实材料去除仿真**
   - 使用 CAMotics 容器、旧 Ubuntu 环境或等效材料去除仿真器。
   - 产出 `camotics-result-bundle.zip`。
   - 回填同一个 V3 job，并保持 input hash、machine context、simulationQuality 匹配。

3. **实机空跑与软料试雕**
   - 完成 `air-run.nc` 离料空跑。
   - 完成旋转夹具方向、比例、回差校准。
   - 用软料试雕佛头样件，记录照片、耗时、缺陷和参数。

4. **真实机床 Profile 固化**
   - 固化 Y 轴旋转每圈等效行程。
   - 固化安全 Z、最大切深、进给、主轴格式。
   - 确认控制器支持的 G-code 指令集合。

## P1：提高雕刻质量

1. OpenCAMLib drop-cutter / waterline / rest machining 策略。
2. 4mm 25 度平底尖刀真实几何建模，包括平底、侧刃角、有效伸出。
3. 粗加工、精加工、清残自动分段。
4. 两端夹持区和过渡区更精细的不可达建模。
5. AI Mesh 缺损检测、补面、重网格和多版本对比。
6. 模型姿态、比例、旋转轴自动校准。
7. 包络误差热力图点击定位和区域修复建议。

## P2：提高操作效率

1. 操作员模式与工程模式彻底分离。
2. 机床库、刀具库、材料库可视化维护。
3. 工艺模板版本管理和成功参数复用。
4. 加工时间、成本、刀具损耗按实机反馈校正。
5. 任务队列升级为 SSE/Webhook，支持长任务取消和重试。
6. Meshy / Tripo / Rodin / 本地 Hunyuan3D 多 Provider 比较。

## P3：生产与商业化

1. 客户、项目、素材、模型、刀路版本管理。
2. 操作员、工艺员、管理员权限。
3. 生产文件审计、下载权限和操作日志。
4. 局域网/云端混合部署。
5. API Key、客户素材、生成模型的分级存储和清理策略。
6. 试雕案例库和缺陷知识库。

## 暂时建议隐藏或弱化的功能

这些功能保留代码或工程入口，但基本可用版不作为主流程宣传：

- 旧版本地图片深度场生产刀路。
- 直接正式生产包下载。
- 多控制器方言扩展。
- FreeCAD / BlenderCAM 生产级自动 CAM。
- 高级成本估算和项目权限。
- 多 AI Provider 完整接入。
- 高精度体素材料去除仿真。

## 后续开发顺序建议

```text
真实 OpenCAMLib 佛头候选刀路
  -> CAMotics/等效材料去除回填
  -> 软料试雕
  -> 修正 wrapY 机床 Profile
  -> 首个可复用佛头工艺模板
  -> 再扩展粗/精/清残和多 Provider
```

原则：每增加一个高级功能，都要继续保持正式生产 NC fail-closed，直到真实 CAM、仿真、空跑、试雕、机床验收绑定同一个 job。
