# HeDiao3D V3 架构与小闭环验证

日期：2026-07-17
分支：`HeDiao3D_V3`

部署参考：

- 环境变量模板：`.env.example`
- Linux 部署指南：`V3_Linux部署指南.md`

## V3 目标

V3 不再继续把所有 CAM 能力都写在前端/单一后端函数里，而是拆成可演进的五层：

| 层 | 职责 | 当前状态 |
| --- | --- | --- |
| 前端 HeDiao3D | 素材、模型预览、中文工艺流程、报告、下载 | 已加入 V3 Orchestrator 小闭环面板、历史任务恢复和 V3 加工包打包下载 |
| 后端 Orchestrator | 任务协调、文件缓存、模型体检、修复计划、CAM 输入准备、调用外部 CAM 引擎 | 已新增引擎探测、job API、历史 job 列表、Mesh 质量产物、CAM 输入计划、adapter 执行开关 |
| CAM 引擎层 | FreeCAD CAM / BlenderCAM / OpenCAMLib | 已预留 adapter slot 和 JSON job 协议；OpenCAMLib 已加入 Python 模块探测和占位 adapter |
| 仿真层 | CAMotics + 自研旋转包裹预览 | 已预留 CAMotics adapter 和产物协议；当前继续使用自研预览 |
| 后处理层 | 三轴控制器 + Y 轴旋转夹具专用 NC | 已通过 `wrapY` 输出验证 |

## 新增接口

### `GET /api/orchestrator/engines`

用于探测本机或服务器上的外部 CAM/仿真引擎。

当前探测项：

- FreeCAD CAM：`FreeCADCmd` / `freecadcmd` / `FreeCAD` / `freecad`
- BlenderCAM / FabexCNC：`blender`
- CAMotics：`camotics-cli` / `camotics`
- HeDiao3D internal Mesh CAM：内置 fallback

### `GET /api/orchestrator/diagnostics`

用于部署自检，返回端口、关键环境变量、外部 adapter 开关、Meshy key、队列状态、关键缓存目录写权限、外部 CAM/仿真引擎探测结果和建议动作。前端 V3 面板会展示最高风险等级和部署建议，方便迁移到 Linux 服务器后快速定位环境问题。

### `POST /api/orchestrator/jobs`

输入：

```json
{
  "modelUrl": "/imported-models/example.glb",
  "settings": {},
  "engine": "auto"
}
```

输出：

- Orchestrator job id
- 引擎选择结果
- fallback 信息
- 生成的刀路 `toolpath`
- 任务日志
- 任务管线状态：队列调度、Mesh 体检/修复计划、CAM 输入准备、CAM 引擎选择、外部 CAM adapter、刀路生成、仿真/空跑、后处理交付
- 任务产物链接：`job.json`、`job-status.json`、`mesh-quality.json`、`repair-plan.json`、`repair-execution.json`、`cam-input-plan.json`、`engine-diagnostics.json`、`adapter-preflight.json`、`toolpath.nc`、`toolpath-summary.json`、`simulation-summary.json`、`air-run.nc`、`production-gate.json`、`delivery-manifest.json`

V3 Orchestrator job 已从同步执行改为后台队列雏形：

- `POST /api/orchestrator/jobs` 创建任务后返回 `queued` 或 `running`。
- 前端通过 `GET /api/orchestrator/jobs/:id` 轮询。
- 前端可通过 `GET /api/orchestrator/jobs` 查看最近任务，恢复历史 job 并继续下载 V3 加工包。
- 前端可通过 `POST /api/orchestrator/jobs/:id/cancel` 取消任务；`queued` 任务会从队列移除，`running` 任务会在阶段检查点停止后续交付。
- `job-status.json` 和历史列表会记录 `currentStage` 与 `progress`，用于长耗时 CAM/仿真任务的进度展示和恢复。
- 任务完成后状态为 `completed`，失败则为 `failed`。
- 当前并发数由 `ORCHESTRATOR_CONCURRENCY` 控制，默认 `1`，为后续 FreeCAD/CAMotics 长任务留出队列调度入口。

### `GET /api/orchestrator/jobs/:id/artifacts/:filename`

用于下载单个 V3 job 产物。前端 V3 ZIP 加工包会根据 `delivery-manifest.json` 逐个读取该接口，然后在浏览器端打包，避免依赖 Vite 静态目录或服务器反向代理规则。

## Adapter 协议落地

新增目录：

```text
adapters/
  README.md
  freecad/freecad_cam_job.py
  blendercam/blendercam_job.py
  camotics/camotics_job.js
  opencamlib/opencamlib_job.py
```

协议约定：

- Orchestrator 为每个任务创建 `public/orchestrator-jobs/<jobId>/`。
- 写入 `job.json`，包含模型路径、工艺参数、输出路径。
- 写入 `job-status.json`，保存任务状态、日志、引擎选择和摘要，方便前端轮询和服务重启后追踪。
- 在 CAM 之前写入 `mesh-quality.json` 和 `repair-plan.json`：
  - `mesh-quality.json` 记录面数、边界边、非流形边、退化面、长轴识别、风险区域、体检评分。
  - `repair-plan.json` 将体检结果转换成可执行建议，例如封孔、修复非流形、删除退化面、降面、重生成高精度模型。
- 写入 `repair-execution.json`，记录本次 job 是否需要修复、是否启用自动修复、是否具备 Meshy key、修复输出候选路径和下一步动作。默认不自动改模型；只有显式设置 `ORCHESTRATOR_AUTO_MESH_REPAIR=true` 才允许进入后端自动修复链路。
- 写入 `cam-input-plan.json`，明确当前 job 的 CAM 输入模型策略：
  - 是否允许用原始模型直接试算。
  - 是否必须先封孔/修非流形/降面。
  - 三轴、旋转夹具、四轴分别采用哪种输入策略。
  - 是否允许外部 CAM 试算和生产 NC。
- 写入 `engine-diagnostics.json`，记录 FreeCAD / BlenderCAM / CAMotics / OpenCAMLib 的安装、adapter、启用开关和安装建议。
- 写入 `adapter-preflight.json`，检查 adapter 脚本、外部命令、模型文件、工作目录、环境开关和 CAM 输入门禁，明确当前能否执行外部 adapter，以及为什么 fallback。
- 外部 adapter 读取 `job.json`，写回 `adapter-report.json` 和 G-code/仿真产物。
- 当 adapter 返回 `completed` 且写出非空 G-code 时，Orchestrator 会摄取该 G-code，解析 G0/G1 点位，并继续走统一的仿真摘要、生产门禁和交付清单；否则自动 fallback 到内置 Mesh CAM。
- 外部 adapter 默认不会执行，必须显式设置 `ENABLE_EXTERNAL_CAM_ADAPTERS=true`，避免在占位脚本阶段误触发生产级输出。
- 当前 adapter 脚本是可执行占位，会返回 `adapter_not_ready`，防止误以为已经具备生产级外部 CAM 能力。
- OpenCAMLib 已纳入引擎探测和 adapter 协议，但定位是底层几何内核，用于后续 drop-cutter、水线、刀具接触计算，不直接替代完整 CAM 软件。
- 内置 fallback 会写出 `toolpath.nc`、`toolpath-summary.json`、`simulation-summary.json` 和 `air-run.nc`，用于验证完整产物流。
- `simulation-summary.json` 当前由自研旋转包裹/三轴预览计算，记录贴合率、未命中点、安全高度点、覆盖率和最大深度；后续 CAMotics adapter 启用后会替换为真实材料去除仿真产物。
- `air-run.nc` 将所有切削 Z 替换为安全高度，用于离料空跑验证行程、夹具方向和后处理轴映射。
- `production-gate.json` 汇总模型质量、CAM 输入、外部引擎、仿真风险和后处理结果，明确当前包是 `blocked`、`trial-only` 还是 `production`。
- `delivery-manifest.json` 给出可下载文件清单，区分报告、离料空跑 NC、试雕/生产 NC，并附带操作说明。
- 前端 V3 面板可按 `delivery-manifest.json` 下载 V3 ZIP 加工包，并自动加入 `README-V3.md`，说明当前门禁等级、可上机范围和下一步操作。

## 小闭环验证

测试素材：

`C:\Users\19719\Desktop\AI创业\雕刻图生成\3D素材\meigui.glb`

自动 smoke test：

```bash
npm run test:v3
```

默认使用 `/meshy-results/material01-meshy.glb`。如需换模型，可设置：

```bash
V3_SMOKE_MODEL_URL=/imported-models/example.glb npm run test:v3
```

测试流程：

1. 调用 `/api/mesh/import` 导入 `meigui.glb`。
2. 调用 `/api/orchestrator/engines` 探测引擎。
3. 调用 `/api/orchestrator/jobs`，请求 `engine=auto`。
4. Orchestrator 先生成 Mesh 质量报告和修复计划。
5. Orchestrator 生成 CAM 输入计划，判断是否允许直接试算、是否允许生产 NC。
6. Orchestrator 生成外部引擎诊断，给出缺失软件和 adapter 启用状态。
7. Orchestrator 优先选择 FreeCAD adapter slot。
8. 本机未检测到 FreeCAD/Blender/CAMotics，因此自动 fallback 到 `internal-mesh-cam`。
9. 使用当前三轴控制器 + Y 轴旋转夹具参数生成 NC。

## 验证结果

| 项目 | 结果 |
| --- | --- |
| FreeCAD CAM | 未安装 |
| BlenderCAM / FabexCNC | 未安装 |
| CAMotics | 未安装 |
| 内置 Mesh CAM fallback | 可用 |
| Orchestrator job 状态 | completed |
| fallbackFrom | freecad |
| resultEngine | internal-mesh-cam |
| 刀路点 | 17,818 |
| 预览点 | 17,818 |
| 后处理 | Y轴旋转包裹 G-code |
| 估算时间 | 54.21 min |
| G-code 元数据 | `ROTARY_WRAP_AXIS=Y ROTARY_WRAP_PER_REV_MM=100.000000 LENGTH_AXIS=X` |
| 任务产物 | `job.json`、`job-status.json`、`mesh-quality.json`、`repair-plan.json`、`repair-execution.json`、`cam-input-plan.json`、`engine-diagnostics.json`、`adapter-preflight.json`、`toolpath.nc`、`toolpath-summary.json`、`simulation-summary.json`、`air-run.nc`、`production-gate.json`、`delivery-manifest.json` |

## 结论

V3 小闭环已经跑通：

```text
前端 V3 面板
  -> 后端 Orchestrator
  -> 后台任务队列
  -> Mesh 体检 / 修复计划
  -> Mesh 修复执行记录 / 自动修复门禁
  -> CAM 输入模型计划
  -> 外部 CAM 引擎探测
  -> 外部引擎诊断 / 安装建议
  -> Adapter 运行预检 / fallback 原因
  -> 外部 CAM adapter 可选执行
  -> 内置 CAM fallback
  -> 仿真摘要 / 离料空跑 NC
  -> 生产门禁 / 交付清单
  -> Y 轴旋转包裹后处理
  -> 前端刀路/预览/下载
```

这证明 V3 的调度层和架构入口已经可用。下一步如果要真正引入专业 CAM 精度，需要在服务器安装 FreeCAD / Blender / CAMotics，并补齐对应 adapter：

1. FreeCAD adapter：导入 STL/GLB 转换网格，创建 Path Job，设置刀具、stock、后处理，导出 G-code。
2. BlenderCAM adapter：面向艺术曲面/浮雕策略生成 G-code。
3. CAMotics adapter：读取 NC，输出材料去除仿真截图或网格。
4. OpenCAMLib adapter：已加入占位脚本和 Python 模块探测；下一步实现 drop-cutter、水线、刀具接触计算，用于逐步替换内置采样核心。
5. Adapter 任务队列：继续增强长耗时任务的取消、重试、进度和资源隔离。

当前 V3 没有假装外部 CAM 已经可用，而是明确区分：

- 外部引擎探测
- adapter 可用性
- fallback 结果
- 后处理结果

这为后续接入真实 FreeCAD/BlenderCAM/CAMotics 留出了清晰边界。
