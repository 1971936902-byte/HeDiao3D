# HeDiao3D V3 新服务器部署记录

记录时间：2026-07-22

## 服务器环境

- 系统：Ubuntu 22.04.3 LTS
- 主机名：ubuntu22
- 项目目录：`/opt/hediao3d`
- Git 分支：`HeDiao3D_V3`
- 当前部署提交：`d4d049d add v3 mvp operator status report`

## Node.js 环境

服务器 apt 源安装 NodeSource 速度较慢，因此采用官方二进制包安装。

- Node.js：`v20.20.2`
- npm：`10.8.2`
- 安装目录：`/opt/node-v20.20.2-linux-x64`
- 命令链接：
  - `/usr/local/bin/node`
  - `/usr/local/bin/npm`
  - `/usr/local/bin/npx`
- npm 镜像：`https://registry.npmmirror.com`

## 项目部署

```bash
cd /opt/hediao3d
git checkout HeDiao3D_V3
npm ci --no-audit --no-fund
npm run build
```

已通过的服务器验证：

```bash
npm run test:v3:machine-contract
npm run test:v3:architecture
npm run build
```

构建产物目录：

```text
/opt/hediao3d/dist
```

## 后端服务

systemd 服务：

```text
/etc/systemd/system/hediao3d-api.service
```

运行方式：

```text
WorkingDirectory=/opt/hediao3d
NODE_ENV=production
API_PORT=8787
ORCHESTRATOR_CONCURRENCY=1
ExecStart=/usr/local/bin/node /opt/hediao3d/server.mjs
```

服务状态：

```bash
systemctl status hediao3d-api.service
```

内部健康检查：

```bash
curl http://127.0.0.1:8787/api/health
```

预期返回：

```json
{"ok":true}
```

## Nginx

站点配置：

```text
/etc/nginx/sites-available/hediao3d
/etc/nginx/sites-enabled/hediao3d
```

用途：

- 静态页面由 `/opt/hediao3d/dist` 提供。
- `/api/` 反向代理到 `http://127.0.0.1:8787/api/`。
- 公开只读产物目录，包括 Meshy 结果、导入预览、编排任务、CAM 校验、机床验收、测试素材等。

内部验证：

```bash
curl -I http://127.0.0.1/
curl http://127.0.0.1/api/health
```

## 公网访问

服务器内部 Nginx 与 API 正常，但直连公网端口测试发现 `80/8080/5193/8788/3000` 无法从外网访问，推测为云服务商安全组、防火墙或端口映射未开放。

为临时访问，已配置 Cloudflare Tunnel：

```text
/etc/systemd/system/hediao3d-cloudflared.service
```

最新临时公网链接：

```text
https://charitable-permitted-productions-perspectives.trycloudflare.com
```

该链接已验证：

```bash
curl -I https://charitable-permitted-productions-perspectives.trycloudflare.com/
curl https://charitable-permitted-productions-perspectives.trycloudflare.com/api/health
```

验证结果：

- 页面：`HTTP/1.1 200 OK`
- API：`{"ok":true}`

注意：Cloudflare quick tunnel 链接是临时链接，服务重启后可能变化。当前 tunnel 已改用 `--protocol http2`，避免该网络环境下 QUIC 连接出现 1033/timeout。正式生产建议绑定域名并配置 named tunnel，或在服务器供应商控制台开放 `80/443` 后直接用 Nginx 对外服务。

## 2026-07-22 14:40 服务器重启后复验

本次按新服务器重配流程重新执行：

```bash
git pull --ff-only origin HeDiao3D_V3
npm ci
npm run build
systemctl restart hediao3d-api
systemctl restart nginx
systemctl restart hediao3d-cloudflared
```

复验结果：

```text
commit=65d1ec8 stabilize linux cam evidence scripts
frontendBuild=ok
apiHealth={"ok":true}
nginxLocal=HTTP/1.1 200 OK
publicUrl=https://charitable-permitted-productions-perspectives.trycloudflare.com
publicHttp=200
```

Native CAM 环境检测：

```text
npm run test:v3:native-cam
ok=true
level=ready
ready=4/4
blockers=[]
```

外部 CAM adapter 探测：

```text
V3_ADAPTER_USE_NATIVE_COMMANDS=true npm run test:v3:external-adapters
ok=true
mode=native
generatedPlans=4
failed=0
```

说明：FreeCAD、BlenderCAM、OpenCAMLib、CAMotics 均可被环境探测识别；但真实 CAM 输出证据、材料去除仿真证据和机床试雕反馈仍未闭环，生产 NC 继续保持 fail-closed。

## 2026-07-22 新服务器环境重配结果

本次在新服务器 `/opt/hediao3d` 重新拉取 `HeDiao3D_V3` 分支、安装依赖、构建并重启服务：

```bash
git pull --ff-only origin HeDiao3D_V3
npm install
npm run build
systemctl restart hediao3d-api
systemctl reload nginx
systemctl restart hediao3d-cloudflared
```

已确认：

- Node.js：`v20.20.2`
- npm：`10.8.2`
- Git：`2.34.1`
- Nginx：`1.18.0`
- FreeCAD：`0.19`
- Blender：`3.0.1`
- FabexCNC/BlenderCAM addon：`CAM_ADDON_OK`
- OpenCAMLib：`opencamlib.ocl` 可用，`dropCutterReady=true`
- CAMotics：`1.2` 可启动

Native CAM 检查：

```bash
npm run test:v3:native-cam
```

结果：

```text
ok=true
level=ready
ready=4/4
blockers=[]
```

外部 adapter native 探测：

```bash
V3_ADAPTER_USE_NATIVE_COMMANDS=true npm run test:v3:external-adapters
```

结果：

```text
ok=true
mode=native
generatedPlans=4
failed=0
```

固定佛头 E2E：

```bash
npm run test:v3:buddha-e2e
```

结果：

```text
ok=true
points=8979
machineAxes X/Y/Z present, A=0
rotaryCoverage=1
postprocessFitRate=1
trialPackageFiles=37
productionAllowed=false
```

服务器端 Chrome/Chromium 浏览器 E2E 暂未纳入阻断项。Ubuntu 22.04 的 `chromium-browser` 会转入 snap 安装，当前网络环境下 snap store 安装长时间阻塞，已清理坏包并保持 apt/dpkg 正常。前端浏览器交互继续使用本地浏览器和公网地址验证。

## 当前结论

新服务器已经完成基础运行环境、项目构建、后端 API、Nginx 和临时公网访问配置。当前版本适合继续进行 V3 基本可用版验证，包括：

- 三轴控制器 + Y 旋转夹具的机床契约检查。
- Meshy/导入模型到旋转包裹 CAM 的流程验证。
- 安全报告、空跑 NC、加工包与后续试雕反馈流程。

仍需在真实机床上完成离料空跑、低速浅切试雕和回传反馈后，才能解除生产 NC 的 fail-closed 限制。

## 2026-07-22 18:38 新服务器重配复验

本次按“换新服务器后重新安装配置环境”的口径复核并重建运行态。服务器已有 `/opt/hediao3d` 基线目录，已确认代码与 GitHub `HeDiao3D_V3` 最新提交一致：

```text
68e4fcf Validate original model imports before CAM cache
```

执行步骤：

```bash
cd /opt/hediao3d
git fetch origin HeDiao3D_V3
npm ci --no-audit --no-fund
npm run build
systemctl restart hediao3d-api
systemctl reload nginx
systemctl restart hediao3d-cloudflared
```

复验结果：

```text
Node.js: v20.20.2
npm: 10.8.2
API: http://127.0.0.1:8787/api/health -> {"ok":true}
Nginx: http://127.0.0.1/ -> HTTP/1.1 200 OK
Services: hediao3d-api / nginx / hediao3d-cloudflared 均为 active
Public URL: https://artists-stack-clause-pat.trycloudflare.com
Public API: https://artists-stack-clause-pat.trycloudflare.com/api/health -> {"ok":true}
```

Native CAM 检查：

```bash
npm run test:v3:native-cam
```

结果：

```text
ok=true
level=ready
ready=4/4
blockers=[]
```

Readiness API 检查：

```bash
npm run test:v3:readiness-api
```

结果：

```text
ok=true
level=blocked
acceptance=11/22
production=false
trial=false
airRun=true
```

说明：当前服务器环境已经可用于页面访问、API 编排、Native CAM 探测和离料空跑包流程；生产级 NC 仍按设计保持 fail-closed，直到真实 CAM 输出证据、CAMotics 材料去除证据、离料空跑、试雕反馈和机床验收闭环完成。
