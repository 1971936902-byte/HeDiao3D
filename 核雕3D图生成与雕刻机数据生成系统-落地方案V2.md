# 核雕3D图生成与雕刻机数据生成系统落地方案 V2

编制日期：2026-07-14  
适用范围：橄榄核、桃核等小型曲面浮雕/微雕的数字建模、曲面贴合、刀路生成、上机打样闭环。

## 1. 结论摘要

原技术文档的产品方向成立，但当前版本存在明显工程化风险：把通用 AI 三维生成、网格修复、四轴 CAM、机床格式兼容写成了确定能力，且承诺了超出普通核雕机、刀具和材料稳定性的精度指标。若按原文直接开发，最大风险不是做不出界面，而是生成的模型看起来像 3D，却不能稳定变成可加工、可复现、不过切的雕刻数据。

本 V2 方案建议采用“AI 生成设计稿 + 可控深度场建模 + 核胚参数化贴合 + 专用四轴刀路 + 机床标定闭环”的路线。AI 不直接作为加工几何的唯一来源，真正进入 CAM 的权威数据应是带物理尺寸、深度上限、刀具约束和曲面坐标的浮雕深度场。

推荐落地优先级：

1. 先做图像/文字到浮雕深度图，再贴合核胚曲面并生成 3+1/四轴 G-code。
2. 暂不承诺“市面所有设备”和 ENG 全兼容，先适配 1-2 台目标机型和 1 套控制系统。
3. 精度验收以“上机打样 + 扫描/显微测量 + 参数回归”为准，不以 AI 输出网格面数为准。
4. MVP 先做到单面/环绕浅浮雕稳定加工，再扩展双面、深浮雕、复杂立体雕。

## 2. 原文可行性评估

| 原文内容 | 可行性判断 | 主要问题 | V2 修正 |
|---|---:|---|---|
| Meshy API 直接生成核雕高精度模型 | 中 | 通用 3D 资产生成不等于可加工浮雕；拓扑、尺度、深度、刀具可达性不可控 | Meshy/图像模型仅用于概念稿或参考网格；CAM 输入改为深度图/高度场 |
| Real-ESRGAN 提升输入精度 | 中 | 超分会增强视觉细节，也可能生成不存在的纹理；不能当作尺寸精度来源 | 作为可选清晰化工具，保留原图和人工确认流程 |
| CGAL 保证 100% 流形修复 | 中 | CGAL 适合检测、修补、重网格化，但任意 AI 坏网格不能保证完全自动修好 | 采用质量门禁；修不好则退回深度场重建或人工修模 |
| OpenCAMLib 深度自研四轴刀路 | 中低 | OpenCAMLib 可作为三轴接触/水线算法参考，但完整核雕四轴联动、后处理和仿真仍需自研 | MVP 采用自研深度场刀路内核，OpenCAMLib/CGAL 作为辅助 |
| 全链路尺寸偏差 <= 0.01mm | 低 | 普通机床回差、刀具跳动、夹具同心度、材料弹性已可能超过该值 | 软件目标 0.01-0.03mm，实物目标按机型标定分级 |
| 刀路理论偏差 <= 0.003mm | 低 | 微小核雕上受刀具半径、步距、控制器插补、A 轴分辨率影响明显 | MVP 设定仿真残差 <= 0.02mm，成熟版再冲刺 <= 0.01mm |
| 输出兼容 90% 核雕机、ENG | 低 | ENG/厂商格式可能非公开；各控制器 G-code 方言不同 | 先建立机型配置和后处理插件，逐台验收 |
| GLB/OBJ/STL 全程无精度损耗 | 中低 | glTF/GLB 通常使用 32 位浮点；STL 不保存单位、拓扑和材质语义 | 内部采用毫米单位的 float64 深度场/网格；导出格式只作交换 |

## 3. V2 总体技术路线

### 3.1 核心原则

1. 加工几何必须可控：所有进入刀路的几何都必须有明确尺寸、深度、边界、刀具可达性。
2. AI 负责创意，不直接负责精度：AI 生成结果必须经过深度约束、人工确认和几何重建。
3. 先做稳定小闭环：从设计、仿真、上机、测量、回归参数形成闭环后，再扩大模型和机型范围。
4. 机床适配配置化：不同控制器、A 轴方向、单位、限位、刀具、夹具参数都进入机型配置文件。

### 3.2 推荐架构

```text
输入层
  文本提示词 / 参考图 / 线稿 / 灰度图 / 核胚尺寸

AI与图像层
  文本生成线稿或浮雕图
  图片清理、抠图、矢量边界、灰度深度初稿
  人工确认关键轮廓和深度语义

可控建模层
  深度场 H(u,v)
  核胚参数模型 S0(u,v)
  曲面贴合 S(u,v)=S0(u,v)+n(u,v)*H(u,v)
  高精度加工网格与轻量预览网格

CAM层
  刀具库
  粗加工、精加工、清根刀路
  过切/欠切/限位/碰撞检查
  后处理器生成机床文件

闭环层
  空跑验证
  试切
  成品扫描/显微测量
  误差补偿参数回写
```

## 4. 核心数据模型

### 4.1 权威加工数据：深度场

系统内部不以 AI 网格作为权威数据，而以二维参数域上的深度场作为权威数据：

```text
u: 核胚长度方向，单位 mm 或归一化 0-1
v: 环向角度，单位 degree/radian 或归一化 0-1
H(u,v): 浮雕深度，单位 mm
M(u,v): 雕刻区域 mask
B(u,v): 边界/保护区/禁雕区
```

优势：

1. 天然适合浅浮雕、单面浮雕和环绕浮雕。
2. 深度上限可控，避免 AI 生成不可加工的倒扣、悬空和薄壁结构。
3. 可直接生成等距扫描、等残留高度、清角等刀路。
4. 可在网页端用画笔编辑，后端用 float64 保存，便于版本管理和误差回归。

### 4.2 核胚曲面模型

MVP 采用可标定椭球/卵形回转体：

```text
x = length_axis(u)
r = radius_profile(u)
theta = v
base_surface = (x, r*cos(theta), r*sin(theta))
normal = outward_normal(base_surface)
relief_surface = base_surface + normal * H(u,v)
```

输入参数：

| 参数 | 说明 |
|---|---|
| 核长 L | 卡尺测量或拍照/扫描估计 |
| 最大直径 Dmax | 影响 A 轴角度到表面距离换算 |
| 两端直径/尖度 | 决定核胚轮廓 |
| 雕刻区域角度范围 | 单面、双面或环绕 |
| 夹持余量 | 两端禁雕区 |

成熟版可支持结构光/手机多视角重建得到真实核胚半径曲线，但 MVP 不依赖此能力。

## 5. 功能方案

### 5.1 输入与设计生成

文本输入：

1. 用户输入题材、风格、构图、核胚尺寸、浮雕深度。
2. 系统生成 2D 线稿/灰度浮雕图，而不是直接生成最终 CNC 网格。
3. 用户确认主体边界、层次关系和禁雕区域。

图片输入：

1. 背景移除、对比度增强、边缘提取。
2. 生成三类图层：轮廓 mask、灰度深度图、细节纹理图。
3. 用户可调最大深度、最小保留线宽、边缘圆角、细节强度。

可选 AI 3D：

1. 用 Meshy 等服务生成效果参考或展示模型。
2. 不把其任意三角网格直接送入刀路。
3. 若确需从 3D 网格提取浮雕，则投影/烘焙成 H(u,v) 后再进入 CAM。

### 5.2 网页编辑器

必须提供的 MVP 编辑能力：

1. 核胚尺寸输入和预览。
2. 深度图画笔：加深、减浅、平滑、擦除、局部锐化。
3. 深度范围锁定：例如 0-0.8mm、0-1.2mm。
4. 最小线宽检查：小于刀具可加工宽度的细节高亮提示。
5. 曲面预览：显示实际贴合后的浮雕，不只显示平面图。
6. 加工风险热力图：过陡、过窄、过深、靠近夹具区域。

后期增强：

1. 局部重生成。
2. 对称/镜像/双面布局。
3. 艺术纹理库。
4. 历史版本对比。

### 5.3 几何与网格处理

内部精度：

1. 所有尺寸统一使用 mm。
2. 后端几何计算使用 float64。
3. 深度场建议 MVP 分辨率 1024x512 或 2048x1024，按核胚尺寸和刀具半径动态调整。
4. 加工网格由深度场重建，预览网格可 decimate。

网格用途划分：

| 用途 | 格式 | 要求 |
|---|---|---|
| 内部权威数据 | npz/zarr + JSON 元数据 | float64, mm, 含核胚参数和版本 |
| 预览模型 | GLB | 轻量、快速加载 |
| 交换模型 | STL/OBJ/GLB | 仅用于导出或第三方查看 |
| CAM 输入 | 深度场 + 核胚参数 + 刀具库 | 不依赖 STL 单独表达加工语义 |

CGAL/Open3D/trimesh 可用于：

1. 网格检测和可视化导出。
2. 孔洞修补、重网格化、法向修复。
3. 从外部网格烘焙深度场。

不应承诺：

1. 任意 AI 网格自动修成 100% 可加工模型。
2. STL/GLB 交换后仍保留全部工程语义。

## 6. 四轴刀路方案

### 6.1 坐标约定

MVP 推荐采用 A 轴绕 X 轴旋转：

```text
X: 核胚长度方向
A: 绕 X 轴旋转角
Z: 刀具径向进给/退刀
Y: 机床侧向偏置，MVP 可固定在中心线或用于刀具补偿
```

必须在机型配置中声明：

1. A 轴正方向。
2. 工件零点位置。
3. 刀尖/刀心坐标定义。
4. 夹持半径和禁入区。
5. X/Z/A 软限位。
6. 每单位脉冲、反向间隙、最大速度、最大加速度。

### 6.2 刀路策略

MVP 三类刀路：

1. 粗加工：按深度层或余量图分层，保留 0.10-0.20mm 余量。
2. 精加工：在 (u,v) 参数域做等距扫描，按刀具半径和目标残留高度计算步距。
3. 清根/描边：对边界、凹槽和高曲率区域生成小刀补加工路径。

刀具补偿：

1. 球头刀：基于刀具半径做曲面包络补偿。
2. 平底刀：适合粗加工和平面区域，需处理边缘台阶。
3. 锥度球头刀：需要记录锥角、尖端半径、有效刃长。

步距建议：

```text
stepover ≈ sqrt(8 * tool_radius * scallop_height)
```

示例：0.5mm 球头刀，目标残留高度 0.01mm，理论步距约 0.2mm。若原文写死 0.01mm 步距，加工时间会暴涨，且未必带来可见收益。系统应按刀具、材料和目标表面质量自动推荐步距。

### 6.3 后处理输出

MVP 输出：

1. 标准 G-code：`.nc` / `.tap` / `.txt`
2. 预览刀路：JSON/CSV，用于网页回放
3. 工艺单：刀具、转速、进给、预计时间、坐标范围、风险区域

暂缓：

1. ENG 私有格式直接输出。
2. “兼容 90% 设备”的泛化承诺。

成熟版通过后处理器插件扩展：

```text
postprocessors/
  generic_grbl_4axis.json
  weihong_profile_a_axis.json
  syntec_profile_a_axis.json
  custom_machine_xxx.json
```

每个后处理器必须包含：

1. 支持/禁止的 G/M 指令。
2. A 轴单位和角度范围。
3. 是否支持 G93/G94、圆弧、宏变量。
4. 小数位、行号、换刀、主轴启停、回零逻辑。
5. 安全起刀、退刀和异常停止策略。

### 6.4 仿真与安全检查

刀路生成后必须做：

1. 坐标范围检查。
2. A 轴角度连续性检查。
3. 过切/欠切估计。
4. 夹具碰撞检查。
5. 刀具有效刃长检查。
6. 空行程和加工时间估算。
7. G-code 反解析，确认导出的文件与内部刀路一致。

## 7. 推荐技术栈

| 模块 | 推荐技术 | 说明 |
|---|---|---|
| 前端 | React/Vue + Three.js | 3D 预览、深度图编辑、刀路回放 |
| 后端 API | FastAPI | 任务提交、文件管理、WebSocket 进度 |
| 异步任务 | Celery/RQ + Redis | AI 生成、网格构建、刀路计算 |
| 数据存储 | PostgreSQL + MinIO | 元数据和模型/刀路文件 |
| 图像处理 | OpenCV, rembg, Real-ESRGAN 可选 | 抠图、边缘、灰度深度预处理 |
| 几何处理 | NumPy, SciPy, Open3D, trimesh, CGAL 可选 | 深度场、网格、检测 |
| CAM 内核 | 自研 Python/C++ 混合 | MVP 可 Python 验证，核心热点再 C++/Rust |
| 部署 | Docker Compose 起步 | 后期再上 Kubernetes |

说明：

1. CGAL 适合作为关键几何算子，但不建议第一版就把所有流程 C++ 化。
2. OpenCAMLib 可研究和复用部分算法思想，但核雕四轴深度场刀路建议自研，减少适配复杂度。
3. Meshy/其他 3D API 应定位为创意输入，不是制造级 CAD/CAM 内核。

## 8. 精度指标修订

### 8.1 软件内部指标

| 指标 | MVP | 商用版 |
|---|---:|---:|
| 深度场数值分辨率 | <= 0.01mm | <= 0.005mm |
| 曲面重建误差 | <= 0.03mm | <= 0.01mm |
| 刀路仿真残差 | <= 0.03mm | <= 0.01-0.02mm |
| G-code 坐标输出 | 3-4 位小数 | 4 位小数 |
| 网页预览加载 | 5-10s | <= 5s |

### 8.2 实物加工指标

实物指标必须按机床等级分档，不写一刀切承诺：

| 等级 | 适用设备 | 可验收目标 |
|---|---|---|
| A | 经标定高刚性四轴小机、优质夹具、稳定刀具 | 关键尺寸误差 0.03-0.05mm |
| B | 普通商用核雕机 | 关键尺寸误差 0.05-0.10mm |
| C | 低成本/老旧设备 | 以视觉还原和不过切为主，尺寸精度单独标定 |

原文中的 0.003mm/0.005mm 可作为长期研发目标，不应作为 MVP 或首版销售承诺。

## 9. MVP 范围

### 9.1 MVP 必做

1. 图片上传生成浮雕深度图。
2. 文本生成/辅助生成 2D 设计稿，再转深度图。
3. 核胚尺寸参数化建模。
4. 单面浅浮雕贴合预览。
5. 深度画笔和最大深度控制。
6. 基础刀具库：球头刀、平底刀。
7. 精加工扫描刀路。
8. G-code 输出。
9. 刀路网页回放。
10. 一台目标雕刻机上完成不少于 20 次试切闭环。

### 9.2 MVP 不做或弱化

1. 不承诺完全自动修复任意 AI 网格。
2. 不承诺 ENG 私有格式。
3. 不承诺所有核雕机通用。
4. 不做复杂倒扣立体圆雕。
5. 不把 20-50 万面网格作为核心卖点。

## 10. 项目实施计划

### 阶段 0：设备与工艺调研，1-2 周

交付物：

1. 目标机型清单和控制器说明。
2. 3-5 套刀具参数。
3. 核胚尺寸样本和夹具参数。
4. 标准测试图案。
5. G-code 空跑验证样例。

### 阶段 1：MVP 闭环，6-8 周

交付物：

1. Web 上传、预览、参数配置。
2. 深度图生成和编辑。
3. 核胚曲面贴合。
4. 单刀精加工 G-code。
5. 刀路回放和范围检查。
6. 第一批实物打样。

验收：

1. 单面浅浮雕可稳定上机。
2. 不出现明显过切和夹具碰撞。
3. 成品构图与预览一致。
4. 同一文件重复加工结果稳定。

### 阶段 2：商用增强，8-12 周

交付物：

1. 粗加工、精加工、清根三段刀路。
2. 机型配置和后处理器插件。
3. 双面/局部环绕布局。
4. 风险热力图。
5. 工艺模板库。
6. 实物误差补偿。

验收：

1. 至少适配 2 台不同控制器机型。
2. 完成 50-100 件样件测试。
3. 输出稳定工艺参数推荐。

### 阶段 3：数据与 AI 优化，3-6 个月

交付物：

1. 核雕题材数据集。
2. 线稿到深度图模型微调。
3. 局部重生成和风格库。
4. 真实核胚扫描/拟合。
5. 批量生产管理。

## 11. 测试与验收体系

### 11.1 软件测试

1. 深度图到曲面的单元测试。
2. 刀具补偿边界测试。
3. G-code 后处理快照测试。
4. G-code 反解析一致性测试。
5. 机型限位和异常参数测试。

### 11.2 工艺测试

标准测试件应包含：

1. 直线、圆弧、细线。
2. 不同深度台阶。
3. 高曲率凹槽。
4. 人像/佛像/花鸟等典型核雕题材。
5. 靠近夹持端的安全边界。

记录数据：

1. 刀具型号和磨损状态。
2. 主轴转速、进给、步距。
3. 加工时间。
4. 成品照片和测量数据。
5. 缺陷位置与原因。

## 12. 风险与应对

| 风险 | 影响 | 应对 |
|---|---|---|
| AI 结果好看但不可加工 | 上机失败或细节丢失 | AI 输出必须转深度场并经过刀具可达性检查 |
| 核胚真实形状与椭球模型不一致 | 局部深浅偏差 | 先用禁雕边界和余量控制，后期加入扫描拟合 |
| 控制器 G-code 方言差异 | 文件不能运行 | 后处理器配置化，逐机型验收 |
| A 轴回差/夹具偏心 | 双面错位、环绕接缝 | 标定流程、试切补偿、固定装夹方案 |
| 细节小于刀具直径 | 还原不足 | 编辑器高亮不可加工细节，推荐更小刀具或放大图案 |
| 加工时间过长 | 商用效率低 | 按残留高度自动推荐步距，分区精细化加工 |

## 13. 最终推荐版本定义

第一版产品应定义为：

> 面向核雕浅浮雕的 AI 辅助设计与四轴加工数据生成系统。

而不是：

> 任意文本/图片一键生成高精度 3D 核雕并兼容所有雕刻机。

推荐首版卖点：

1. 从图片/文字快速生成核雕浮雕设计。
2. 可视化调整深浅和构图。
3. 自动贴合橄榄核曲面。
4. 一键生成目标机型 G-code。
5. 刀路预览和加工风险提示。
6. 通过实物打样不断校准工艺。

## 14. 参考资料

1. Meshy API 文档：https://docs.meshy.ai/
2. CGAL Polygon Mesh Processing 文档：https://doc.cgal.org/latest/Polygon_mesh_processing/index.html
3. OpenCAMLib 官方文档：https://opencamlib.readthedocs.io/
4. Three.js GLTFLoader 文档：https://threejs.org/docs/#examples/en/loaders/GLTFLoader
5. Khronos glTF 2.0 规范：https://registry.khronos.org/glTF/specs/2.0/glTF-2.0.html
6. Real-ESRGAN 官方仓库：https://github.com/xinntao/Real-ESRGAN
7. LinuxCNC G-code 文档：https://linuxcnc.org/docs/stable/html/gcode/g-code.html

