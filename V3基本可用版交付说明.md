# HeDiao3D V3 基本可用版交付说明

日期：2026-07-22
分支：`HeDiao3D_V3`
定位：安全试雕 MVP，不是完整商用 CAM，不承诺直接量产上机。

## 1. 首版目标

本版先交付一条可以实际操作、可以下载安全试雕包、可以继续回填证据的最小闭环：

```text
导入/生成 3D 模型
  -> 生成 V3 试雕刀路与安全包
  -> 查看 3D / 模拟雕刻 / 报告
  -> 下载安全试雕包
  -> 离料空跑、旋转标定、软料试雕
  -> 回填 Linux CAM/CAMotics/机床验收证据
```

首版明确不追求一步到位替代 Aspire、ArtCAM、Fusion、Mastercam 等成熟 CAM 软件。

## 2. 当前可用范围

| 模块 | 首版状态 | 操作结论 |
| --- | --- | --- |
| 前端 HeDiao3D | 可用 | V3 聚焦模式默认开启，保留素材/模型、参数、中文流程、报告、下载主入口 |
| 3D 模型 | 可用 | 支持 Meshy GLB/STL、原始 `.stl/.obj/.glb/.gltf` 导入和佛头测试样件 |
| 后端 Orchestrator | 可用 | 支持 job、文件缓存、交付清单、试雕包、证据回填、生产门禁 |
| 后处理 | 可用作安全试雕 | 目标为三轴控制器 + Y 轴旋转夹具，`wrapY` 输出 X/Y/Z |
| 刀具 | 可用作首版默认 | 默认支持 4mm 25 度平底尖刀工艺边界 |
| 仿真 | 可用于工程验证 | 自研模拟雕刻 + CAMotics/等效材料去除结果回填 |
| Linux CAM 整单包 | 可用作证据闭环 | 可下载到 Linux 服务器执行 OpenCAMLib/CAMotics 并回填结果 |
| 基本可用版状态报告 | 可用 | 每个 job 生成 `mvp-operator-status.json/md`，一页式说明当前能做什么、禁止做什么和生产解锁缺口 |
| 正式生产 NC | 默认锁定 | 真实 CAM、材料去除、空跑、试雕、机床验收未齐时禁止放行 |

## 3. 推荐启动

```bash
npm install
npm run dev:v3
```

终端会输出本次前端访问地址和 API 端口。不要手工猜端口，避免打开旧服务。

## 4. 首版操作流程

1. 打开 V3 页面。
2. 导入 `.stl/.obj/.glb/.gltf` 模型，或点击载入佛头测试模型。
3. 检查右侧 3D 视图是否正常显示。
4. 使用默认三轴 Y 旋转夹具参数。
5. 点击“生成试雕刀路与安全包”。
6. 先阅读 `mvp-operator-status.md`，确认当前是否只允许空跑/低风险试雕。
7. 在右侧查看“模拟雕刻”和报告。
8. 点击“下载安全试雕包”。
9. 先运行空跑/标定文件，不直接使用任何仿真 NC 上机。
10. 有 Linux CAM 环境时，下载 Linux CAM 整单包，执行后回填真实结果 ZIP。
11. 软料试雕后回填机床验收记录。

## 5. 首版验收命令

轻量验收：

```bash
npm run test:v3:architecture
npm run test:v3:machine-contract
npm run test:v3:mvp-basic
npm run test:v3:mvp-release
npm run test:v3:mvp-one-command
```

覆盖：

- V3 五层架构覆盖审计：前端、Orchestrator、CAM、仿真、后处理
- 目标机床契约审计：X 长度、Y 旋转夹具、Z 刀深、`wrapY`、4mm 25 度平底尖刀、禁止 A 轴
- Orchestrator 语法检查
- V3 聚焦 UI 合约
- 佛头 3D 固定样件
- 原始 3D 模型导入成功/失败双路径：有效 STL 可进入 CAM，无三角面的 OBJ 必须提示无法进入 CAM 且禁用“生成试雕刀路与安全包”
- `wrapY` 三轴 Y 旋转后处理回归
- CAMotics/等效材料去除结果校验器
- 首版交付文档、启动脚本、固定样件和后续清单完整性
- 一键基本可用版验收报告，输出到 `reports/v3-mvp-one-command-acceptance.*`

完整工程验收仍可运行：

```bash
npm run test:v3:small-loop-acceptance
npm run build
```

## 6. 下载文件边界

允许首版使用：

- `safe-trial.zip`
- `mvp-operator-status.md/json`
- `air-run.nc`
- `rotary-calibration-airrun.nc`
- 安全报告、参数、模型、交付清单
- Linux CAM / CAMotics 证据包

禁止误用：

- `camotics-preview.nc` 只能用于仿真，不能上机。
- Linux CAM 整单包不是生产包。
- evidence/review/report 类 ZIP 不是加工交付文件。
- `production-package` 未放行时，不允许绕过系统下载正式生产 NC。

## 7. 首版完成定义

本版满足以下条件即可视为基本可用：

- 用户能导入或载入佛头 3D 模型。
- 系统能拒绝无效 STL/OBJ/GLB，不把仅本地预览误标为后端 CAM 可读取。
- 用户能生成 V3 试雕刀路与安全包。
- 用户能查看模拟雕刻和安全报告。
- 用户能下载安全试雕包。
- 系统能阻止未验收的正式生产 NC。
- 后续真实 CAM、仿真、试雕结果能通过回填入口进入证据链。

## 8. 当前不作为首版阻断

以下能力很重要，但不阻断基本可用版发布：

- FreeCAD / BlenderCAM 稳定生产级输出。
- 商用级 OpenCAMLib drop-cutter / waterline 全策略。
- CAMotics 在 Ubuntu 24.04 本机稳定运行。
- 多刀具粗加工、精加工、清残完整自动策略。
- 自动修复所有 AI Mesh 缺损。
- 直接生成可量产的正式 NC。
- 多用户权限、客户项目管理、云端任务隔离。

这些内容已整理到 `V3后续完善项清单.md`。

## 9. 发布建议

首版对外表述建议使用：

```text
HeDiao3D V3 安全试雕版：支持 3D 模型导入、三轴 Y 旋转夹具试雕刀路、安全试雕包、模拟雕刻预览和 CAM/CAMotics 证据回填。
```

不要表述为：

```text
下载 NC 后可直接雕刻成品。
完整替代商业 CAM。
AI 多图生成后自动得到生产级刀路。
```
