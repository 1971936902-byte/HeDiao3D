# 修改点汇总：建议参数与 A 轴修正

更新时间：2026-07-18

对应提交：`a58a606 Add suggested CAM parameter markers`

## 1. 参数滑条增加“建议参数”

- 在刀路生成后，根据上一版刀路结果、当前刀具、材料和机床配置计算建议参数。
- 建议参数覆盖以下项目：
  - 浮雕深度
  - X 步距
  - A 步距
  - 最大单层切深
  - 粗加工余量
  - 进给速度
  - 主轴转速
- 在参数滑条上方增加琥珀色小三角，表示建议值所在位置。
- 鼠标悬停小三角时显示建议来源说明，例如根据最大雕刻深度、刀具推荐步距或材料进给上限计算。
- 增加“应用建议”按钮，可一次性应用全部建议参数。

## 2. 小三角交互修正

- 点击单个小三角后，只移动对应滑条到建议值，不再清空已经生成的刀路。
- 点击单个小三角后，其它建议标记仍然保留，方便逐项对比和调整。
- 修正小三角 hover 时被全局按钮样式影响而上移的问题。
- 修正小三角与滑条滑块中心不对齐的问题：
  - 原先按整条滑条宽度计算位置。
  - 现在按滑块中心的实际移动范围计算，并补偿滑块半径。
- 修正浮点显示问题，例如 `0.6000000000000001°` 现在显示为 `0.6°`。

## 3. 建议参数计算规则

- 最大雕刻深度：
  - 来自 `toolpath.summary.maxDepth`。
  - 当实际刀路深度过小，认为是空白或无效场景，不显示深度建议，避免误导。
- 最大单层切深：
  - 取刀具和材料单层切深上限的保守折减值。
- 粗加工余量：
  - 按刀具预设余量和刀具直径比例计算。
- X 步距：
  - 按刀具推荐步距和刀具直径比例计算。
- A 步距：
  - 四轴模式建议不超过 `0.6°`，用于降低环向刀痕风险。
- 进给速度：
  - 按材料精加工进给、刀具建议进给和机床最大进给取保守值。
- 主轴转速：
  - 按材料、刀具和机床转速上限取保守值。

## 4. A 轴角度输出修正

- 四轴 G-code 输出时，将 A 轴角度归一到 `0~360°` 范围。
- 空跑程序同样使用归一后的 A 轴角度。
- 避免连续累加角度导致 `A` 值过大，引发机床或安全校验异常。

## 5. A 轴跳变安全校验修正

- 安全校验不再直接用 `当前 A - 上一 A` 判断跳变。
- 改为使用最短角度差判断，例如 `359° -> 0°` 会按 `1°` 处理，而不是误判为 `359°` 跳变。
- 减少跨 0/360 度边界时的误报警。

## 6. 涉及文件

- `src/App.tsx`
  - 增加建议参数计算。
  - 增加建议参数栏和小三角交互。
  - 修正滑条数值格式化和标记定位。
- `src/styles.css`
  - 增加建议参数栏、小三角、滑条标记样式。
  - 修正 hover 位移和滑块对齐。
- `src/cam.ts`
  - 本地 CAM G-code 和空跑程序的 A 轴角度归一化。
- `server.mjs`
  - Mesh CAM 后端 G-code 的 A 轴角度归一化。
- `src/manufacturingProfiles.ts`
  - A 轴跳变校验改为最短角度差。

## 7. 验证记录

- `npm run build` 通过。
- `node --check server.mjs` 通过。
- 已部署并验证服务器 API 正常。
- 最新访问地址示例：
  - `http://223.109.239.11:21370/?v=suggested-marker-align`

