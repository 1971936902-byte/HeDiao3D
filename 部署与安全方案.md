# 核雕 3D/CAM 系统本地与云端混合部署方案

更新时间：2026-07-16

## 1. 推荐结论

当前阶段建议优先采用“店内局域网部署”：

1. 前端页面运行在店内服务器或主控电脑。
2. Meshy API Key 只放在后端 `.env` 或服务器环境变量中。
3. 前端只调用 `/api/meshy/*`、`/api/mesh/*` 等代理接口。
4. 客户素材、AI Mesh、STL、刀路和加工包统一归档到服务器项目目录。
5. 高精度仿真、Mesh 修复、重网格和批量 CAM 后续迁移到后端任务队列。

这样可以兼顾试用速度、客户素材安全、API Key 安全和店内多电脑访问。

## 2. 三种部署模式

| 模式 | 适用场景 | 优点 | 风险 |
|---|---|---|---|
| 纯本地单机 | 演示、离线试算、单人开发 | 启动简单，不依赖局域网 | 数据只在本机，换电脑难追溯，不适合生产 Key |
| 店内局域网 | 门店试雕、多人协作、操作员访问 | Key 不暴露，素材可归档，操作员可用浏览器访问 | 需要一台稳定主机或小服务器 |
| 云端混合 | 多门店、远程工艺、长期客户归档 | 可做任务队列、对象存储、审计和多端同步 | 需要权限、存储、带宽和合规设计 |

## 3. 安全边界

### 3.1 API Key

- 禁止把 Meshy Key 写入前端代码、加工包、浏览器可下载文件。
- 推荐只在后端 `.env` 中配置，例如 `MESHY_API_KEY=...`。
- 前端只提交图片或任务参数给本项目后端，由后端请求 Meshy。
- 生产环境需要记录调用时间、任务 ID、失败原因和大致积分消耗，但不要记录完整 Key。

### 3.2 客户素材

- 客户原图、裁切图、AI Mesh、STL、G-code 和试雕反馈照片应按项目编号归档。
- 加工包默认不包含外部模型下载链接；需要跨设备复核时再临时开启。
- 云端部署时建议使用对象存储，并按客户/项目做访问隔离。

### 3.3 加工文件

- 操作员角色只允许下载通过安全校验、完成空跑确认和夹持确认的正式文件。
- 存在 critical 风险时，只允许下载诊断报告和离料空跑文件。
- 加工包内固定包含 `parameters.json`、安全报告、质量报告和操作说明，便于追溯。

## 4. 店内局域网部署建议

### 4.1 服务器

建议配置：

- Windows 主控电脑或 Linux 小服务器。
- 16 GB 内存起步；如果后续做本地 AI/高精度仿真，建议 32 GB 以上。
- 固定局域网 IP，例如 `192.168.1.10`。
- 项目缓存目录，例如 `./data/hediao3d-cache`。

### 4.2 服务拆分

| 服务 | 推荐位置 | 说明 |
|---|---|---|
| 前端页面 | 店内服务器 | 操作员浏览器访问 |
| Meshy 代理 | 店内服务器后端 | 读取 `.env`，不暴露 Key |
| Mesh 体检/修复 | 店内服务器后端 | 后续可升级为队列 |
| 高精度仿真 | 店内服务器或云端 Worker | 避免浏览器卡顿 |
| 文件归档 | 店内服务器磁盘 | 按项目编号分目录 |

## 5. 云端混合升级路径

当店内试雕流程稳定后，再考虑云端混合：

1. 上传客户素材到对象存储。
2. 后端生成短期签名链接供 AI Provider 使用。
3. Mesh 生成、修复、CAM、仿真进入任务队列。
4. 前端任务中心通过轮询、SSE 或 Webhook 查看进度。
5. 项目、客户、反馈、成本估算和导出记录进入数据库。
6. 管理员查看审计日志和版本历史。

## 6. 当前产品已落地内容

- 新增“部署”阶段，可配置纯本地、店内局域网、云端混合三种模式。
- 可配置 API Key 存放位置、素材存储位置、重计算位置、缓存目录和访问地址。
- 页面会输出部署健康检查：API Key、素材存储、重计算、访问地址。
- `parameters.json` 已写入部署配置，交付包可追溯当前项目的部署策略。
- 操作员下载闸口继续受安全报告、空跑确认、夹持确认控制。

## 7. 下一步建议

1. 后端补项目目录归档接口，把素材、Mesh、STL、NC 和报告保存到服务器。
2. 把 Mesh 修复、高精度仿真和 CAM 迁移到后端任务队列。
3. 增加部署环境自检接口：Key 是否存在、缓存目录是否可写、任务队列是否可用。
4. 增加操作日志：谁生成、谁确认、谁下载、下载了哪个版本。
5. 对云端模式补对象存储签名 URL 和访问过期策略。
