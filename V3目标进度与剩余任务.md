# HeDiao3D V3 目标进度与剩余任务

日期：2026-07-17  
分支：`HeDiao3D_V3`

## 1. 当前目标

当前阶段不追求把项目做成完整商用 CAM 软件，而是先完成一条可验证、可回放、可逐步上机试雕的安全闭环：

```text
导入/生成真实 3D 模型
  -> V3 Orchestrator 生成安全试雕数据
  -> 下载安全试雕包
  -> 离料空跑/软料试雕
  -> 回填试雕反馈和机床验收
  -> 再决定是否推进生产 NC 解锁
```

目标机型按用户实际设备收敛为：

```text
三轴控制器 + Y轴旋转夹具
X = 长度方向
Y = 旋转夹具等效行程/角度步进
Z = 刀深/安全高度
刀具 = 4mm 25度平底尖刀
```

## 2. 当前已完成

| 模块 | 当前状态 | 说明 |
| --- | --- | --- |
| 前端主流程 | 已收敛 | 主流程保留 3D 模型、模拟雕刻、报告；旧版热力图/G-code/生产下载入口在 V3 聚焦模式下暂时隐藏 |
| 3D 模型输入 | 可用 | 支持 Meshy 生成 GLB/STL，也支持导入原始 `.glb/.stl/.obj` 做预览和后端缓存 |
| V3 Orchestrator | 可用小闭环 | 支持 job 创建、队列、历史恢复、文件缓存、交付清单、试雕包打包 |
| 下一步行动清单 | 已增强 | 每个 V3 job 生成 `next-action-checklist.md`，明确当前允许空跑/试雕的文件、禁止上机文件和生产解锁缺口；CAMotics、试雕反馈和机床验收回填后会自动刷新证据状态 |
| 后处理 | 已验证基础链路 | `desktop-3axis-rotary-y` / `wrapY` 后处理回归通过，输出 X/Y/Z，不输出真实 A 轴 |
| 安全试雕包 | 已可下载 | 包含安全试雕执行计划、核验清单、空跑/标定/试雕相关文件和哈希绑定 |
| Native CAM 服务端包 | 已新增 | Native CAM 验收接口提供 `server-package.zip`，一键下载 bootstrap、环境模板、验收清单、真实输出验收脚本和包清单 |
| Linux CAM 闭环交接说明 | 已新增 | Native CAM 服务端包新增 `linux-cam-closed-loop-handoff.md`，把 Native CAM 验收、真实输出 ZIP、CAMotics 结果 ZIP、V3 回填、readiness 复核和现场验收串成一页式操作顺序 |
| Job级闭环交接说明 | 已新增 | 每个 V3 job 生成 `linux-cam-closed-loop-handoff.md`，并进入 delivery-manifest、package-integrity 和安全试雕 ZIP，下载试雕包后也能看到 Native CAM/CAMotics/readiness/现场验收顺序 |
| Native CAM 真实输出回填 | 已增强 | 支持单独回填 `native-cam-real-output-acceptance.json`，也支持上传 Linux 回传 ZIP；`native-cam-real-output-check.sh` 会自动生成 `native-cam-real-output-bundle.zip`，导入端自动提取验收 JSON 和 `v3-external-adapter-validation.json` 做源报告哈希绑定 |
| CAMotics 准备包 | 已增强 | 生成 `camotics-result-validate.js`、`camotics-linux-operator-checklist.md` 和一键下载的 Linux 仿真 ZIP；验证脚本通过后自动生成 `camotics-result-bundle.zip`，用于一次性回填真实仿真结果 |
| CAMotics 结果回填 | 已增强 | 支持单独回填 JSON/截图/STL，也支持上传 Linux 服务器回传的结果 ZIP，自动提取 `camotics-result.json`、本地校验报告和截图/STL 并保留原包审计 |
| 证据闭环回归 | 已新增 | `test:v3:evidence-closed-loop` 会创建 V3 job，导入 Native CAM 真实输出 ZIP 与当前 job 的 CAMotics 结果 ZIP，验证证据档案/下一步清单/加工包索引刷新，同时确认正式生产包仍被锁定 |
| Readiness CAMotics 证据视图 | 已增强 | 总门禁新增 `readinessCamoticsEvidence`，优先识别最新 job 的 `production-evidence-dossier.json` 中的材料去除证据，避免 job 已完成 CAMotics 回填但 readiness 仍显示缺失 |
| 前端总门禁材料去除证据 | 已增强 | V3 总门禁面板直接显示 `readinessCamoticsEvidence` 的来源、eligible、input/cli/motion 绑定状态和关联 job，便于判断 CAMotics 证据是否真正进入生产证据链 |
| OpenCAMLib neutral 绑定 | 已增强 | 外部 adapter 自动生成 `neutral-toolpath.json` 时，会同步生成 `neutral-toolpath-import-validation.json`，绑定 adapter 输出、后处理输入和 sourceSnapshot 哈希 |
| 生产下载门禁 | 已锁定 | 当前默认只允许 trial-only，正式生产 NC 必须等待真实 CAM/仿真/试雕证据 |
| 生产证据档案门 | 已增强 | 正式生产包下载现在必须通过 `productionReadinessAudit`，同时证明真实外部 CAM、真实材料去除仿真、NC/控制器检查、试雕与机床验收同包绑定 |
| 文档 | 已建立 | 已有 V3 架构、Linux 部署、开源 CAM 接入、试雕执行计划等文档 |

## 3. 已暂时隐藏或降级的功能

这些功能不是删除，而是在当前 V3 聚焦模式下不放在主流程中，避免误以为已经达到成熟 CAM 精度。

| 功能 | 处理方式 | 原因 |
| --- | --- | --- |
| 旧版本地浮雕生成生产下载 | 主流程隐藏 | 只能证明早期 MVP 流程，不适合作为真实佛头 3D 加工依据 |
| 旧版粗加工/精加工/清残下载按钮 | 主流程隐藏 | 当前没有真实外部 CAM 和材料去除仿真支撑，不应给出生产感按钮 |
| G-code 文本预览主入口 | 主流程隐藏 | 当前更重要的是试雕包核验和机床验收，代码级预览先放到工程模式 |
| 热力图主入口 | 主流程隐藏 | 指标仍有价值，但现阶段容易被误解为生产级精度证明 |
| 正式 ZIP/NC/TAP/TXT/CSV 下载 | 默认锁定 | 未通过生产门禁前不能让用户直接上机 |

## 4. 当前验证结果

最近一次验证：

```text
npm run build: 通过
npm run test:v3: 通过
npm run test:v3:postprocess: 通过
npm run test:v3:readiness-api: 通过
npm run test:v3:machine-acceptance-api: 通过
npm run test:v3:native-cam-api: 通过
npm run test:v3:native-cam-real-output-import-api: 通过
npm run test:v3:camotics-cli-prepare: 通过
npm run test:v3:camotics-cli-package-api: 通过
npm run test:v3:camotics-result-api: 通过
npm run test:v3:evidence-closed-loop: 通过
npm run test:v3:neutral-import: 通过
npm run test:v3:closed-neutral-handoff: 通过
浏览器 UI 检查: 通过
```

关键结果：

```text
V3 小闭环: ok
交付等级: trial-only
后处理 Profile: desktop-3axis-rotary-y
机床轴输出: X/Y/Z
A 轴输出: 0
空跑 Z: 固定安全高度 22mm
下一步行动清单: next-action-checklist.md 已进入 delivery-manifest、package-integrity 和安全试雕 ZIP；证据回填后会重写并重新哈希
Native CAM 服务端包: server-package.zip 已包含 bootstrap/env/checklist/real-output-check/manifest
Linux CAM 闭环交接说明: linux-cam-closed-loop-handoff.md 已进入 Native CAM artifact、server-package.zip 和 readiness Native CAM 证据列表
Job级闭环交接说明: linux-cam-closed-loop-handoff.md 已进入每个 V3 job 的交付清单、安全试雕 ZIP、package-integrity 和 machining-package-index readFirst
Native CAM 真实输出回填: Linux 脚本自动生成 native-cam-real-output-bundle.zip；前端/后端支持 ZIP 一次性导入验收报告和外部 adapter 验证报告，自动完成 sourceReportBinding
CAMotics 准备包: ready-for-linux-camotics，包含本地结果校验脚本、Linux 操作清单、Linux 仿真包 ZIP 下载接口和 camotics-result-bundle.zip 自动生成
CAMotics 结果回填: 支持 result ZIP 一次性导入，保留 imported-camotics-result-bundle.zip 审计原件
证据闭环回归: Native CAM 真实输出验收 ready + 当前 job CAMotics material-removal-verified 后，productionEvidenceDossier 会刷新为 trial-evidence-incomplete，生产包仍 423 锁定
Readiness CAMotics 证据视图: readiness 会公开 readinessCamoticsEvidence；当最新 job 的 CAMotics 证据比历史全局 contract 更新且已达标时，总门禁使用 latest-job-evidence-dossier 作为材料去除证据来源
前端总门禁材料去除证据: V3 UI 已显示 readinessCamoticsEvidence 来源、eligible、input/cli/motion 和 job 绑定状态
前端 V3 主线收敛: CAM 页固定显示“导入/生成3D模型、生成安全试雕数据、下载安全试雕包、生成/下载 CAMotics Linux 仿真包”，旧版直接 NC 下载、正式生产包和高级 Adapter 面板在试雕聚焦模式下继续收起
证据审查包: 新增 `/api/orchestrator/jobs/:id/evidence-review-package`，集中打包 production gate、package integrity、后处理追溯、CAMotics、试雕/机床验收等证据和缺失项清单；该包仅用于审查，不作为上机包
OpenCAMLib closed neutral handoff: trial-only，neutral/sourceSnapshot 已绑定，生产仍锁定
Production readiness audit: 已进入 readiness/evidence dossier；未满足现场证据同包绑定时生产包继续 423 锁定
```

这说明当前系统可以继续做安全试雕包闭环，但还不能宣称“下载后直接雕刻成品”。

## 5. 还未完成的关键任务

### P0：必须完成，才可能进入真实试雕

1. 在 Linux CAM 服务器安装并验证 FreeCAD / OpenCAMLib / CAMotics。
2. 让至少一个外部 CAM adapter 输出真实刀路或中立刀位点，而不是 fallback/fixture/synthetic。
3. 用 CAMotics 或等效方式跑真实材料去除仿真，并把结果回填到当前 job。
4. 用同一套佛头 3D 模型完成：
   - Orchestrator job
   - 安全试雕包下载
   - 离料空跑
   - 软料试雕
   - 反馈和照片记录
   - 机床验收记录
5. 校准 Y 旋转夹具：
   - Y 行程与实际旋转角度换算
   - 方向正反
   - 圆周展开比例
   - 两端不可加工区域

### P1：试雕后提升精度

1. OpenCAMLib drop-cutter / waterline 接入真实佛头曲面。
2. 三轴+旋转夹具专用粗加工、精加工、清残策略。
3. 刀具半径补偿和 25 度平底尖刀真实几何模型。
4. 端部保护区、夹持区、不可达区自动避让。
5. 试雕误差反补偿：根据实物照片/测量数据修正深度和包络。

### P2：产品化体验

1. 工程模式与操作员模式彻底分离。
2. 机床 Profile、刀具库、材料库可视化维护。
3. 加工包版本管理和试雕案例库。
4. 支持更多控制器方言。
5. 支持公网部署的账号、权限、任务隔离和文件清理策略。

## 6. 下一步建议

下一步优先做“真实外部 CAM + CAMotics 结果回填”的服务器闭环，而不是继续扩展前端按钮。

推荐顺序：

1. 部署 Linux CAM 环境。
2. 跑 `npm run test:v3:native-cam` 和 adapter 验收。
3. 选一个佛头 STL/GLB 作为固定样件。
4. 用 OpenCAMLib/FreeCAD 输出真实候选刀路。
5. 回填 CAMotics 真实仿真结果。
6. 生成安全试雕包。
7. 离料空跑和软料试雕。

只有这些证据全部绑定到同一个 job 和同一套下载文件哈希后，`productionReadinessAudit.allowProductionPackage=true`，才允许正式生产 NC 下载。
