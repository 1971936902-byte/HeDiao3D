# HeDiao3D V3 目标进度与剩余任务

日期：2026-07-22
分支：`HeDiao3D_V3`
最新提交：以 `git log -1 --oneline` 为准

## 0. 当前整体进度快照

截至 2026-07-19，本项目已经从早期“前端生成演示刀路”推进到 V3 安全闭环架构：

| 方向 | 完成度 | 当前结论 |
| --- | ---: | --- |
| 前端可操作流程 | 约 85% | 3D 模型、V3 任务、安全试雕包、报告和回填入口可用；真实浏览器 E2E 已覆盖佛头测试模型、本地 STL 原始模型导入、后端 CAM 缓存、模拟雕刻视图和安全试雕包下载；本轮已把 V3 关键 ZIP/报告下载统一到带网络错误、HTTP 错误和空文件提示的包装器，并纳入 focused-ui 防回退测试，仍需继续补更多 GLB/OBJ 导入失败态浏览器测试 |
| 后端 Orchestrator | 约 82% | job 队列、缓存、交付清单、证据包、生产门禁、小闭环测试均可用；readiness 已拆分“生产总门禁”和“当前 job 安全试雕/离料空跑状态”，并透传 OpenCAMLib 生产差距审查，避免 blocked 生产状态掩盖可执行的安全试雕下一步或真实 CAM 缺口 |
| 三轴控制器 + Y 旋转夹具后处理 | 约 75% | `wrapY` 输出 X/Y/Z，后处理追溯、NC 静态分析、控制器方言检查通过；仍需实机标定参数和软料试雕证据 |
| OpenCAMLib 几何内核接入 | 约 65% | 已有旋转 heightfield/neutral 预览、小闭环、contact report 契约、候选包预检、运行时能力探测、runner readiness 回填和 Linux 一键真实候选链路；probe/spike/runner 已优先兼容官方 `opencamlib.ocl` 与 `PathDropCutter` 接入形态，并有 fake 官方绑定 ready 回归；PathDropCutter contact report 已补采样步距、stepToCutterRatio、残料估算、路径覆盖率和生产阻断原因；本轮新增 `productionGapReview` 与 strict contact `topErrors/failedChecks` 贯穿后端、候选包、readiness 和前端，能直接看到实验 API、残料证据或哈希/覆盖率失败的具体原因；还不是生产级 drop-cutter/contact CAM |
| FreeCAD / BlenderCAM 接入 | 约 20% | 有 adapter 计划和验收框架；FreeCAD 降为 P1 规则三轴参考 CAM，BlenderCAM/FabexCNC 降为 P1/P2 艺术 Mesh 研究路线；真实稳定输出尚未接入 |
| CAMotics / 材料去除仿真 | 约 62% | 准备包、结果回填、校验器和证据链已完成；CAMotics run package / result / Linux validator / 后端 evidenceQuality 已新增上游 Native CAM/OpenCAMLib 证据哈希绑定，避免仿真结果脱离真实 CAM 候选来源；`residualClosureReview` 已把“材料去除仿真已闭环”和“OpenCAMLib 残料/过切生产证据是否已闭合”拆开显示；本轮新增 CAMotics/等效仿真结果 `residualValidation`，支持 `maxGougeMm/maxUndercutMm/maxResidualStockMm`、测量或 swept-volume validated 依据和容差校验，合格后 residual closure 可提升到 `production-residual-closed`；P0 保留“材料去除证据”，具体执行可用 CAMotics、旧系统或等效仿真器 |
| 现场试雕闭环 | 约 30% | 机床验收、空跑、试雕反馈 API 与证据模型已完成；还缺真实机器数据回填 |
| 直接生产可用 | 约 25% | 当前仍应定位为“安全试雕/工程验证阶段”，不能承诺下载后直接雕成成品 |

当前最重要的判断：

```text
可以继续做：安全试雕包、离料空跑、软料试雕、Linux CAM 证据回填。
不应承诺：正式生产 NC 直接上机、成品级 CAM 精度、完整商用 CAM 替代。
```

### 0.1 并行推进分工（2026-07-18 更新）

为了加快工程进度，后续按模块拆分给多个 Agent/子任务并行推进，但主线必须统一到同一个目标机型和同一套安全门控。

| 模块 | 当前负责人/方向 | 优先级 | 当前结论 |
| --- | --- | --- | --- |
| CAM 主链路 | 主 Agent | P0 | 优先推进 OpenCAMLib 真实 cutter-contact/drop-cutter 中立输出；本轮已增强候选包预检，新增 artifact manifest 和 neutral handoff contract |
| 前端操作员主流程 | 主 Agent + 子任务审查 | P0/P1 | 已继续收敛为“导入3D模型 -> 生成三轴Y旋转试雕刀路 -> 模拟雕刻 -> 下载安全试雕包/NC”；V3 聚焦模式下隐藏 CAM 模式/后处理/精修策略等工程下拉，并把机床验收、质量体检、材料去除、成本等详细面板转入工程模式 |
| Linux/真实 CAM 环境 | 子任务审查完成 | P0 | Linux 框架已有；P0 收敛为 OpenCAMLib 真实 drop-cutter/cutter-contact + CAMotics/等效材料去除证据；FreeCAD/BlenderCAM 暂不作为佛头旋转核雕主线 |
| 测试体系 | 子任务审查完成 | P0/P1 | 后端安全架构覆盖较好，最缺真实佛头 E2E、前端按钮/视图 E2E、真实 CAMotics 材料去除质量测试 |

当前工程策略：

```text
先补 P0 主链路：真实 OpenCAMLib 输出契约 -> 真实 CAM 结果导入 -> CAMotics/等效材料去除 -> 安全试雕包。
同步做 UI 收敛：隐藏非必要按钮，保留操作员能理解的四步主流程。
暂缓功能：完整商用 CAM 替代、多控制器方言扩展、旧版浮雕生产下载、多格式散落下载。
```

本轮新增验证：

```text
npm run test:v3:opencamlib-candidate-package: 通过
npm run test:v3:opencamlib-contact-validate: 通过
npm run test:v3:rotary-neutral-handoff: 通过
npm run test:v3:buddha-e2e: 通过
npm run test:v3:opencamlib-probe: 通过（当前 Windows 环境 level=missing；已升级为优先探测官方 Python 入口 opencamlib.ocl，再兼容 opencamlib/ocl）
npm run test:v3:opencamlib-adapter-readiness: 通过（runner fail-closed 时写出 opencamlib-runner-readiness.json 和 contact spike 预检证据）
npm run test:v3:opencamlib-contact-spike: 通过（当前 Windows 环境 fail-closed blocked；新增 fake opencamlib.ocl ready 回归，确认 PathDropCutter 分支可产出非生产 neutral spike）
npm run test:v3:opencamlib-runner: 通过（`HEDIAO3D_OPENCAMLIB_RUNNER_PATH_DROPCUTTER_OUTPUT` 实验入口可用；fake opencamlib.ocl 下 runner 可产出 PathDropCutter neutral + 带自适应 pathGrid、pathCoverage、stepToCutterRatio、residualMaterial/tolerances、candidateMachineFit、materialRemovalReadiness 的 contact report，默认仍 fail-closed）
npm run test:v3:opencamlib-contact-validate: 通过（28 checks；strict contact 现在要求 pathCoverage >= 98%、端部保护区 ready、残料必须 measured 或有 swept-volume/material-removal/validated 依据，并继续区分 experimental-real-api 与 production-candidate）
npm run test:v3:opencamlib-candidate-package: 通过（28 checks；候选包预检会透传 evidenceClass，并把 experimental-real-api、覆盖率不足、残料仅工程估算或 model/plan/neutral/contact hash 错配明确阻断为工程证据）
npm run test:v3:opencamlib-candidate-package: 通过（strict contact 摘要新增 topErrors/failedChecks，productionGapReview 会携带失败 check evidence，便于定位当前生产阻断项）
npm run test:v3:native-cam-real-output-import-api: 通过（Native CAM 真实输出 ZIP 可随包导入 opencamlib-runner-readiness.json，并在 readiness 中显示 runner blocked/ready 状态）
npm run test:v3:native-cam-real-output-import-api: 通过（Native CAM 接触验证导入新增 topErrors/failedChecks；低覆盖率样例能暴露 contact-path-coverage-x 的 reported/expected 明细）
npm run test:v3:native-cam-package-self-check: 通过（85 checks；服务包自检确认 native-cam-real-output-check.sh 会携带 runner readiness 与 real candidate 证据，实际运行 opencamlib-real-candidate-run.mjs 的 fail-closed 路径、`native-cam-closed-loop-check.mjs --self-check-only` evidenceChain，并新增 CAMotics material-removal runner provided-result 小闭环；closed-loop 现在会在发现 neutral 输出时自动运行 OpenCAMLib candidate package validation，并把 candidatePackageStep/readyForImport 写入 evidenceChain）
npm run test:v3:camotics-cli-package-api: 通过（CAMotics Linux 准备包现在写入 upstreamCamEvidence，结果模板和本地 validator 要求回填结果绑定同一组 Native CAM/OpenCAMLib 证据哈希）
npm run test:v3:camotics-cli-package-api: 通过（CAMotics upstreamCamEvidence 现在会把 OpenCAMLib candidate package validation 与 candidate package bundle 也纳入哈希绑定；材料去除结果必须保留候选包预检证据哈希）
npm run test:v3:camotics-cli-package-api: 通过（CAMotics/Linux 结果模板和操作清单新增 residualValidation.maxGougeMm/maxUndercutMm/maxResidualStockMm，引导把残料/过切证据与材料去除证据分开闭合）
npm run test:v3:camotics-material-validate: 通过（17 checks；新增 upstream-cam-evidence 正反例，缺失或不匹配时 fail-closed）
npm run test:v3:camotics-result-api: 通过（后端 CAMotics evidenceQuality 识别 upstreamCamEvidence matched/not-required，并阻断未绑定结果进入生产证据；新增 residualClosureReview，证据档案和加工包索引会显示残料/过切闭环复核状态，且不绕过生产门禁）
npm run test:v3:camotics-result-api: 通过（residualClosureReview 新增 checks/topBlockers/nextActions；API 响应、productionEvidenceDossier 和 machiningPackageIndex 都会保留残料闭环首要阻断原因）
npm run test:v3:camotics-material-validate: 通过（新增 residualValidation；无残料指标时保持 productionResidualEvidenceReady=false，提供 measured/swept-volume validated 且 gouge/undercut 在容差内时可标记 productionResidualEvidenceReady=true）
npm run test:v3:camotics-result-api: 通过（带 residualValidation 的 CAMotics/等效仿真回填可把 residualClosureReview.status 提升为 production-residual-closed，但生产包仍需其它总门禁证据）
npm run test:v3:evidence-closed-loop: 通过（Native CAM + CAMotics 证据闭环可保留材料去除 verified，同时正式生产包仍锁定）
npm run test:v3:readiness-runbook-result-import-api: 通过（Runbook ZIP 导入会公开 Linux native-cam closed-loop evidenceChain，并保留 CAMotics upstream 绑定摘要）
npm run test:v3:readiness-api: 通过（总门禁仍保持 production=false；Linux 证据链未齐或 blocked 时不会误放行）
npm run test:v3:readiness-api: 通过（readiness 会保留 native CAM strict contact 失败明细，不再只显示笼统 critical）
npm run test:v3:focused-ui: 通过（新增 residual-closure-review-visible 和严格接触失败提示，锁定“残料/过切复核/接触验证阻断原因”必须在 V3 聚焦报告区可见）
npm run test:v3:focused-ui: 通过（新增 v3-download-error-guidance，锁定 V3 安全试雕包、CAMotics Linux 包、OpenCAMLib 输入包、Linux 整单包和证据审查包必须共享可见下载失败提示）
npm run test:v3:frontend-e2e: 通过
npm run test:v3:frontend-import-e2e: 通过
npm run test:v3:small-loop-acceptance: 通过（28/28）
npm run build: 通过
```

本轮新增固定回归样件：

```text
基准清单: public/v3-fixtures/buddha-baseline.json
默认 GLB: /meshy-results/019f6a05-c78b-7c70-b07f-ea857a54bea5.glb
默认 STL: /meshy-results/019f6a05-c78b-7c70-b07f-ea857a54bea5.stl
校验命令: npm run test:v3:buddha-fixture
用途: V3 smoke、后处理、CAMotics、证据闭环统一使用同一佛头 Mesh 回归输入
```

## 1. 当前目标

当前阶段不追求把项目做成完整商用 CAM 软件，而是先完成一条可验证、可回放、可逐步上机试雕的安全闭环：

```text
导入/生成真实 3D 模型
  -> V3 Orchestrator 生成安全试雕数据
  -> 下载安全试雕包
  -> 离料空跑/软料试雕
  -> 回填试雕反馈和机床验收
  -> 再决定是否推进生产 NC 解锁
```

目标机型按用户实际设备收敛为：

```text
三轴控制器 + Y轴旋转夹具
X = 长度方向
Y = 旋转夹具等效行程/角度步进
Z = 刀深/安全高度
刀具 = 4mm 25度平底尖刀
```

### 1.1 基本可用版收敛口径（2026-07-19）

为尽快交付一版可操作版本，V3 当前发布口径收敛为“安全试雕 MVP”：

```text
必须可用：
  导入/载入 3D 模型
  生成试雕刀路与安全包
  查看模拟雕刻和安全报告
  下载安全试雕包
  保持正式生产 NC fail-closed
  支持 Linux CAM/CAMotics/机床验收证据回填

暂不阻断首版：
  完整商用 CAM 精度
  FreeCAD/BlenderCAM 生产级稳定输出
  自动清残/多刀具完整策略
  CAMotics 在 Ubuntu 24.04 本机直接可运行
  多用户/客户/云端权限体系
```

首版说明：`V3基本可用版交付说明.md`

后续清单：`V3后续完善项清单.md`

轻量验收：

```bash
npm run test:v3:mvp-basic
```

## 2. 当前已完成

| 模块 | 当前状态 | 说明 |
| --- | --- | --- |
| 前端主流程 | 已收敛 | 主流程保留 3D 模型、模拟雕刻、报告；旧版热力图/G-code/生产下载入口在 V3 聚焦模式下暂时隐藏 |
| 前端聚焦 UI 防回退 | 已新增 | 新增 `npm run test:v3:focused-ui`，源码级锁定 V3 聚焦操作流：主按钮必须是“生成试雕刀路与安全包”，成功后切到“模拟雕刻”，本地模型预览与后端 CAM 缓存状态必须区分，旧版直接生产下载必须在聚焦模式隐藏；该测试已纳入小闭环 |
| 前端浏览器 E2E | 已增强 | 新增 `npm run test:v3:frontend-e2e` 与 `npm run test:v3:frontend-import-e2e`，无需 Playwright/Puppeteer 依赖，直接用 Chrome DevTools Protocol 启动本机 Chrome、API 和 Vite；真实点击“载入佛头测试模型”或在浏览器内构造 `.stl` 文件走“导入原始3D模型”，验证后端 CAM 缓存、生成试雕刀路、右侧切到“模拟雕刻”、Canvas 可见，并拦截确认 `safe-trial.zip` 下载触发；已纳入小闭环 |
| 3D 模型输入 | 可用 | 支持 Meshy 生成 GLB/STL，也支持导入原始 `.glb/.stl/.obj/.gltf` 做预览和后端缓存；OBJ 已进入后端 CAM 小闭环验收 |
| V3 Orchestrator | 可用小闭环 | 支持 job 创建、队列、历史恢复、文件缓存、交付清单、试雕包打包 |
| 下一步行动清单 | 已增强 | 每个 V3 job 生成 `next-action-checklist.md`，明确当前允许空跑/试雕的文件、禁止上机文件和生产解锁缺口；CAMotics、试雕反馈和机床验收回填后会自动刷新证据状态 |
| Job级机型边界证据 | 已增强 | `next-action-checklist.md` 和 `machining-package-index.json` 会读取最新 Native CAM 真实输出验收，离线显示 `targetMachineBoundaryStatus`，避免下载包脱离网页后误判通用 CAM 输出 |
| 后处理 | 已验证基础链路 | `desktop-3axis-rotary-y` / `wrapY` 后处理回归通过，输出 X/Y/Z，不输出真实 A 轴 |
| 制造参数后端门控 | 已新增 | `manufacturing-setup-report.json` 会统一校验刀具、材料、机床、进给、转速、切深和步距；critical 项进入 `production-gate.json` 阻断 |
| Neutral 刀路连续排序 | 已新增 | 外部 `neutral-toolpath.json` 进入 wrapY/wrapA 后处理前会生成 `toolpath-sequencing-report.json`，按旋转角分行、X 向奇偶行往复排序，并记录排序前后行数、行走距离、旋转跳跃和 X 回跳；该报告进入交付清单、加工包索引和哈希完整性 |
| 安全试雕包 | 已可下载 | 包含安全试雕执行计划、核验清单、空跑/标定/试雕相关文件和哈希绑定 |
| Native CAM 服务端包 | 已增强 | Native CAM 验收接口提供 `server-package.zip`，一键下载 bootstrap、环境模板、验收清单、服务包离线自检、诊断证据打包脚本、真实输出验收脚本、OpenCAMLib runner/probe/contact spike/一键真实候选链路/contact 验收器、CAMotics 材料去除 runner/验收器和包清单；API 摘要已公开 `packageSelfCheck`、`diagnosticsBundle` 和完整 manifest 文件列表 |
| Native CAM 服务包自检回归 | 已增强 | `test:v3:native-cam-package-self-check` 会实际生成 Native CAM 服务包、运行包内 `native-cam-server-package-self-check.mjs`，再运行 `opencamlib-real-candidate-run.mjs` 和 `camotics-material-removal-run.mjs` 的缺输入 fail-closed 路径、`native-cam-closed-loop-check.mjs --self-check-only`、带最小 OpenCAMLib candidate 文件的 closed-loop 汇总路径和 `native-cam-diagnostics-bundle.mjs`，验证可生成闭环 evidenceChain 与诊断 ZIP，并用缺失 CAMotics 验收器的反例确认会 fail-closed；当前自检覆盖 85 项 |
| Native CAM 一键闭环检查入口 | 已增强 | 服务端包新增 `native-cam-closed-loop-check.mjs` 并接入 readiness acceptance plan/runbook，可在 Linux 侧串联服务包自检、真实 CAM 输出验收和 CAMotics 材料去除验收，生成 `native-cam-closed-loop-check.json`；本轮新增 `hediao3d.native-cam-linux-evidence-chain.v1`，汇总 Native CAM/OpenCAMLib/CAMotics 证据、bundle SHA-256、strict contact 状态和 CAMotics upstream 绑定；该入口只汇总证据并保持生产锁，不解锁 NC |
| Native CAM 诊断证据包 | 已增强 | 服务端包新增 `native-cam-diagnostics-bundle.mjs`，Linux 侧可在没有真实 CAM 输出前先运行服务包自检、OpenCAMLib runtime probe、OpenCAMLib real contact spike、FreeCAD/Blender/CAMotics/Node 命令探测，生成 `native-cam-diagnostics.json/md` 和 `native-cam-diagnostics-bundle.zip`；该证据包只用于环境诊断，`productionLocked=true` |
| Readiness Runbook 服务包自检 | 已增强 | 总门禁 acceptance plan 新增 `native-cam-package-self-check` 步骤，`v3-acceptance-runbook.sh` 会要求 Linux 侧先运行 `node native-cam-server-package-self-check.mjs` 并保留 `native-cam-server-package-self-check.json` |
| Readiness Runbook Linux evidenceChain | 已增强 | Runbook ZIP 导入会从 `native-cam-closed-loop-check.json.evidenceChain` 提取 Linux 侧 Native CAM、OpenCAMLib、CAMotics、upstream 绑定和阻断摘要；公开摘要进入 `linuxEvidence.evidenceChain`，且 evidenceChain blocked 时 `productionSafe` 保持关闭 |
| Linux CAM 服务闭环入口 | 已前置 | V3 聚焦模式下也显示服务包生成、下载和真实输出 ZIP/JSON 回填入口；该入口只补真实 CAM 证据链，不解锁生产 NC |
| Linux CAM 闭环交接说明 | 已新增 | Native CAM 服务端包新增 `linux-cam-closed-loop-handoff.md`，把 Native CAM 验收、真实输出 ZIP、CAMotics 结果 ZIP、V3 回填、readiness 复核和现场验收串成一页式操作顺序 |
| Job级闭环交接说明 | 已新增 | 每个 V3 job 生成 `linux-cam-closed-loop-handoff.md`，并进入 delivery-manifest、package-integrity 和安全试雕 ZIP，下载试雕包后也能看到 Native CAM/CAMotics/readiness/现场验收顺序 |
| Native CAM 真实输出回填 | 已增强 | 支持单独回填 `native-cam-real-output-acceptance.json`，也支持上传 Linux 回传 ZIP；`native-cam-real-output-check.sh` 会自动生成 `native-cam-real-output-bundle.zip`，导入端自动提取验收 JSON、`v3-external-adapter-validation.json`、`opencamlib-contact-output-validation.json`、`opencamlib-runner-readiness.json` 和 `opencamlib-real-candidate-run.json`，做源报告哈希绑定与真实候选链路摘要 |
| Native CAM strict contact 绑定 | 已增强 | Native CAM 真实输出验收现在必须绑定 `opencamlib-contact-output-validation.json`；OpenCAMLib production-candidate 若缺少 ready strict contact 验证，会自动降级为 critical，不能进入生产证据链 |
| Native CAM 目标机型边界 | 已增强 | Linux 服务包和真实输出验收 ZIP 现在写入 `target-machine-boundary.json`；回填端校验三轴控制器+Y轴旋转夹具、`wrapY`、4mm 25度平底尖刀，不匹配时降级为 review |
| 前端总门禁机型边界提示 | 已增强 | V3 总门禁的真实 CAM 输出验收行现在显示 `machineBoundary matched/missing/mismatch`，缺失或不匹配时直接显示边界警告 |
| CAMotics 准备包 | 已增强 | 生成 `camotics-result-validate.js`、`camotics-linux-operator-checklist.md` 和一键下载的 Linux 仿真 ZIP；验证脚本通过后自动生成 `camotics-result-bundle.zip`，用于一次性回填真实仿真结果；结果模板已绑定 `machineContext` |
| CAMotics 结果回填 | 已增强 | 支持单独回填 JSON/截图/STL，也支持上传 Linux 服务器回传的结果 ZIP，自动提取 `camotics-result.json`、本地校验报告和截图/STL 并保留原包审计；生产证据必须匹配 G-code 哈希、运行包哈希、运动画像和机床上下文 |
| CAMotics 回填审计 | 已新增 | 每次回填都会生成 `camotics-result-import.json`，记录来源、ZIP 条目、preview/run-package 哈希绑定和本地校验摘要，进入交付清单和完整性哈希 |
| CAMotics 材料去除验收器 | 已新增 | 新增独立 `v3-camotics-material-removal-validate.mjs`，可在 Linux/本地对 `camotics-result.json` 与 `camotics-cli-run-package.json` 做哈希、运动画像、机床上下文、非 synthetic 和截图/STL 证据校验，通过后生成 `camotics-result-bundle.zip` |
| CAMotics 材料去除执行包装 | 已新增 | Native CAM 服务包新增 `camotics-material-removal-run.mjs`，Linux 侧可从 `camotics-cli-run-package.json` 尝试调用 `HEDIAO3D_CAMOTICS_RUN_COMMAND`、`HEDIAO3D_CAMOTICS_COMMAND` 或本机 `camotics/camotics-cli`，生成真实 `camotics-result.json` 后自动运行材料去除 validator 并产出 `camotics-result-bundle.zip`；没有真实仿真器或没有结果 JSON 时 fail-closed，不生成 synthetic 通过证据 |
| CAMotics 上游 CAM 证据绑定 | 已新增 | `camotics-cli-run-package.json` 会捕获 job 目录中的 `native-cam-real-output-acceptance.json`、`opencamlib-contact-output-validation.json`、`opencamlib-real-candidate-run.json`、`opencamlib-candidate-package-validation.json`、`v3-external-adapter-validation.json` 等来源证据哈希；`camotics-result-template.json`、Linux 校验器和后端 `evidenceQuality.upstreamCamEvidence` 会要求回填结果保持同源绑定，防止材料去除仿真与真实 CAM 候选来源脱节 |
| CAMotics 上游机床适配门禁 | 已增强 | `camotics-cli-run-package.json` 和 `camotics-result-template.json` 现在会携带 OpenCAMLib candidate `machineFit` 摘要；Linux 随包 validator 与独立 `v3-camotics-material-removal-validate.mjs` 会拒绝绑定到 `critical` 机床适配候选包的材料去除结果 |
| 残料/过切闭环复核 | 已新增 | `production-gate.json` 的 `simulationEvidence` 新增 `hediao3d.residual-closure-review.v1`；当 CAMotics/等效材料去除通过但 OpenCAMLib residual 仍是工程估算时，状态为 `engineering-closed` 或 `simulation-verified-upstream-review`，证据档案显示为 review，不单独解锁生产 NC |
| 等效材料去除仿真器证据 | 已新增 | `camotics-cli-run-package.json` 现在声明 `simulatorEvidence`，`camotics-result-template.json` 和校验器要求记录 `simulator.name/version/sourceCommand`；CAMotics 不可用时允许容器、旧系统或等效材料去除仿真器，但必须通过同一 hash/motion/machine/artifact 校验，仍不能单独解锁生产 |
| Readiness Runbook CAMotics 本地验收 | 已增强 | 总门禁 acceptance plan 新增 `camotics-material-removal-validate` 步骤，要求 Linux 侧在回填前运行 `node camotics-material-removal-validate.mjs --result camotics-result.json --run-package camotics-cli-run-package.json` 并保留 `camotics-result-local-validation.json` |
| 证据闭环回归 | 已新增 | `test:v3:evidence-closed-loop` 会创建 V3 job，导入 Native CAM 真实输出 ZIP 与当前 job 的 CAMotics 结果 ZIP，验证证据档案/下一步清单/加工包索引刷新，同时确认正式生产包仍被锁定 |
| 小闭环一键验收 | 已增强 | `npm run test:v3:small-loop-acceptance` 会启动临时 API 端口，串行执行前端构建、固定佛头样件、真实浏览器操作员 E2E、真实浏览器原始 STL 导入 E2E、V3 smoke、OBJ 模型导入、制造参数门控、后处理回归、readiness、runbook 结果回填、Native CAM/CAMotics 包、机床验收、生产包解锁闭环等测试，避免并行抢 latest job/readiness 造成误报 |
| 真实佛头 E2E 回归 | 已新增 | `npm run test:v3:buddha-e2e` 使用固定佛头 STL 451,536 面走 Orchestrator，验证 8979 点三轴Y旋转刀路、X/Y/Z 输出、A轴为0、Mesh体检 ready、后处理追溯 fitRate=1、安全试雕 ZIP、CAMotics 预览禁止上机和正式生产包 423 锁定；已纳入 `small-loop-acceptance` |
| OpenCAMLib 小闭环入口 | 已增强 | 新增 `npm run test:v3:opencamlib-small-loop`，固定验证 Orchestrator 调用 OpenCAMLib heightfield runner、生成 neutral-toolpath、转 wrapY NC、生成 CAM handoff 质量报告，并确认 preview scaffold 不解锁生产 |
| OpenCAMLib 运行时能力探测 | 已增强 | `adapters/opencamlib/opencamlib_probe.py` 和 `npm run test:v3:opencamlib-probe` 可在 Linux/本地探测 `opencamlib`/`ocl` 模块、版本、surface/cutter/drop-cutter 候选符号和 `dropCutterReady` 状态；本轮新增 `recommendedBindings`、`runnerReadiness` 和 `integrationPlan`，用于判断是否可开始真实 contact runner spike；服务包自检与 Native CAM 包清单已覆盖该文件。该探测只作为真实 runner 实现前的环境证据，不生成刀路、不解锁生产 |
| OpenCAMLib runner readiness | 已新增 | `opencamlib_runner.py` 在真实输出未实现或 Linux 绑定不足时，会 fail-closed 写出 `opencamlib-runner-readiness.json` 与 `opencamlib-real-contact-spike.json` 预检证据；`opencamlib_job.py` 会把该摘要带入 adapter-report，便于 Orchestrator 明确看到模块、probe、spike、模型、刀具和 Y 旋转映射缺口 |
| OpenCAMLib 一键真实候选链路 | 已新增 | Native CAM 服务包新增 `opencamlib-real-candidate-run.mjs`，Linux 侧可在同一目录串联 runtime probe、real contact spike、`HEDIAO3D_OPENCAMLIB_RUNNER_PATH_DROPCUTTER_OUTPUT=true` runner、strict contact validate 和 candidate package validate；脚本输出 `opencamlib-real-candidate-run.json`，默认 `productionLocked=true`，只在 strict production-candidate 证据齐全时显示 ready-for-import；真实输出 ZIP 回填后 readiness 会显示该链路的 ready/blocked 摘要 |
| OpenCAMLib job级输入交接包 | 已新增 | 当前 V3 job 可通过 `GET /api/orchestrator/jobs/:id/opencamlib-candidate-inputs.zip` 下载 Linux real-candidate 输入包，包含路径归一化的 `job.json`、`opencamlib-kernel-plan.json`、`repaired-model.stl`、清单和 README；前端聚焦模式新增“下载OCL输入包”，用于复制到 Native CAM 服务包目录后运行 `opencamlib-real-candidate-run.mjs`，该包只补真实 CAM 候选输入，不解锁生产 NC |
| OpenCAMLib PathDropCutter 质量报告 | 已增强 | `opencamlib_runner.py` 的真实 API 实验分支现在会按 `stepoverMm`/刀径自适应计算 PathDropCutter 横向行数，保留环境变量覆盖和行数上限，并在 contact report 中写入 `pathGrid`、`pathCoverage`、`contactPointCount`、`xStepMm/crossStepMm`、`stepToCutterRatio`、`tolerances`、保守 `residualMaterial` 估算和 `productionCandidateBlockers`；独立 validator、后端 neutral 导入分类和 OpenCAMLib adapter 分类现在都要求 X/cross pathCoverage >= 98%；粗采样或覆盖不足会明确阻断生产候选，避免 Linux 侧只看到“缺字段”而看不到质量差距 |
| OpenCAMLib 端部保护区证据 | 已增强 | PathDropCutter runner 现在按 `leftHoldMm/rightHoldMm/endTransitionMm` 缩进采样路径，只覆盖旋转夹具可加工 X 区间；`opencamlib-cutter-contact-report.json` 新增 `protectedZones`，strict validator、后端 neutral 直接导入和候选包预检都要求 `enabled=true`、`violationCount=0`、采样 X 不进入左右夹持/过渡保护区；Native CAM ZIP 回填、Linux closed-loop evidenceChain、readiness runbook、`next-action-checklist.md` 和 `machining-package-index.json` 现在都会保留端部保护区 ready/review/missing 摘要 |
| Native CAM 覆盖率诊断摘要 | 已新增 | Native CAM 真实输出导入、导入 artifact 和 readiness 现在都会公开 `contactValidationStatus.pathCoverage` / `contactValidation.pathCoverage`，单独显示 X 向与旋转/横向覆盖率检查是否 ready/missing/review；覆盖不足仍保持 critical/review 门禁，不会误解锁生产 |
| OpenCAMLib 一键真实候选覆盖率摘要 | 已新增 | `opencamlib-real-candidate-run.json`、Linux `native-cam-real-output-check.sh`、Native CAM ZIP 回填和 readiness 现在都会保留 `contactValidationPathCoverage` 与 `candidatePackageBlockedReason`，把真实候选链路中的 strict contact 覆盖率、候选包阻断原因和生产锁状态串在同一个摘要里 |
| Linux 闭环 evidenceChain 覆盖率追溯 | 已新增 | `native-cam-closed-loop-check.json.evidenceChain.openCamLib` 现在也会携带 `contactPathCoverage`、`candidatePackageLevel`、`candidatePackageReadyForImport`、`candidatePackageBlockedReason` 和 `candidatePackage` 文件摘要；closed-loop 在发现 `neutral-toolpath.json` 时会自动运行 `opencamlib-candidate-package-validate.mjs --root .`；runbook ZIP 回填后，后端 readiness 和前端 Linux OpenCAMLib 摘要会显示候选包预检步骤、证据 JSON 是否回填、覆盖率、哈希绑定或候选包阻断原因 |
| CAMotics 上游候选包绑定 | 已增强 | `camotics-cli-run-package.json` 和 `camotics-result-template.json` 的 `upstreamCamEvidence.files[]` 现在会记录 `opencamlib-candidate-package-validation.json` 与 `opencamlib-candidate-package-bundle.zip`；Linux 侧 validator 会要求回填 `camotics-result.json.inputs.upstreamCamEvidence` 保持这些 SHA-256 不变，避免材料去除结果脱离 OpenCAMLib 候选包预检证据 |
| 离线加工包 OpenCAMLib 证据追溯 | 已新增 | `next-action-checklist.md` 和 `machining-package-index.json` 现在会写入 `linuxOpenCamLibEvidence`，离线显示 Linux/OpenCAMLib 真实候选状态、strict contact pathCoverage、候选包 level、可导入状态和阻断原因；下载安全试雕 ZIP 后也能判断真实 CAM 卡点 |
| OpenCAMLib 真实 contact spike | 已新增 | 新增 `adapters/opencamlib/opencamlib_contact_spike.py` 和 `npm run test:v3:opencamlib-contact-spike`；Linux 上 OpenCAMLib/ocl API 齐全时尝试最小 BatchDropCutter/drop-cutter 调用并写出 `opencamlib-real-contact-spike.json` 与非生产 `neutral-toolpath-spike.json`，本地缺模块时 fail-closed。该 spike 只证明 API 可调用，不是生产候选 |
| OpenCAMLib job 级运行时证据 | 已新增 | `opencamlib_job.py` 生成 kernel plan 和 run template 时，会同步写出 `opencamlib-runtime-probe.json`，并在 `adapter-report.json.metrics.opencamlibRuntimeProbe` 暴露 level、selectedModule、dropCutterReady 和候选符号计数；Linux 回填时可以直接看到真实 CAM 内核环境是否满足 drop-cutter 接入前置条件 |
| OpenCAMLib 旋转包裹采样 | 已新增 | 新增 `npm run test:v3:rotary-heightfield`，直接验证 runner 可按 X 长度 + 360° 旋转角对闭合 STL 做径向采样，生成 `stl-rotary-heightfield-preview` neutral 点；该模式比平面 Z 投影更接近三轴控制器 + Y 旋转夹具，但仍是 preview scaffold，不解锁生产 |
| 4mm 25度平底尖刀预览包络 | 已新增 | 旋转 heightfield 现在按 4mm 刀具默认 2mm 有效半径生成相邻 X/角度外包络，并在 `opencamlib-cutter-envelope-report.json` 中记录 25度角、0.4mm 平底、角度容差、包络抬升和采样数；该证据用于试雕预判，不作为生产级刀补 |
| OpenCAMLib 旋转 neutral handoff | 已新增 | 新增 `npm run test:v3:rotary-neutral-handoff`，由 Orchestrator 直接调用 OpenCAMLib adapter 的旋转 heightfield 自动 runner，验证闭合旋转 STL -> 231 点 neutral -> 32 个往复加工行 -> wrapY NC -> 生产锁定的后端闭环 |
| Readiness CAMotics 证据视图 | 已增强 | 总门禁新增 `readinessCamoticsEvidence`，优先识别最新 job 的 `production-evidence-dossier.json` 中的材料去除证据，避免 job 已完成 CAMotics 回填但 readiness 仍显示缺失 |
| 前端总门禁材料去除证据 | 已增强 | V3 总门禁面板直接显示 `readinessCamoticsEvidence` 的来源、eligible、input/cli/motion/machine 绑定状态和关联 job，便于判断 CAMotics 证据是否真正进入生产证据链 |
| 安全试雕状态拆分 | 已新增 | readiness 新增 `safeTrialReadiness`，单独汇总 latest job 是否可离料空跑、是否可下载试雕 NC、安全试雕包文件、缺口项和下一步动作；前端显示“安全试雕状态”，同时保持 `blockedForProduction=true`，不会误解锁生产 NC |
| 前端残料/过切复核 | 已新增 | V3 聚焦报告区在“仿真证据”下方显示 `残料/过切复核`，区分 `生产残料证据已闭合`、`工程闭环`、`仿真已过，上游待复核` 和 `未闭合`；该显示已纳入 `test:v3:focused-ui` 防回退 |
| 前端 Linux OpenCAMLib 证据摘要 | 已新增 | V3 总门禁的 Linux 证据区现在直接显示 `Linux OpenCAMLib` 行，包含真实候选状态、pathCoverage 覆盖率状态、候选包 level/可导入状态和阻断原因；操作员不需要打开 JSON 也能判断真实 CAM 卡点 |
| V3 目标差距审计 | 已新增 | readiness 现在输出 `goalAudit` 和 `v3-goal-audit.md`，按前端、Orchestrator、CAM 引擎、仿真、后处理、现场证据六层标记 ready/partial/blocked，并列出可继续隐藏或暂缓的非必要功能 |
| Native CAM 同源审计 | 已增强 | 真实输出验收导入时会保存绑定 `v3-external-adapter-validation.json` 的 handoff 审计快照；CAM 引擎层优先使用同源审计，避免后续安全模板验证误判真实 CAM 证据 |
| CAMotics 执行预检 | 已新增 | 每个 V3 job 自动生成 `camotics-execution-preflight.json/md`，检查准备包、预览 NC、结果校验脚本、CAMotics 命令和回填结果状态；前端可手动重新预检，readiness 会显示当前主机是否可直接运行材料去除仿真 |
| OpenCAMLib neutral 绑定 | 已增强 | 外部 adapter 自动生成 `neutral-toolpath.json` 时，会同步生成 `neutral-toolpath-import-validation.json`，绑定 adapter 输出、后处理输入和 sourceSnapshot 哈希 |
| OpenCAMLib contact report 分类 | 已新增 | Neutral API 导入现在会识别嵌入式 `hediao3d.opencamlib-cutter-contact-report.v1`，区分 `missing-contact-report`、`preview-scaffold`、`contact-report-review` 和 `production-candidate`；runner 已自动生成并嵌入 hash-bound preview contact report，但该分类只证明 CAM handoff 候选质量，不能单独解锁生产 NC |
| OpenCAMLib 真实输出契约 | 已新增 | `opencamlib-kernel-plan.json` 现在声明 `productionCandidateCriteria`，`opencamlib-run-template.py` 提供 fail-closed 的 `write_candidate_outputs()` 骨架，固定 neutral-toolpath、contact report、model/plan/neutral 哈希绑定和生产候选条件 |
| OpenCAMLib 输出验收器 | 已新增 | 新增 `opencamlib-contact-output-validate.mjs`，可在 Linux CAM 服务端验证真实 neutral-toolpath/contact report 的 schema、preview/fixture/synthetic 标记、model/plan/neutral 哈希绑定和 production-candidate 条件；该脚本已进入 Native CAM server-package |
| OpenCAMLib 候选包预检 | 已新增 | 新增 `opencamlib-candidate-package-validate.mjs`，可在 Linux CAM 服务端对真实候选输出目录做一键预检，生成 `opencamlib-candidate-package-validation.json` 和轻量 ZIP，先验证 neutral/contact/plan/model 绑定再进入 Native CAM 回填 |
| OpenCAMLib 候选机床适配预检 | 已增强 | `opencamlib-candidate-package-validation.json` 新增 `machineFit`，在 Native CAM 回填前检查 neutral 是否包含 X/Z 与 Y/A 旋转坐标、是否落入夹持/端部保护区、是否超深、旋转覆盖是否不足；critical 会阻断候选包，review 只允许工程复核，不解锁生产 |
| OpenCAMLib 生产差距审查 | 已增强 | `opencamlib-candidate-package-validation.json`、`opencamlib-candidate-package-bundle.zip` 和 `opencamlib-real-candidate-run.json` 新增 `productionGapReview`，按 cam-artifacts、cam-contact、machine-boundary、simulation、residual-gouge、orchestrator-handoff 分组显示 critical、review 和 production-blocker；该摘要已进入 Native CAM 真实输出验收、closed-loop evidenceChain、runbook ZIP 回填、readiness Markdown 和前端“OpenCAMLib差距审查”行，候选包可导入与生产证据缺口分开表达，避免把“可进入 CAMotics/试雕链路”误读成“可生产” |
| Linux 机床适配证据链 | 已增强 | `opencamlib-real-candidate-run.json`、`native-cam-closed-loop-check.json.evidenceChain.openCamLib`、Runbook ZIP 回填摘要和前端总门禁现在会透传 `candidateMachineFit`，操作员可直接看到真实 CAM 候选是否适配三轴控制器 + Y轴旋转夹具 |
| OpenCAMLib 真实 contact 候选门槛 | 已增强 | `opencamlib-contact-output-validate.mjs` 现在要求 production-candidate 同时提供真实 drop-cutter/cutter-contact/waterline 算法族、4mm 25度平底尖刀几何、采样命中率、步距/刀径比例、过切/欠切残料指标；runner 已能提供这些字段的工程估算，但仍因 `experimental-real-api` 和残料/材料去除未验证而保持阻断；弱证据反例在 strict 模式下失败 |
| OpenCAMLib adapter 候选分类 | 已增强 | `opencamlib_job.py` 的 production-candidate 分类现在也要求基础 strict evidence，不再只凭 `quality.productionCandidate=true` 和哈希绑定放行；生成的 `opencamlib-run-template.py` 会固定写入 tool/contactSampling/residualMaterial/tolerances |
| OpenCAMLib neutral API 候选分类 | 已增强 | 后端 `neutral-toolpath` 导入接口也同步要求 strict evidence；直接上传 neutral 时，弱 contact report 即使哈希绑定且声明 `productionCandidate=true`，也只能进入 `contact-report-review` |
| 后处理交接就绪判定 | 已增强 | readiness 的 `postprocessHandoffReadiness` 优先读取最新 job 的 `production-evidence-dossier.json`，只有 neutral/external G-code 源绑定、NC 静态检查和控制器方言检查同时满足时才显示 ready |
| Neutral 机床适配度 | 已新增 | `neutral-toolpath-import-validation.json` 增加 `machineFit`，分析三轴控制器 + Y轴旋转夹具的 X 有效加工区、旋转覆盖、深度范围、夹持/端部保护区风险；前端 V3 摘要和加工包 README 已显示关键结论 |
| 旋转夹具实测标定 | 已增强 | 机床验收必须回填方向、90/180/360 度实测值和反向间隙；证据档案新增 `rotary-calibration-evidence`，未通过时生产审计继续锁定 |
| 离料空跑证据 | 已增强 | `production-evidence-dossier.json` 和 `machining-package-index.json` 已公开 `hediao3d.air-run-evidence.v1`；生产审计必须同时看到旋转标定空跑、整条离料空跑和 `air-run.nc`/`rotary-calibration-airrun.nc` 哈希绑定通过 |
| 生产下载门禁 | 已锁定 | 当前默认只允许 trial-only，正式生产 NC 必须等待真实 CAM/仿真/试雕证据 |
| 生产证据档案门 | 已增强 | 正式生产包下载现在必须通过 `productionReadinessAudit`，同时证明真实外部 CAM、真实材料去除仿真、NC/控制器检查、试雕与机床验收同包绑定 |
| 文档 | 已建立 | 已有 V3 架构、Linux 部署、开源 CAM 接入、试雕执行计划等文档 |

## 3. 已暂时隐藏或降级的功能

这些功能不是删除，而是在当前 V3 聚焦模式下不放在主流程中，避免误以为已经达到成熟 CAM 精度。

| 功能 | 处理方式 | 原因 |
| --- | --- | --- |
| 旧版本地浮雕生成生产下载 | 主流程隐藏 | 只能证明早期 MVP 流程，不适合作为真实佛头 3D 加工依据 |
| 旧版粗加工/精加工/清残下载按钮 | 主流程隐藏 | 当前没有真实外部 CAM 和材料去除仿真支撑，不应给出生产感按钮 |
| G-code 文本预览主入口 | 主流程隐藏 | 当前更重要的是试雕包核验和机床验收，代码级预览先放到工程模式 |
| 热力图主入口 | 主流程隐藏 | 指标仍有价值，但现阶段容易被误解为生产级精度证明 |
| 正式 ZIP/NC/TAP/TXT/CSV 下载 | 默认锁定 | 未通过生产门禁前不能让用户直接上机 |

## 4. 当前验证结果

最近一次验证：

```text
npm run test:v3:small-loop-acceptance: 通过（28/28）
npm run test:v3:readiness-runbook-result-import-api: 通过
npm run test:v3:readiness-api: 通过
npm run test:v3:opencamlib-runner: 通过（PathDropCutter pathGrid/pathCoverage 回归）
npm run test:v3:native-cam-real-output-import-api: 通过（Native CAM 导入/readiness 公开 contact pathCoverage 与 protectedZones 摘要，并覆盖覆盖率不足反例）
npm run test:v3:native-cam-package-self-check: 通过（78 checks；服务包自检锁定 real-output、real-candidate、closed-loop evidenceChain 的 pathCoverage/protectedZones 诊断字段）
npm run test:v3:native-cam-package-self-check: 通过（84 checks；新增 CAMotics material-removal runner 文件/命令/schema/fail-closed 回归）
npm run test:v3:native-cam-api: 通过（Native CAM 服务包 ZIP 暴露 camotics-material-removal-run.mjs，checklist/manifest/ZIP 均包含执行入口）
npm run test:v3:readiness-runbook-result-import-api: 通过（runbook ZIP 回填保留 Linux evidenceChain OpenCAMLib pathCoverage/protectedZones 摘要）
npm run test:v3:focused-ui: 通过（前端总门禁显示 Linux OpenCAMLib pathCoverage 与候选包阻断原因）
npm run test:v3:evidence-closed-loop: 通过（离线 next-action-checklist.md 与 machining-package-index.json 保留 Linux OpenCAMLib pathCoverage、protectedZones、候选包状态和阻断原因，生产包仍锁定）
npm run test:v3:camotics-cli-package-api: 通过（CAMotics Linux 包同时覆盖 OpenCAMLib candidate 输入包下载，ZIP 含 job.json / opencamlib-kernel-plan.json / repaired-model.stl / manifest / README）
npm run test:v3:focused-ui: 通过（前端显示 protectedZones，并暴露“下载OCL输入包”）
npm run test:v3:readiness-runbook-result-import-api: 通过（runbook ZIP 回填仍可公开 Linux evidenceChain）
npm run test:v3: 通过（V3 smoke，trial-only，佛头样件 8979 点）
npm run test:v3:opencamlib-runner: 通过（PathDropCutter runner 端部保护区缩进采样与 protectedZones contact report）
npm run test:v3:opencamlib-contact-validate: 通过（27 checks；新增 protectedZones present/no-violation/sampled-bounds strict 门槛）
npm run test:v3:opencamlib-candidate-package: 通过（candidate package 预检继承 protectedZones strict 门槛）
npm run test:v3:neutral-import: 通过
npm run test:v3:neutral-toolpath-api: 通过
npm run test:v3:production-package-unlock: 通过
npm run test:v3:native-cam-package-self-check: 通过（78 checks；Linux 服务包保留新 OpenCAMLib validator 与 protectedZones evidenceChain）
npm run test:v3: 通过（V3 smoke，trial-only，佛头样件 8979 点）
npm run build: 通过
npm run test:v3:evidence-closed-loop: 通过（闭环 readiness 保留 strict contact pathCoverage）
npm run test:v3:native-cam-package-self-check: 通过（73 checks）
npm run test:v3:buddha-fixture: 通过
npm run test:v3:opencamlib-contact-validate: 通过（24 checks，含弱证据和覆盖率不足反例）
npm run test:v3:rotary-heightfield: 通过
npm run test:v3:evidence-review-package-api: 通过
npm run build: 通过
npm run test:v3: 通过
npm run test:v3:postprocess: 通过
npm run test:v3:readiness-api: 通过
npm run test:v3:machine-acceptance-api: 通过
npm run test:v3:native-cam-api: 通过
npm run test:v3:native-cam-real-output-import-api: 通过
npm run test:v3:camotics-cli-prepare: 通过
npm run test:v3:camotics-cli-package-api: 通过
npm run test:v3:camotics-result-api: 通过
npm run test:v3:evidence-closed-loop: 通过
npm run test:v3:small-loop-acceptance: 通过
npm run test:v3:opencamlib-small-loop: 通过
npm run test:v3:rotary-heightfield: 通过
npm run test:v3:rotary-neutral-handoff: 通过
npm run test:v3:opencamlib-runner: 通过
npm run test:v3:neutral-toolpath-api: 通过
npm run test:v3:neutral-import: 通过
npm run test:v3:closed-neutral-handoff: 通过
浏览器 UI 检查: 通过
```

最新 `npm run test:v3:small-loop-acceptance` 已加入固定佛头样件校验、真实佛头端到端回归、真实浏览器操作员 E2E、真实浏览器原始 STL 导入 E2E、OBJ 后端 CAM 导入校验、制造参数后端门控、Native CAM 诊断包自检、OpenCAMLib runner readiness、OpenCAMLib real contact spike gate 和生产包解锁闭环，覆盖 28 个步骤全部通过时才算 V3 小闭环通过：

```text
frontend-build
buddha-fixture
buddha-e2e
focused-ui-contract
frontend-operator-e2e
frontend-import-model-e2e
v3-smoke
obj-model-import
manufacturing-setup
postprocess-regression
readiness-api
readiness-runbook-result-import
native-cam-api
native-cam-package-self-check
opencamlib-small-loop
opencamlib-contact-validate
opencamlib-probe
opencamlib-adapter-readiness
opencamlib-contact-spike
opencamlib-candidate-package
rotary-heightfield-runner
rotary-neutral-handoff
camotics-cli-package-api
camotics-material-validate
camotics-result-api
machine-acceptance-api
evidence-closed-loop
production-package-unlock
```

本轮确认的关键安全结果：

```text
三轴旋转 Y 后处理: ready
NC 静态分析: ready
控制器方言: ready
OpenCAMLib 旋转 neutral 小闭环: 通过
CAMotics 结果导入与材料去除校验器: 通过
机床验收 API: 通过
证据闭环: 通过
正式生产包: 仍按设计锁定
```

关键结果：

```text
V3 小闭环: ok
交付等级: trial-only
后处理 Profile: desktop-3axis-rotary-y
机床轴输出: X/Y/Z
A 轴输出: 0
空跑 Z: 固定安全高度 22mm
下一步行动清单: next-action-checklist.md 已进入 delivery-manifest、package-integrity 和安全试雕 ZIP；证据回填后会重写并重新哈希
Job级机型边界证据: next-action-checklist.md 显示 Native CAM机型边界 matched/missing/mismatch；machining-package-index.json 写入 nativeCamRealOutputAcceptance.targetMachineBoundaryStatus，下载包离线也能复核真实 CAM 是否适配目标机型
Native CAM 服务端包: server-package.zip 已包含 bootstrap/env/checklist/self-check/diagnostics-bundle/closed-loop-check/real-output-check/opencamlib-runner/probe/contact-spike/contact-validator/manifest
Linux CAM 闭环交接说明: linux-cam-closed-loop-handoff.md 已进入 Native CAM artifact、server-package.zip 和 readiness Native CAM 证据列表
Job级闭环交接说明: linux-cam-closed-loop-handoff.md 已进入每个 V3 job 的交付清单、安全试雕 ZIP、package-integrity 和 machining-package-index readFirst
Native CAM 真实输出回填: Linux 脚本自动生成 native-cam-real-output-bundle.zip；前端/后端支持 ZIP 一次性导入验收报告和外部 adapter 验证报告，自动完成 sourceReportBinding
Native CAM 目标机型边界: native-cam-real-output-bundle.zip 现在包含 target-machine-boundary.json；导入端会公开 targetMachineBoundaryStatus，缺失或不匹配时 ready 降为 review，避免把通用 CAM 输出误当作当前三轴控制器+Y轴旋转夹具可用证据
前端总门禁机型边界提示: 真实 CAM 输出验收摘要会显示 machineBoundary 状态，缺失/不匹配时直接提示三轴控制器 + Y轴旋转夹具 / wrapY / 4mm 25度平底尖刀未被证明
Readiness Runbook 回填: 新增 `POST /api/orchestrator/readiness/runbook-result`，支持 Linux CAM/部署服务器回传 v3-acceptance-runbook-result.json 或结果 ZIP；总门禁会读取该结果但不会仅凭 runbook 解锁生产
Readiness Runbook Linux evidenceChain: ZIP 回填会公开 `linuxEvidence.evidenceChain`，展示 Native CAM ready level、OpenCAMLib real candidate/contact、CAMotics upstreamEvidenceStatus 和 crossChecks；若 evidenceChain blocked，则 runbook public summary 不允许 productionSafe
前端 Runbook 回填入口: V3 总门禁面板新增验收脚本结果 JSON/ZIP 上传控件，导入后刷新 readiness，并显示 identityValid/blockingFailedCount/productionSafe；productionSafe 现在还要求 runbook ZIP 内的 native-cam-closed-loop-check.json 与 camotics-result-local-validation.json 随包证据齐全；前端已显示 Linux 证据状态、必需证据计数、缺失文件和 Linux 证据 JSON 下载入口，避免 raw JSON 通过但缺 ZIP 时被误判
Runbook 结果ZIP: v3-acceptance-runbook.sh 执行后会自动生成 v3-acceptance-runbook-result-bundle.zip，包含结果 JSON、步骤 JSONL、上传说明，并在存在时附带 native-cam-closed-loop-check.json、native-cam-real-output-acceptance.json、camotics-result-local-validation.json 等 Linux CAM 证据，可直接回填到前端总门禁
CAMotics 准备包: ready-for-linux-camotics，包含本地结果校验脚本、Linux 操作清单、Linux 仿真包 ZIP 下载接口和 camotics-result-bundle.zip 自动生成；camotics-preview.nc 已写入 ROTARY_WRAP_AXIS/ROTARY_WRAP_PER_REV_MM/LENGTH_AXIS 头部
CAMotics 结果回填: 支持 result ZIP 一次性导入，保留 imported-camotics-result-bundle.zip 审计原件；evidenceQuality 现在同时校验 inputIdentity、cliRunPackage、motionConsistency 和 machineContext
CAMotics 回填审计: camotics-result-import.json 已记录回填来源、ZIP entries、preview NC SHA-256、camotics-cli-run-package SHA-256、本地校验摘要和安全锁，便于 Linux 服务器结果与当前 job 对账
证据闭环回归: Native CAM 真实输出验收 ready + 当前 job CAMotics material-removal-verified 后，productionEvidenceDossier 会刷新为 trial-evidence-incomplete，生产包仍 423 锁定
小闭环一键验收: `npm run test:v3:small-loop-acceptance` 已纳入 28 个串行步骤，覆盖 frontend build、固定佛头基准、佛头 API E2E、真实浏览器操作员 E2E、真实浏览器原始 STL 导入 E2E、V3 Orchestrator、OBJ 后端 CAM 导入、制造参数后端门控、三轴旋转后处理、readiness、runbook 结果 ZIP 回填与 Linux CAM 随包证据导入、Native CAM 服务包、Native CAM 服务包自检、OpenCAMLib neutral 小闭环、OpenCAMLib runtime probe、OpenCAMLib runner readiness fail-closed 证据、OpenCAMLib real contact spike gate、OpenCAMLib contact 输出验收器、OpenCAMLib 候选包预检、OpenCAMLib 旋转包裹采样、OpenCAMLib 旋转 neutral handoff、CAMotics Linux 包、CAMotics 材料去除验收器/结果回填、机床验收、证据闭环和生产包解锁闭环
Native CAM strict contact 绑定: 真实输出 ZIP 可随包携带 `opencamlib-contact-output-validation.json`，导入端会公开 `contactValidationStatus`；如果 OpenCAMLib production-candidate 缺少 ready strict contact 验证，`native-cam-real-output-acceptance` 自动变为 critical，readiness 和加工包索引都会暴露该阻断项
OpenCAMLib 小闭环: `npm run test:v3:opencamlib-small-loop` 已作为显式入口；该测试证明 OpenCAMLib neutral handoff 能进入 HeDiao3D wrapY 后处理，但仍标记 heightfield preview scaffold，生产 NC 不解锁
OpenCAMLib 旋转包裹采样: `npm run test:v3:rotary-heightfield` 已通过；闭合旋转样件按 X + 旋转角采样，证明 runner 能按旋转夹具逻辑采样模型外表面，而不是仅做平面浮雕投影
4mm 25度平底尖刀预览包络: `npm run test:v3:rotary-heightfield` 已增强验证；闭合旋转样件输出 231 个 neutral 点、0 个未命中、覆盖 33 个角度，4mm 刀具有效半径 2mm，最大包络抬升 0.75mm；`opencamlib-cutter-envelope-report.json` 已绑定 tool angle/flat tip/radius 和 rotary envelope 指标
Neutral 连续排序: `npm run test:v3:neutral-toolpath-api` 已增强验证；故意乱序的 neutral 点会在后处理前转成 `rotary-wrap-boustrophedon` 往复行顺序，生成 `toolpath-sequencing-report.json`，并进入 delivery-manifest、package-integrity 和 job summary；生产包仍不解锁
OpenCAMLib contact report 分类: `npm run test:v3:neutral-toolpath-api` 和 `npm run test:v3:neutral-import` 已验证普通 neutral 显示 missing-contact-report，未绑定 report 显示 contact-report-review，绑定 `hediao3d.opencamlib-cutter-contact-report.v1` 的 neutral 显示 production-candidate；`npm run test:v3:rotary-heightfield` 和 `npm run test:v3:rotary-neutral-handoff` 已验证 runner 自动写入 preview-scaffold contact report 并保持生产锁定
OpenCAMLib 真实输出契约: `npm run test:v3:adapters` 已验证 `opencamlib-kernel-plan.json` 声明 `opencamlib-cutter-contact-report.json` 和 productionCandidateCriteria，`opencamlib-run-template.py` 包含 `write_candidate_outputs()`、`hediao3d.opencamlib-cutter-contact-report.v1`、`neutralToolpathWithoutContactReportSha256` 和 fail-closed 提示
OpenCAMLib 输出验收器: `npm run test:v3:opencamlib-contact-validate` 已验证合格 contact 输出为 ready，preview scaffold 在 strict 模式下为 critical；`npm run test:v3:native-cam-api` 已验证该验收器进入 Native CAM server-package.zip
OpenCAMLib 真实 contact 候选门槛: `npm run test:v3:opencamlib-contact-validate` 已增强为 24 项检查；真实 production-candidate 必须同时通过算法族、刀具几何、采样命中率、stepToCutterRatio、X/cross pathCoverage >= 98%、过切/欠切指标和 model/plan/neutral 哈希绑定，仅写 `productionCandidate=true`、缺少残料指标或覆盖率不足都会失败
OpenCAMLib 旋转 neutral handoff: `npm run test:v3:rotary-neutral-handoff` 已通过；Orchestrator 可通过 `HEDIAO3D_OPENCAMLIB_ROTARY_HEIGHTFIELD_PREVIEW=true` 调用旋转 heightfield 自动 runner，闭合旋转样件生成 231 点 neutral、32 个加工行、Y 旋转 wrap NC，仍只允许 air-run，不允许 trial/production
Readiness CAMotics 证据视图: readiness 会公开 readinessCamoticsEvidence；当最新 job 的 CAMotics 证据比历史全局 contract 更新且已达标时，总门禁使用 latest-job-evidence-dossier 作为材料去除证据来源
前端总门禁材料去除证据: V3 UI 已显示 readinessCamoticsEvidence 来源、eligible、input/cli/motion/machine 和 job 绑定状态；加工包 README 也同步显示机床上下文，避免下载包脱离网页后误判
V3 目标差距审计: readiness 增加 goalAudit，自动按六层架构输出差距、缺失证据、下一步动作和应继续隐藏的功能；前端总门禁显示目标差距摘要，`v3-goal-audit.md` 可作为下一轮开发/验收清单
Native CAM 同源审计: 真实输出 ZIP/JSON 回填后会保留绑定源报告的 handoffClassificationAudit；CAM 引擎层用 `bound-native-source-report` 作为优先证据，最新安全模板 adapter 报告仍保留但不再误伤同源真实输出验收
CAMotics 执行预检: 新增 `POST /api/orchestrator/jobs/:id/camotics-execution-preflight`；预检报告进入 delivery-manifest、package-integrity、安全试雕包、Linux 仿真包和加工包索引，用于说明当前主机能否直接跑 CAMotics，仍不解锁生产
CAMotics 上游 CAM 证据绑定: CAMotics 准备包、结果模板、Linux validator 和后端回填均已识别 `upstreamCamEvidence`；当准备包捕获到 Native CAM/OpenCAMLib 证据文件时，回填结果必须匹配这些 SHA-256，否则材料去除证据不能进入 productionEvidenceEligible
CAMotics 上游机床适配门禁: CAMotics 准备包和材料去除 validator 已绑定 candidateMachineFit；如果 OpenCAMLib 候选包机床适配为 critical，材料去除结果即使哈希匹配也不能成为生产证据
前端 V3 主线收敛: CAM 页固定显示“导入/生成3D模型、生成安全试雕数据、下载安全试雕包、生成/下载 CAMotics Linux 仿真包”，旧版直接 NC 下载、正式生产包和高级 Adapter 面板在试雕聚焦模式下继续收起
Linux CAM 服务闭环入口: 试雕聚焦模式下开放“生成服务包”、服务端 ZIP 下载和真实输出验收回填，方便把 Linux CAM 服务器验证结果接回当前证据链；正式生产 NC 仍保持锁定
Native CAM closed-loop evidenceChain: `native-cam-closed-loop-check.json` 现在包含 `hediao3d.native-cam-linux-evidence-chain.v1`，可离线查看 Native CAM real output、OpenCAMLib real candidate/contact、CAMotics local validation、result bundle 和 upstream 绑定状态；`npm run test:v3:native-cam-api` 已锁定该脚本契约
Runbook evidenceChain 后端接入: `extractV3RunbookLinuxEvidence()` 已把 Linux closed-loop evidenceChain 压缩成公开摘要并进入 latest runbook result；`npm run test:v3:readiness-runbook-result-import-api` 已验证 CAMotics upstream 绑定为 matched 时可被 readiness 摘要读取
证据审查包: 新增 `/api/orchestrator/jobs/:id/evidence-review-package`，集中打包 production gate、package integrity、后处理追溯、CAMotics、试雕/机床验收等证据和缺失项清单；该包仅用于审查，不作为上机包
证据审查包回归: 新增 `npm run test:v3:evidence-review-package-api`，真实创建 V3 job、下载并解析 evidence-review ZIP、校验不含上机 NC、生产包仍 423 锁定
OpenCAMLib closed neutral handoff: trial-only，neutral/sourceSnapshot 已绑定，生产仍锁定
后处理交接就绪: readiness 现在优先使用 latest-job-evidence-dossier；仅当最新 job 的 neutral/external G-code 源绑定、NC 静态分析和控制器方言检查通过时，才把 postprocess handoff 标记为 ready
Neutral 机床适配度: neutral-toolpath-import-validation.json 已输出 machineFit，覆盖 X=长度方向、Y=旋转夹具、Z=刀深/安全高度、端部保护区和旋转角覆盖率；V3 页面与加工包 README 已显示适配等级、覆盖率、端部风险和超深风险
OpenCAMLib 候选包机床适配前置: opencamlib-candidate-package-validation.json 已输出 hediao3d.opencamlib-candidate-machine-fit-preflight.v1；真实 CAM 输出在 Native CAM 回填前就会检查缺 Y/A 旋转坐标、端部保护区、深度上限和旋转覆盖风险，critical 时 fail-closed
Linux 机床适配证据链: Runbook ZIP 回填后，v3-acceptance-runbook-linux-evidence.json 和前端总门禁会显示 candidateMachineFit，包括旋转轴、每圈距离、旋转覆盖、端部/超深/缺旋转风险计数
旋转夹具实测标定: machine-acceptance-record.json 新增 rotaryCalibration，按方向、90/180/360 度误差和 backlash 阈值计算 pass/review/failed；production-evidence-dossier 和 productionReadinessAudit 已把该项作为现场放行证据之一
离料空跑独立证据: production-evidence-dossier/machining-package-index 现在写入 hediao3d.air-run-evidence.v1；productionReadinessAudit 要求旋转标定空跑、整条离料空跑、操作员确认和当前加工包哈希全部通过
Production readiness audit: 已进入 readiness/evidence dossier；未满足现场证据同包绑定时生产包继续 423 锁定
```

这说明当前系统可以继续做安全试雕包闭环，但还不能宣称“下载后直接雕刻成品”。

### 4.1 Linux CAM 服务器实测状态（2026-07-18）

服务器资源已升级并确认：

```text
CPU: 4 核
内存: 7.7GiB
系统: Ubuntu 24.04.3 LTS
项目路径: /opt/hediao3d/HeDiao3D
分支: HeDiao3D_V3
最新提交: 43a6097
```

服务器端已完成的实测：

```text
Node/npm/git: 可用
Blender: 4.0.2 已安装
OpenCAMLib: python3 import opencamlib 通过
FreeCAD snap: freecad.cmd --version 通过
CAMotics: 官方 1.2.0 deb 已尝试安装，但运行失败
npm run test:v3:native-cam: partial，ready=1/4
npm run test:v3:opencamlib-small-loop: 通过（Linux 服务器复测）
npm run test:v3:native-cam-package-self-check: 通过，readyLevel=ready，73 checks
node opencamlib-real-candidate-run.mjs .: 待在 Linux server-package 目录用真实 job/plan/model 执行
npm run test:v3:rotary-neutral-handoff: 通过，231 neutral 点 / 32 行 / airRun=true / production=false
npm run test:v3:opencamlib-contact-validate: 通过，readyLevel=ready
服务器工作区: clean
```

当前真实阻断项：

```text
FreeCAD CAM:
  freecad.cmd 命令已识别，但 FreeCAD Path/CAM Python 探测失败。
  实测原因：snap 内 CAM/Path import 触发 PySide/Qt 私有符号冲突。

BlenderCAM/FabexCNC:
  Blender 已安装，但未检测到 cam/blendercam/fabex 插件模块。

CAMotics:
  /usr/bin/camotics 已出现，但 --version 运行失败。
  实测缺失：libv8.so.3.14.5，另有 libssl.so.1.1、libcrypto.so.1.1、libQt5WebSockets.so.5 风险。
  结论：Ubuntu 24.04 上官方旧 deb 不能作为当前生产仿真环境。

apt/dpkg:
  系统存在 linux 7.0.0-28 + NVIDIA DKMS 配置失败遗留问题。
  CAMotics 已被 unpack/configure，但 dpkg audit 仍显示内核相关包半配置，需要服务器系统层面单独修复。
```

本次代码修正：

```text
1. Native CAM 检测已支持 FreeCAD snap 命令 freecad.cmd / /snap/bin/freecad.cmd。
2. Native CAM 检测改为必须 --version 退出码为 0 才算可用。
3. CAMotics 这类“命令存在但依赖坏了”的情况不再误判 ready。
4. readiness 报告会记录 failedCommandProbes，显示真实失败原因。
5. OpenCAMLib 小闭环被提升为独立 npm 入口，并纳入一键小闭环验收。
6. OpenCAMLib runner 新增显式旋转 heightfield 预览模式，可按 X + 旋转角径向采样 STL，为后续替换成真实 drop-cutter/contact 算法提供更贴近目标机型的数据结构。
7. 旋转 heightfield 预览已加入 4mm 25度平底尖刀的有效半径包络指标，能在报告中暴露刀具半径导致的欠切/包络抬升趋势，但仍不是最终生产刀补。
8. Neutral 后处理前新增旋转行往复排序报告，减少外部中立点直出造成的长距离回跳，为真实三轴控制器 + Y 旋转夹具试雕提供更接近机床加工习惯的 NC 顺序证据。
9. OpenCAMLib adapter 新增显式旋转 heightfield 预览开关，后端 Orchestrator 已能端到端验证旋转 STL -> neutral -> wrapY NC；下一步应把该 preview runner 替换为真实 OpenCAMLib cutter-contact/drop-cutter 计算。
10. Native CAM 服务包新增 `native-cam-diagnostics-bundle.mjs`，Linux 侧可先生成诊断 ZIP 回传环境证据，不需要等待真实 CAM 输出；该脚本和 ZIP 生成已纳入服务包自检和 28/28 小闭环。
11. Native CAM 服务包新增 `opencamlib-contact-spike.py`，Linux 侧在 probe 发现完整 API 绑定后可尝试最小真实 BatchDropCutter/drop-cutter 调用；当前 Windows 缺模块时 fail-closed，仍会输出 blocked 报告供诊断包回传。
12. Native CAM 服务包已随包携带 `opencamlib-runner.py`，Linux 侧不需要完整源码树也能运行同源 runner/readiness handoff；服务包自检已验证 runner readiness schema 和生产锁边界。
13. OpenCAMLib runner 新增 `opencamlib-runner-readiness.json`，真实 runner 未实现或 Linux 绑定不足时也能给出机器可读的 blocker、probe 状态、contact spike 状态、模型解析和目标刀具/Y旋转映射证据。
14. 浏览器 E2E 改为用 Chrome DevTools Protocol 新建明确的应用标签，避免误连初始空白页/扩展页导致“页面就绪”偶发超时。
```

下一步技术决策：

```text
短期:
  继续以 OpenCAMLib 作为服务器上已就绪的真实几何内核基础。
  每次 Linux 部署后先运行 npm run test:v3:native-cam，再运行 npm run test:v3:opencamlib-small-loop。
  CAMotics 暂不作为 Ubuntu 24.04 本机必备项，先保留“外部结果回填/可插拔仿真”链路。

中期:
  评估两条路线：
  A. 使用容器或旧版 Ubuntu 运行 CAMotics，输出 camotics-result-bundle.zip 回填。
  B. 替换为更现代的 G-code 仿真/材料去除引擎，保持 HeDiao3D 的结果导入 schema 不变。

FreeCAD:
  优先寻找非 snap 的可脚本化 FreeCAD/FreeCADCmd 环境，或把 FreeCAD 降级为规则三轴参考 CAM。

BlenderCAM/FabexCNC:
  后续再安装并验证插件；在插件稳定前，不把它作为 P0 生产路径。
```

## 5. 还未完成的关键任务

### P0：必须完成，才可能进入真实试雕

1. 将 OpenCAMLib 当前 `experimental-real-api` 输出升级为可验收的 production-candidate 证据，核心是补齐残余材料/接触质量/机床证据，而不是再扩大前端功能。
2. 将 Native CAM contactValidation 从 `critical` 推进到 `ready`，包括 `postprocessEligible=true`、`productionCandidate=true`、残余 gouge/undercut measured-or-validated。
3. 将已回填的 `native-cam-real-output-bundle.zip` 与 `camotics-result-bundle.zip` 纳入同一个 readiness 闭环，继续生成最新 readiness 报告。
4. 用固定佛头回归样件完成：
   - Orchestrator job
   - 安全试雕包下载
   - 离料空跑
   - 软料试雕
   - 反馈和照片记录
   - 机床验收记录
5. 校准 Y 旋转夹具：
   - Y 行程与实际旋转角度换算
   - 方向正反
   - 圆周展开比例
   - 两端不可加工区域
6. 根据实机控制器确认最终 NC 方言：
   - 是否支持 `G90/G21/G0/G1/F/S/M3/M5/M30`
   - Y 轴旋转每圈等效行程
   - 安全 Z、最大 Z 下刀深度、最高进给
   - 主轴启停和暂停指令格式

### P1：试雕后提升精度

1. 将 OpenCAMLib 旋转 heightfield 预览替换/升级为真实 drop-cutter / cutter-contact / waterline，并接入真实佛头曲面。
2. 三轴+旋转夹具专用粗加工、精加工、清残策略。
3. 将当前 4mm 25度平底尖刀预览包络升级为真实 V 刀/平底尖刀几何模型，包括刀尖平底、侧刃角、有效伸出和材料接触校准。
4. 端部保护区、夹持区、不可达区自动避让。
5. 试雕误差反补偿：根据实物照片/测量数据修正深度和包络。

### P2：产品化体验

1. 工程模式与操作员模式彻底分离。
2. 机床 Profile、刀具库、材料库可视化维护。
3. 加工包版本管理和试雕案例库。
4. 支持更多控制器方言。
5. 支持公网部署的账号、权限、任务隔离和文件清理策略。

## 6. 下一步建议

下一步优先做“OpenCAMLib 真实 cutter-contact/drop-cutter + CAMotics/等效材料去除结果回填”的服务器闭环，而不是继续扩展前端按钮。每次生成 readiness 后先看 `goalAudit`/`v3-goal-audit.md`，优先补 blocked 层的真实证据。

截至 2026-07-22 15:05，固定佛头在服务器上已经完成 148 万级 OpenCAMLib neutral/contact 工程证据生成：

```text
neutral-toolpath points=1,487,639
machineFit=ok
rotaryCoverageRatio=0.9944444444
depthMax=0.6096mm
native-cam-real-output-bundle.zip=已生成并回填
productionAllowed=false
```

剩余 P0 不再是“能否跑出佛头刀位点”，而是：

```text
1. 材料去除仿真包已生成并回填，当前 `simulationEvidence.level=material-removal-verified`。
2. OpenCAMLib 输出仍标记为 experimental-real-api，Native CAM contactValidation 仍为 critical，尚不能作为 production-candidate。
3. 残余 gouge/undercut 仍是工程估算，尚未达到 measured-or-validated；系统已新增 residualClosureReview 将其明确标为工程复核项，避免把材料去除仿真通过误解为生产残料证据闭合。
4. 真实机床离料空跑、软料试雕、机床验收尚未回填。

新增 2026-07-22 15:30 佛头材料去除闭环结果：

```text
camotics-result-bundle.zip=已生成并回填
simulator=HeDiao3D internal swept-envelope material-removal simulator
synthetic=false
motionLineCount=8983
materialRemovedMm3=29422.530843
simulationEvidence.level=material-removal-verified
productionGate.level=trial-only
productionAllowed=false

新增 2026-07-22 17:17 佛头 Linux CAM 整单回填结果：

```text
jobId=970989c4-c357-4360-94e8-931fa3e55bab
linux-cam-job-local-validation.level=ready-for-v3-upload
native-cam-real-output-bundle.zip=已生成并上传
camotics-result-bundle.zip=已生成并上传
readiness.safeTrialReadiness.status=safe-trial-ready
productionGapReview.level=blocked
productionGapReview.criticalCount=3
productionGapReview.productionBlockerCount=1
productionAllowed=false
```

本次实测证明：Linux 整单包下载、Native CAM/OpenCAMLib 运行、CAMotics/等效材料去除、证据上传、readiness 复核链路已经贯通。当前真实阻断不在前端流程，而在 OpenCAMLib 仍为 `experimental-real-api`，strict contact validation 为 critical，残料/过切证据未达到 measured-or-validated，且真实机床验收尚未回填。
```
```

推荐顺序：

1. 部署 Linux CAM 环境。
2. 跑 `npm run test:v3:native-cam`、`npm run test:v3:opencamlib-probe`、`npm run test:v3:opencamlib-contact-spike` 和 adapter 验收。
3. 选一个佛头 STL/GLB 作为固定样件。
4. 用 OpenCAMLib 输出真实中立刀位点和 cutter-contact 报告；FreeCAD 暂作为 P1 参考 CAM。
5. 先查看/运行 `camotics-execution-preflight.json`，确认当前主机可跑 CAMotics；若不可跑，下载仿真包到可运行 CAMotics 的容器/旧系统，或使用等效材料去除仿真器执行。
6. 回填 CAMotics/等效材料去除真实仿真结果。
7. 生成安全试雕包。
8. 离料空跑和软料试雕。

只有这些证据全部绑定到同一个 job 和同一套下载文件哈希后，`productionReadinessAudit.allowProductionPackage=true`，才允许正式生产 NC 下载。
