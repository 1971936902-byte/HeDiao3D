# 佛头 3D 数据测试报告

测试时间：2026-07-16

## 测试对象

- 原始 Meshy 数据：`public/meshy-results/material01-meshy.glb`、`public/meshy-results/material01-meshy.stl`
- Meshy 修复数据：`public/meshy-results/repair-019f66a1-d65f-7075-965a-94acb105dd65.stl`
- Meshy 重网格数据：`public/meshy-results/remesh-019f66b2-b86b-754d-ba90-183ed2ed8175.glb`、`public/meshy-results/remesh-019f66b2-b86b-754d-ba90-183ed2ed8175.stl`

## 测试参数

- 核胚尺寸：长 38 mm，直径 15 mm
- 雕刻角度：360°
- 刀具：0.6 mm 球刀
- X 步距：0.12 mm
- A 步距：1.2°
- 左右夹持：各 2 mm
- 端部过渡：1.2 mm
- 进给：180 mm/min
- 主轴：12000 rpm
- 安全高度：22 mm

## 测试结果

| 数据 | Mesh 体检 | 三角面 | 边界边 | 非流形边 | 刀路点 | 未命中采样点 | 估算加工时长 |
| --- | --- | ---: | ---: | ---: | ---: | ---: | ---: |
| `material01-meshy.stl` | ready / 100.0 | 423884 | 0 | 0 | 85785 | 1134 | 82.1 min |
| `repair-019f66a1...stl` | ready / 100.0 | 423884 | 0 | 0 | 85785 | 1134 | 82.1 min |
| `remesh-019f66b2...stl` | review / 91.9 | 83194 | 10 | 10 | 85785 | 1082 | 81.4 min |

## 页面验证

- AI Mesh 测试结果可以正常加载，右侧显示为 `Meshy AI Mesh / GLB-STL真实网格`。
- Mesh 体检可以正常完成，并能识别长轴为 Z。
- CAM 可以生成 360° 刀路，范围为 `X -17.0~17.0 / A -180~180`。
- 右侧 `模拟雕刻`、`热力图`、`报告` 页面均可打开。
- 热力图显示最差区域约为 `X -2.7mm / A 300deg`，该区域贴合率为 0%，未命中 `89/89`。

## 结论

当前佛头 3D 数据可以完成从 Mesh 载入、体检、刀路生成、模拟雕刻、热力图诊断到报告展示的完整流程。

从测试结果看，`material01-meshy.stl` 和 `repair` 版本的 Mesh 封闭性更好，适合作为当前默认测试数据；`remesh` 版本虽然未命中点从 1134 降到 1082，但体检出现少量边界边和非流形边，顶部、主体、右端仍有局部风险，不建议直接替代默认模型。

当前主要风险不是文件无法生成刀路，而是 Mesh 表面采样仍有约 1.3% 未命中点，且安全高度校验提示 `22.0mm` 接近最高刀位。实机前应先提高安全高度、做离料空跑，并继续优化 Mesh 采样策略。

## 后续优化建议

1. 增加“载入原始 / 修复 / 重网格佛头”的测试数据选择，避免测试入口只加载 `material01`。
2. 对 Mesh CAM 采样增加双向射线或最近点兜底，减少局部角度下的未命中。
3. 对热力图最差区域输出对应的 X/A 坐标和模型位置，方便定位顶部或侧面缺损。
4. 将安全高度默认值提高到 `最高刀位 + 2~3mm`，避免刚好贴近最高 Z。
5. 对 `remesh` 结果增加二次体检门槛：若边界边或非流形边高于原始模型，则不自动替换为重网格结果。

## 2026-07-16 本地浏览器复测：素材01多图重新生成

复测入口：本地浏览器 `http://127.0.0.1:5192/`

输入素材：页面“载入素材01”加载的 4 张多角度佛头图。

素材质量提示：

- `素材01/0.png`：60.8 分，建议重拍
- `素材01/1.png`：63.6 分，建议重拍
- `素材01/2.png`：60.5 分，建议重拍
- `素材01/3.png`：60.7 分，建议重拍

Meshy 生成结果：

- 生成耗时：约 161 秒
- GLB：`public/meshy-results/material01-meshy.glb`
- STL：`public/meshy-results/material01-meshy.stl`
- STL 大小：约 22.6 MB
- GLB 大小：约 15.7 MB

Mesh 体检：

| 数据 | Mesh 体检 | 三角面 | 边界边 | 非流形边 | 退化面 | 长轴 |
| --- | --- | ---: | ---: | ---: | ---: | --- |
| `019f6a05-c78b...stl` | ready / 100.0 | 451536 | 0 | 0 | 0 | Z |

CAM 与诊断：

- 刀路范围：`X -17.0~17.0 / A -180~180`
- 刀路采样点：85785
- 未命中采样点：602
- 包络贴合率：约 99.3%
- 连续贴合：99.9%
- 热力图风险格：4/36
- 严重风险格：0
- 最差区域：`X -1.4mm / A 320deg`，贴合 88.7%，未贴合 176/1560
- 安全校验：仍存在 `安全高度 22.0mm 低于或接近最高刀位 22.0mm` 阻断项

复测结论：

重新用素材01多图调用 Meshy 生成的 3D Mesh 质量优于之前的 `material01-meshy.stl`。未命中点从之前测试的 1134 降至 602，且热力图严重风险格为 0，说明本次 Mesh 对 CAM 包络更友好。当前主要剩余问题是素材本身质量偏低，以及默认安全高度过低；实机前仍建议提高安全高度并做离料空跑。

## 2026-07-18 V3 固定佛头回归样件

为避免后续 V3 小闭环反复使用不同模型造成结果漂移，已将素材01多图重新生成的 Meshy 结果固定为 V3 默认佛头回归样件：

- 基准清单：`public/v3-fixtures/buddha-baseline.json`
- 默认 GLB：`public/meshy-results/material01-meshy.glb`
- 默认 STL：`public/meshy-results/material01-meshy.stl`
- 校验命令：`npm run test:v3:buddha-fixture`

基准锁定内容：

- GLB/STL SHA-256 和文件大小。
- STL 三角面数、包围盒、长轴 `Z`。
- 6 张素材01源图 SHA-256。
- 目标机型：三轴控制器 + Y轴旋转夹具。
- 目标刀具：4mm 25度平底尖刀。
- 核胚：38mm 长、15mm 直径、左右夹持各 2mm。

当前 V3 smoke、后处理、CAMotics、证据闭环等默认测试已切换到该佛头回归样件。该样件只证明回归输入稳定，不解锁生产 NC；正式上机仍需要真实 CAM、真实材料去除仿真、离料空跑、软料试雕和机床验收。
