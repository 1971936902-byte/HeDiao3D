# 核雕3D CAM MVP 验收清单

## 验收目标

1. 用户可以传入一张或多张核雕图片。
2. 点击“3D生成”后生成完整 360 度核胚立体视图。
3. 3D 视图支持旋转、缩放、平移。
4. 支持 3D/深度微调。
5. 点击“生成刀路”后生成主流刀路文件。
6. 支持下载模型和刀路文件。

## 推荐验收流程

### 1. 启动项目

```bash
npm install
npm run dev
```

打开终端提示的本地地址，例如：

```text
http://127.0.0.1:5174/
```

### 2. 图片输入

方式一：点击“上传一张或多张核雕图片”，选择本地图片。

方式二：点击“载入示例图案”，系统会载入两张内置深度图案，用于快速演示多图流程。

方式三：点击“载入素材01”，系统会载入项目目录 `public/test-assets/01/` 中的 6 张真实核雕素材。

验收结果：

1. 左侧出现图片缩略图。
2. 多张图片时，“生成模式”可选“当前选中图片”、“多图平均融合”或“本地360°环绕浮雕（非AI Mesh）”。
3. 使用“载入素材01”时，应出现 `素材01/0.png` 到 `素材01/5.png` 共 6 张缩略图。

### 3. 3D生成

点击“3D生成”。

验收结果：

1. 右上角状态显示“已生成3D浮雕”。
2. 右侧显示完整核胚 360 度 3D 模型。
3. 鼠标拖动可旋转，滚轮可缩放，右键可平移。
4. 选择“本地360°环绕浮雕（非AI Mesh）”时，雕刻角会切换为 360°，多张图片会被用于周向环绕网格。

### 4. 3D微调

调整以下参数：

1. 核长
2. 最大直径
3. 浮雕深度
4. 包覆角度
5. 图像对比
6. 平滑次数
7. 反转深浅
8. 显示网格

验收结果：

1. 3D 模型实时变化。
2. 底部指标同步显示当前网格、最大深度、雕刻角。

### 5. 局部修模

在“局部修模”面板中选择：

1. 加深
2. 减浅
3. 平滑

调节笔刷和强度后，在深度图画布上拖动。

验收结果：

1. 深度图发生局部变化。
2. 右侧 3D 模型同步更新。
3. 已生成的旧刀路会被清空，避免误用过期刀路。

### 6. 生成刀路

设置刀路参数：

1. 刀具直径
2. X步距
3. A步距
4. 进给
5. 主轴
6. 后处理：通用四轴、维宏风格、新代风格

点击“生成刀路”。

验收结果：

1. 3D 视图上出现红色刀路轨迹。
2. 底部显示刀路点数、估算时间、后处理、X/A 范围、基础校验。
3. 出现下载按钮。

### 7. 文件导出

可下载文件：

1. `STL`：当前 3D 模型文件。
2. `NC`：通用 G-code。
3. `TAP`：常见机床刀路扩展名。
4. `TXT`：文本 G-code。
5. `CSV`：刀路点数据。

验收结果：

1. 点击各下载按钮后浏览器触发下载。
2. G-code 文件包含 `G21`、`G90`、`G94`、`M3`、`G1 X... A... Z...` 等基础指令。

## 已知边界

当前版本是前端 MVP，用于验证“图片到 3D 预览再到基础四轴刀路”的产品链路。真实量产上机前仍需补：

1. 目标雕刻机实机后处理器。
2. 夹具、限位、刀具有效刃长碰撞校验。
3. 实物试切误差补偿。
4. 后端高精度 CAM 服务。

