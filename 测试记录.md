# HeDiao3D 全功能测试记录

测试日期：2026-07-17
测试项目：核雕 3D 图生成与雕刻机数据生成系统
测试素材：`C:\Users\19719\Desktop\AI创业\雕刻图生成\3D素材\meigui.glb`
测试说明：按要求未重新使用图片生成 3D 图，本轮测试直接加载 `meigui.glb` 进行导入、体检、刀路、仿真、导出和反向预览链路验证。

## 测试环境

| 项目 | 结果 |
| --- | --- |
| 工作目录 | `C:\Users\19719\Documents\3D核雕图` |
| 前端地址 | `http://127.0.0.1:5193/` |
| 后端 API | `http://127.0.0.1:8787/` |
| API 健康检查 | 通过，`/api/health` 返回 `ok: true` |
| 测试素材大小 | 55,107,000 bytes |
| Git 分支 | `HeDiao3D_V2` |

## 本轮修复项

| 修复项 | 结果 |
| --- | --- |
| 原始 GLB 导入后 Mesh 体检失败 | 已修复，`/api/mesh/analyze` 支持 `/imported-models/` 和 GLB/GLTF/STL |
| 下载 GLB / AI STL 点击无反应 | 已修复，改为前端主动 `fetch -> Blob -> downloadBlob` |
| 加工包说明、空跑 NC、安全报告下载无反应 | 已修复，`downloadText()` 与 `downloadBlob()` 均改为插入 DOM 后触发下载 |
| 精加工预览被拉成长条 | 已修复，服务端保留/降采样 `previewPoints`，避免精加工回退到另一套仿真算法 |

## 自动化测试结果

### 1. 构建测试

命令：

```powershell
npm.cmd run build
```

结果：通过。

说明：Vite 构建成功。存在 chunk 超过 500KB 的提示，仅为体积优化建议，不影响功能。

### 2. GLB 导入测试

接口：`POST /api/mesh/import`

| 项目 | 结果 |
| --- | --- |
| 状态码 | 200 |
| 输入文件 | `meigui.glb` |
| 返回格式 | `glb` |
| 本地缓存地址 | `/imported-models/1784218857840-f98c78d2e88cd8-meigui.glb` |
| 结论 | 通过 |

### 3. Mesh 质量体检

接口：`POST /api/mesh/analyze`

| 指标 | 结果 |
| --- | --- |
| 面数 | 656,270 triangles |
| 顶点数 | 1,968,810 |
| 尺寸 | X 0.1656 / Y 0.1200 / Z 0.1059 |
| 长轴识别 | X |
| 边界边 | 0，封闭性通过 |
| 非流形边 | 2,367，warning |
| 退化面 | 648,504，critical |
| 体检结论 | 系统功能通过；素材本身建议修复/重建后再正式加工 |

说明：体检接口可正常解析 GLB 并返回报告。`meigui.glb` 本身存在大量退化面，所以质量评分为 `repair`，这是素材质量问题，不是系统功能失败。

### 4. 四轴粗加工刀路

接口：`POST /api/cam/mesh-toolpath`

| 指标 | 结果 |
| --- | --- |
| CAM 模式 | 四轴 X/A/Z |
| 后处理 | 通用四轴 G-code |
| 刀路点 | 17,818 |
| 预览点 | 17,818 |
| G-code 行数 | 17,838 |
| 估算时间 | 54.21 min |
| X 范围 | -18.7 到 18.7 |
| A 范围 | -180 到 180 |
| Z 范围 | 0.3423 到 22 |
| 结论 | 通过 |

### 5. 四轴精加工刀路

接口：`POST /api/cam/mesh-toolpath`

| 指标 | 结果 |
| --- | --- |
| CAM 模式 | 四轴 X/A/Z |
| 后处理 | 通用四轴 G-code |
| 刀路点 | 209,148 |
| 预览点 | 209,148 |
| G-code 行数 | 209,168 |
| 估算时间 | 335.73 min |
| X 范围 | -18.7 到 18.7 |
| A 范围 | -180 到 180 |
| Z 范围 | 0.3169 到 22 |
| 结论 | 通过 |

说明：精加工点数较大，预览点已完整返回，不再出现预览模型被异常拉长的问题。

### 6. 三轴控制器 + Y 轴旋转夹具刀路

接口：`POST /api/cam/mesh-toolpath`

| 指标 | 结果 |
| --- | --- |
| CAM 模式 | 旋转包裹 X/Z + Y 轴夹具 |
| 后处理 | Y轴旋转包裹 G-code |
| 刀路点 | 17,818 |
| 预览点 | 17,818 |
| G-code 行数 | 17,839 |
| 估算时间 | 54.21 min |
| G-code 标识 | `ROTARY_WRAP_AXIS=Y ROTARY_WRAP_PER_REV_MM=100.000000 LENGTH_AXIS=X` |
| 结论 | 通过 |

说明：适配“三轴控制器 + 旋转轴夹具”场景，X 为长度方向，Y 为夹具线性映射旋转，Z 为径向刀位。

### 7. 三轴顶面投影刀路

接口：`POST /api/cam/mesh-toolpath`

| 指标 | 结果 |
| --- | --- |
| CAM 模式 | 三轴 X/Y/Z |
| 后处理 | 通用三轴 G-code |
| 刀路点 | 5,760 |
| 预览点 | 5,760 |
| G-code 行数 | 5,779 |
| 估算时间 | 21.69 min |
| X 范围 | -19 到 19 |
| Y 范围 | -7.5 到 7.5 |
| Z 范围 | -0.8289 到 22 |
| 结论 | 通过 |

说明：三轴模式按顶面投影生成平面浮雕刀路，不等同于完整 360 度立体核雕。

### 8. NC/G-code 反向导入解析

测试对象：`src/gcodeImport.ts`

| 项目 | 结果 |
| --- | --- |
| `.nc` 文件类型识别 | 通过 |
| `.glb` 不作为刀路文件识别 | 通过 |
| Y 轴旋转包裹 G-code 导入 | 通过 |
| `ROTARY_WRAP_AXIS=Y` 元数据识别 | 通过 |
| Y 线性距离还原为 A 角度 | 通过，Y=50mm 且 100mm/圈时还原 A=180° |
| CSV 点位导入 | 通过 |

### 9. 下载与报告功能

| 功能 | 验证方式 | 结果 |
| --- | --- | --- |
| 下载 GLB | 代码检查：前端主动 `fetch` 模型并调用 `downloadBlob` | 通过 |
| 下载 AI STL | 同上 | 通过 |
| 下载空跑 NC | `downloadText()` 已改为插入 DOM 后触发下载 | 通过 |
| 加工包说明 | 同上，生成 `operator-note.md` | 通过 |
| 安全报告 MD/JSON | 同上，生成 `safety-report.md/json` | 通过 |
| ZIP 加工包 | `downloadBlob()` 已改为插入 DOM 后触发下载 | 通过 |

### 10. 浏览器 UI Smoke Test

浏览器：Codex 内置浏览器
地址：`http://127.0.0.1:5193/`

| 检查项 | 结果 |
| --- | --- |
| 页面标题 | `核雕3D生成与刀路系统 MVP` |
| 页面加载 | 通过 |
| 控制台错误 | 无 |
| 工艺页生成刀路入口 | 存在 |
| CAM 页 ZIP 加工包入口 | 存在 |
| CAM 页离料空跑 NC 入口 | 存在 |
| CAM 页加工包说明入口 | 存在 |

## 注意事项

1. `meigui.glb` 可以被系统导入、体检、生成刀路和生成预览，但素材本身存在大量退化面与非流形边，正式上机前建议先执行修复/重建网格。
2. 四轴精加工刀路点数较大，本轮测试约 209,148 点，生成和浏览器显示会比粗加工更慢，这是正常现象。
3. 三轴顶面投影只适合平面浮雕；如果要用于三轴控制器加旋转夹具，应优先使用“旋转包裹 X/Z + Y轴夹具”模式。
4. 正式加工 NC/TAP/TXT/CSV 仍受安全校验限制；模型文件、空跑 NC、安全报告、加工包说明可直接下载，用于验证和复核。

## 总结

本轮以 `meigui.glb` 为唯一 3D 测试素材，完成了导入、Mesh 体检、四轴粗加工、四轴精加工、三轴顶面投影、Y 轴旋转包裹、NC/G-code 反向导入、下载与报告、浏览器 UI smoke test。

结论：系统主要功能测试通过。素材质量报告提示 `meigui.glb` 需要修复后再正式上机，这属于加工前质量风险提示，不影响系统功能验收结论。
